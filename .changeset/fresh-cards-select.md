---
'@sdcorejs/angular': minor
---

Add the standalone `SdCard` and `SdCardGroup` components with accessible single, multiple, object-comparator, disabled, color, stable `autoId`, and standalone selection behavior through the `@sdcorejs/angular/components/card` entry point.
