import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';
import { Routes } from './layout.routing';
@NgModule({
  declarations: [],
  imports: [RouterModule.forChild(Routes)],
  providers: [],
})
export class SdLayoutModule {}
