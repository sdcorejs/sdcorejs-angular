var e=[{id:"unreleased",title:"Unreleased",suffix:null,date:null,anchor:"unreleased",unreleased:!0,packageVersions:[],summaryMarkdown:"",markdown:"",sections:[]},{id:"release-2-0",title:"Release 2.0",suffix:"2.0",date:"2026-08-15",anchor:"release-2-0",unreleased:!1,packageVersions:[{angularMajor:19,version:"19.2.0"},{angularMajor:20,version:"20.2.0"},{angularMajor:21,version:"21.2.0"}],summaryMarkdown:"Release suffix `2.0` publishes `19.2.0`, `20.2.0`, and `21.2.0` as a stable release across the maintained Angular lines.\n\n**Read the breaking-change entry below before upgrading.** The suffix jumps from `1.6` to `2.0` deliberately: the major digit of an npm version is locked to the Angular line, so it can never signal a break. The leading digit of the release suffix is the only signal this changelog has, and this release removes three exported surfaces. `npm install @sdcorejs/angular@^19.0.0` does not protect you here \u2014 the compiler will.",markdown:'Release suffix `2.0` publishes `19.2.0`, `20.2.0`, and `21.2.0` as a stable release across the maintained Angular lines.\n\n**Read the breaking-change entry below before upgrading.** The suffix jumps from `1.6` to `2.0` deliberately: the major digit of an npm version is locked to the Angular line, so it can never signal a break. The leading digit of the release suffix is the only signal this changelog has, and this release removes three exported surfaces. `npm install @sdcorejs/angular@^19.0.0` does not protect you here \u2014 the compiler will.\n\n### Changed (BREAKING for consumers)\n\n- **`SdLicenseService`, `SdBaseSecureComponent` and `ISdCoreConfiguration.licenseKey` are gone.** The package is MIT and public; no component has enforced a license since the gate was made dormant, so the three surfaces were carrying a promise the library does not keep. Removed outright rather than left deprecated: an exported base class whose constructor could still `throw` on a mis-configured host is a production hazard for anyone who inherits it.\n\n  Migration \u2014 delete `licenseKey` from the object you provide for `SD_CORE_CONFIGURATION` (the field no longer exists, so TypeScript will point at it):\n\n  ```diff\n   const sdCoreConfig: ISdCoreConfiguration = {\n  -  licenseKey: environment.sdLicenseKeys,\n     language: \'vi\',\n     format: { number: \'1.234.567,89\' },\n   };\n  ```\n\n  If you `extends SdBaseSecureComponent` anywhere, drop the `extends` clause and the `super()` call \u2014 the class did nothing except call `enforceLicense()`. The entry points `@sdcorejs/angular/services/license` and `@sdcorejs/angular/components/base` no longer resolve, and `services/license/sd-license.md` is no longer published. Consumers gating features by permission should keep using `*sdPermission`, which is unrelated and unchanged. `ISdCoreConfiguration.language` \u2014 always supported, never documented \u2014 is now in the configuration doc.\n\n### Changed\n\n- **`<sd-tree-select>`\'s trigger is now the same shell as `<sd-select>`.** It used to draw its own bordered `<button>`, which meant a form mixing the two showed two different field heights, corner radii, label positions and error placements, and the `label` input was accepted but never rendered at all. The trigger is a readonly `<input matInput>` inside a `<mat-form-field>`, so it inherits the floating label, the error subscript, the suffix slot and the size/appearance tokens from the same place every other control does. The only intentional difference is the suffix icon \u2014 `account_tree` instead of the select caret \u2014 signalling that this control opens a tree dialog rather than a dropdown; the clear `\xD7` replaces that icon while a value is present, exactly as in `<sd-select>`. New inputs `size`, `appearance`, `helperText` and `hideInlineError` come with the shell. Inside the modal, the search box is an `<sd-input>` and Cancel/Apply are `<sd-button>`s (`outline`/`secondary` and `fill`/`primary`); the body now sets its own `16px` padding, since `.sd-modal-body` is deliberately padding-free and the search field had been sitting flush against the modal edge. **Behaviour note:** `aria-invalid` now follows Material\'s convention \u2014 unset while the field is both `required` and empty, `true` for every other error \u2014 because the field is a Material control now. The message element is a `<mat-error>` (still tagged `data-tree-select-error`) instead of a bare `<div>`.\n\n- **A single-selection `<sd-tree>` renders radios instead of checkboxes.** `selector.single` already limited the selection to one node, but the control still looked like a checkbox \u2014 so the limit only announced itself after the user ticked a second node and watched the first one clear. `<sd-tree-select>` without `multiple` picks this up for free. The radios are deliberately not wrapped in a `<mat-radio-group>`: a group binds Arrow Up/Down to moving the selection, and tree rows already use those keys to navigate. Clicking the selected radio still clears it, exactly as the checkbox did. New public signal `selectionSingle()`.\n\n- **`<sd-time>`\'s picker trigger is sized like every other suffix affordance.** It was the last control still using `mat-icon-button`, whose 40px box and 48px touch target pushed the clock away from the right edge and gave it a visibly larger hit area than the icons in `<sd-date>` / `<sd-datetime>` / `<sd-date-range>`. It is now the same plain `<button class="sd-suffix-icon sd-suffix-btn">` those three already use, so the four controls finally line up. `<sd-time-range>` inherits the fix through its two endpoints.\n\n- **The selection quick-action bar stays closed when the selection has no actions**, in both `<sd-table>` and `<sd-tree>`. It used to open on any selection, so a table or tree that declares `selector.visible` without `selector.actions` floated a bar over the content whose entire payload was a count the checkboxes already showed and a `\xD7` to undo the click that opened it. The gate is the *resolved* action list, so a selection mixing rows with different permitted actions can resolve to zero and keep the bar closed too; deselect through the checkboxes. `<sd-tree>` exposes the decision as `selectionQuickActionOpened()`. This is what removes the bar from `<sd-tree-select>`\'s picker modal, where the tree declares no actions and Apply/Cancel already live in the footer.\n\n- **`<sd-time-range>` clears one endpoint at a time.** The single group-level `\xD7` sat after the "to" field, aligned with neither input, and could only empty both ends at once \u2014 so narrowing a range meant clearing it and retyping. `clearable` is forwarded to both `<sd-time>` endpoints instead, giving each field the same in-suffix clear `<sd-input>` has. The `\u2192` separator is sized to the input row height (`--sd-time-range-row-height`, resolved per `size`) rather than a hard-coded 56px, so it is centred against the fields instead of against the taller error-subscript box. `clear()` still empties both endpoints regardless of `clearable`. Removed: the `showClear` signal and the `.sd-time-range-clear` element.\n\n### Fixed\n\n- **`<sd-time>`\'s clear button never appeared.** `showClear` is a `computed` reading `formControl.value` \u2014 a plain property that emits no signal \u2014 so it settled on `false` at first paint, while the field was empty, and never recomputed. The bug had been dormant because `clearable` defaults to `false`; `<sd-time-range>` turning it on is what surfaced it. It now tracks the control\'s event stream *and* `valueModel`, the latter because the shared form connector writes programmatic values with `{ emitEvent: false }` and so never ticks the event stream at all.\n\n### Added\n\n- **Live demos for the six directives and seven pipes** that shipped as reference text only: `*sdDesktop`, `*sdMobile`, `[sdHoverCopy]`, `a[sdHref]`, `[sdScroll]`, `[sdTooltip]`, and `sdEmpty`, `sdFormatDate`, `sdFormatDatetime`, `sdFormatNumber`, `sdSafeHtml`, `sdTimeDifferent`, `sdView`. 28 new showcase sections; each page moves from a published-doc entry to a full demo page, so the Examples tab and the deep-linkable section anchors work there like they do for components.\n\n### Changed\n\n- **The `<sd-table>` column-setup dialog was rebuilt around its own density.** Every row is a list of on/off values, so the three `sd-switch` controls became `sd-checkbox` \u2014 a switch reads as a heavyweight, standalone setting and seven stacked pairs of them dominated the dialog. The title and width fields drop to `size="sm"` and every cell gains vertical padding, so text no longer sits flush against the row edge. The grid lines move from Material\'s default (`on-surface` at ~12%) to `outline-variant`, keep only horizontal rules, and sit inside a single rounded border, so the eye follows rows instead of a heavy lattice. The width and toggle columns are width-capped to give the header-title field the remaining space, the drag handle became a real `<button>` (keyboard/AT reachable, `grab` cursor, no button chrome) and rows highlight on hover. **New:** a localized hint above the table (`core.component.table.config.drag-hint`) advertises drag-to-reorder, which had no affordance beyond an unlabelled handle; turning **Hi\u1EC3n th\u1ECB** off now dims the row and disables its title/width/fixed/truncate editors \u2014 those settings do nothing for a hidden column \u2014 while leaving the display checkbox itself enabled so the column can be restored. Footer buttons lose their `mr-8`; the modal footer already lays them out with `gap: 8px`, so the margin only widened the gaps unevenly. Two new i18n keys across all five locales; first specs for the dialog (6).\n\n- **A `checkbox` field built in form-builder rendered nothing at runtime.** `item.component.html` is form-render\'s only type dispatcher and its two `@if / @else if` chains had no `checkbox` branch and no fallback, so the field silently disappeared \u2014 the palette, the attribute panel and the schema type had all shipped without the renderer half. New `lib-checkbox` control, built on the same shape as the other simple controls (same inputs, `defaultValue` applied only while the value is still `undefined` so unticking is not undone on the next render, and `setVariables` honoured). Note that `sd-checkbox` has no `required` input, so a checkbox field\'s `validate.required` is not enforced; the binding is deliberately absent rather than written and silently ignored.\n- **`sd-tree` rows were misaligned wherever checkboxes are shown** \u2014 which is every `<sd-tree-select>`, since it turns the selector on by default. `--sd-tree-control-size` is 28px and the chevron fills that slot exactly, but the checkbox only had its `height` clamped while Material\'s `.mdc-checkbox` is a content-box sized by `--mdc-checkbox-state-layer-size` (40px), so the real box overflowed the slot and the column drifted; the `transform: scale(0.85)` that tried to compensate could not, because it scales around the centre of the already-clamped box. Forcing the state-layer token to the slot size makes the checkbox fill it like the chevron does, and the scale hack is gone (the tick is now a full 18px, matching the 20px chevron and item icon). Rows also aligned their controls to the middle of the whole text block, so a label wrapping to two lines dragged the chevron and checkbox down with it; controls now align to the first line. Single-line rows are pixel-identical \u2014 the row padding moves 6px \u2192 8px precisely so that `flex-start` and `center` coincide against the 44px `min-height`.\n- **`form-generic`\'s table field used slots that do not exist.** Its side-drawer projected `<div sdTitle>` and `<div sdBody>`, but `sd-side-drawer` only accepts `sdHeaderLeft` / `sdHeaderRight` / `sdFooterLeft` / `sdFooterRight` plus a default `ng-content` \u2014 so both blocks fell into the drawer body, putting "T\u1EA1o m\u1EDBi/c\u1EADp nh\u1EADt" inside the content while the header stayed stuck on "Chi ti\u1EBFt". The drawer title is now driven by the actual task (add row / update row / detail, two new i18n keys), and the content sits in the default slot.\n- **The same drawer was indented and scrolled sideways.** Its content carried `margin: -16px`, which existed to cancel padding the drawer no longer applies (`.sd-side-drawer-content` is deliberately `padding: 0` so each consumer decides). The negative margin therefore dragged the content past the edge. Dropped, along with a `height: calc(100vh - 97px)` whose magic number no longer matched the header/footer. Fields stack with a declared `gap` instead of a hard `mt-16` on every one of them, including the first.\n- **Fields in a `group` sat flush against the section frame** \u2014 `.sd-section-body` is also `padding: 0` by design, and the fields only carried their own `px-8`, so the group now declares its own inset.\n- **The expression pill lost all of its styling inside the validation dialog.** `.fb-expr` was styled from `form-builder.component.scss` under `:host ::ng-deep`, which only reaches `expression-builder` while it sits inside the builder\'s DOM; the validation dialog renders through an overlay on `document.body`, so the rules never applied and the control collapsed into bare text ("FEEL / Thi\u1EBFt l\u1EADp bi\u1EC3u th\u1EE9c / functions"). The styles moved to the component that owns the markup, so they hold in any host.\n- **Deleting a nested condition removed the wrong row.** The level-2 delete button passed `idxLv1` \u2014 the index of the *parent group* \u2014 into the child condition list, so removing the second child deleted the first, and removing anything from a group positioned beyond the child count did nothing at all.\n\n### Changed\n\n- **The Save button in the table field\'s drawer sat at double the standard inset.** `.sd-side-drawer-footer` already pads 16px \u2014 every other `sdFooterRight` consumer in the library relies on that alone \u2014 but the projected wrapper also carried `pr-16`, for 32px total. The extra padding was a leftover from when this markup targeted a non-existent `sdFooter` slot and fell into the unpadded drawer body; the later rename to the real slot made it redundant. The sibling `text-right` was inert too (the slot is already a right-aligned flex container).\n- **The form-builder\'s JSON, variable and validation dialogs were reworked.** JSON moves from a bare `sd-textarea` to `sd-code-editor` with `language="json"` and is pretty-printed on open \u2014 it used to be a single unreadable line that could not realistically be hand-edited. The variable rows become bordered cards on a fixed `key / label / delete` grid (they were a flex row where both inputs stretched and the delete button rendered as large as a primary action), the add control changes from `<a href="javascript:;">` to a real button, and both dialogs gain an empty state. In the validation dialog the delete control was an `<sd-icon (click)>` \u2014 not a button, so it could not be reached or activated from the keyboard \u2014 and each rule was a `mat-elevation-z2` block with inline styles; rules are bordered cards now and the `function` branch finally labels its select. The toolbar buttons for variables and validations carry a count badge, so the form\'s configuration is visible without opening either dialog. Three new i18n keys across all five locales.\n\n### Fixed\n\n- **The `<sd-table>` footer showed two different backgrounds.** Material paints the paginator itself with `--mat-paginator-container-background-color`, whose default is `--mat-sys-surface` \u2014 not white \u2014 so the pager half of the footer sat on a different colour than the action half. The paginator is part of the footer rather than a surface of its own, so it is transparent now and takes the footer background.\n\n### Changed\n\n- **The `<sd-table>` paginator is more compact.** Its row kept Material\'s default `--mat-paginator-container-size` (56px min-height) and the previous/next buttons their 40px hover state layer, which is a lot of footer for a table whose rows are 36px. Both come down through Material\'s own tokens (40px row, 32px state layer, 20px icon) instead of overriding sizes on the elements.\n\n### Added\n\n- **`<sd-table>` can render content in the command column\'s header** through a new `sdTableCommandHeaderDef` template (exported as `SdTableCommandHeaderDefDirective`). That header cell has always been empty \u2014 it only reserves width for the per-row command buttons \u2014 so it is the natural home for a table-level action. It takes no field argument, unlike `sdTableCellDef`/`sdTableTitleDef`, because there is only one command column, and it renders no wrapper at all when no template is projected. `form-generic`\'s table field now puts its "add row" button there instead of below the table, which removes a full strip of vertical space and places "add" directly above the edit/delete buttons it belongs with.\n\n- **`<sd-select multiple>` can offer a "T\u1EA5t c\u1EA3" row** \u2014 new opt-in input `showSelectAll` (default `false`) renders a checkbox row at the top of the panel, but only when `items` is a static array or `Signal` (it stays hidden for `SdSearch` lazy items, where the full dataset is unknown so "all" has no meaning). Ticking selects every enabled item matching the current search text and **adds** to the existing selection, so entries selected outside the active filter survive; unticking removes only that scope. The scope is computed from the source array rather than the rendered list, so `limit` paging cannot silently leave items out, and items disabled through `disabledField` are never touched in either direction. The checkbox carries the usual checked/indeterminate/unchecked states. `sdChange`/`sdSelection` still fire only on panel close. New public method `toggleSelectAll()`, new i18n key `core.form.select.selectAll` across all five locales, and the row exposes `data-autoid="<autoId>-select-all"`. The row reuses the option\'s own `<mat-pseudo-checkbox>` markup so it inherits option sizing, spacing and theme colour instead of restyling a real `mat-checkbox`; because that element is `aria-hidden`, the row itself carries `role="checkbox"`, `aria-checked`, `tabindex` and Enter/Space.\n- **`SdToastData` + `SdToastType` are now exported** from `@sdcorejs/angular/services/notify`. Both already sat on the public surface of `SdNotifyService` (`toasts: WritableSignal<ToastData[]>`, `clearByType(type: ToastType)`) but were not exported, so a consumer could not annotate either. Aliased with the `Sd` prefix like the existing `NotifyOption as SdNotifyOption`. The toast container/toast components stay internal \u2014 they are not exported and `\u2026/services/notify/components` is not an entry point, contrary to what `sd-notify.md` used to claim.\n- **Docs for three previously undocumented entry points** \u2014 `components/form-generic` (form builder, form renderer, `sd-feel-expression`, the schema model and the configuration token), `components/ckeditor-styles`, and `forms/directives` (`sdSuffixDef` / `sdLabelDef` / `sdViewDef` / `sdItemDef`, with a table of which control reads which).\n- **CI actually runs the tests.** A `test` job now gates every npm publish, and a new `ci.yml` runs `check:sync`, lint, build, the unit suite and the repo script tests on every push and pull request. Previously nothing ran the ~4,600 specs at all, and `npm run lint:release` \u2014 a documented pre-release step \u2014 was red on a clean `main`.\n- **64 new i18n keys across all five locales** (vi, en, ja, ko, zh; 525 \u2192 589, parity verified) covering `query-bar`, `query-builder`, `tree`, `sd-hover-copy` and `modules/layout`, which previously shipped hardcoded Vietnamese and English.\n- **Coverage \u0111o tr\xEAn m\u1EABu s\u1ED1 \u0111\u1EA7y \u0111\u1EE7, v\xE0 threshold si\u1EBFt l\u1EA1i \u0111\xFAng s\u1ED1 \u0111o** \u2014 `coverage-includes.spec.ts` d\xF9ng `import.meta.webpackContext` k\xE9o m\u1ECDi file source v\xE0o test bundle, n\xEAn file kh\xF4ng spec n\xE0o import v\u1EABn \u0111\u01B0\u1EE3c instrument (tr\u01B0\u1EDBc \u0111\xF3 n\xF3 kh\xF4ng n\u1EB1m \u1EDF c\u1EA3 t\u1EED s\u1ED1 l\u1EABn m\u1EABu s\u1ED1, khi\u1EBFn vi\u1EC7c th\xEAm test \u0111\u1EA7u ti\xEAn v\xE0o m\u1ED9t v\xF9ng ch\u01B0a c\xF3 test c\xF3 th\u1EC3 l\xE0m % T\u1EE4T). `tsconfig.spec.json` m\u1EDF `include` sang `**/*.ts` \u2014 h\u1EC7 qu\u1EA3 ph\u1EE5 \u0111\xE1ng gi\xE1: `ng test` gi\u1EDD type-check c\u1EA3 source d\u01B0\u1EDBi path mapping source (kh\xF4ng ph\u1EA3i `dist`). Threshold 72/62/71/72 \u2192 **78/67/75/78**, s\xE1t s\u1ED1 \u0111o th\u1EADt 79.01/68.00/76.48/79.83 (tr\u01B0\u1EDBc \u0111\xE2y gate th\u1EA5p h\u01A1n th\u1EF1c t\u1EBF ~7pp n\xEAn g\u1EA7n nh\u01B0 kh\xF4ng ch\u1EB7n g\xEC).\n- **Karma kh\xF4ng c\xF2n b\xE1o xanh cho m\u1ED9t run R\u1ED6NG** \u2014 bundle n\u1EB7ng h\u01A1n l\xE0m Chrome b\u1ECB coi l\xE0 ch\u1EBFt tr\u01B0\u1EDBc khi ch\u1EA1y spec \u0111\u1EA7u ti\xEAn (`Disconnected \u2026 ping timeout`), 0 spec ch\u1EA1y m\xE0 exit code v\u1EABn 0. `browserNoActivityTimeout` / `browserDisconnectTimeout` / `captureTimeout` / `pingTimeout` \u0111\u01B0\u1EE3c n\xE2ng \u0111\u1EC3 ch\u1EDD \u0111\xFAng page load n\u1EB7ng (kh\xF4ng ph\u1EA3i \u0111\u1EC3 che spec ch\u1EADm).\n- **32 i18n key m\u1EDBi (m\u1ED7i key \xD7 5 locale, parity 599)** \u2014 nh\xF3m `query-bar` (popover: swap-field, placeholder ch\u1ECDn/nh\u1EADp gi\xE1 tr\u1ECB, loading lazy-values; field-picker empty; to\xE0n b\u1ED9 menu saved-filters), nh\xF3m `modules/layout/sidebar` (v2 rail + mobile v2: brand, nh\xF3m menu, \u0111\xF3ng menu, placeholder t\xECm ki\u1EBFm, primary-nav, "Th\xEAm"), v\xE0 `core.component.form-builder.select-item`. \u0110\xE2y l\xE0 c\xE1c chu\u1ED7i ti\u1EBFng Vi\u1EC7t hardcode n\u1EB1m D\u01AF\u1EDAI ng\u01B0\u1EE1ng budget n\xEAn checker c\u0169 kh\xF4ng b\xE1o.\n- **\u0110\u1EB7t t\xEAn b\u1ED9 l\u1ECDc \u0111\xE3 l\u01B0u d\xF9ng dialog th\u1EADt** \u2014 `SdQuerySavedFiltersMenu.promptSave()` chuy\u1EC3n t\u1EEB `window.prompt` sang `SdConfirmService.withInput` (nay l\xE0 `async`). `window.prompt` ch\u1EB7n UI thread, kh\xF4ng style/d\u1ECBch \u0111\u01B0\u1EE3c v\xE0 b\u1ECB v\xF4 hi\u1EC7u trong m\u1ED9t s\u1ED1 ng\u1EEF c\u1EA3nh nh\xFAng.\n- **\xD4 s\u1ED1 trong file Excel xu\u1EA5t ra gi\u1EDD TH\u1EACT S\u1EF0 c\xF3 number format** \u2014 `SdExcelService.export` t\u1EEBng set `numFmt = \'#\'` r\u1ED3i g\xE1n \u0111\xE8 c\u1EA3 object `style` ngay sau (exceljs thay th\u1EBF to\xE0n b\u1ED9 style khi g\xE1n), n\xEAn format ch\u01B0a t\u1EEBng \u0111\u01B0\u1EE3c \xE1p. Nay numFmt n\u1EB1m trong c\xF9ng object style: s\u1ED1 nguy\xEAn mang `\'0\'`, s\u1ED1 th\u1EADp ph\xE2n gi\u1EEF General (format \'#\' c\u0169 n\u1EBFu \u0111\u01B0\u1EE3c \xE1p s\u1EBD hi\u1EC3n th\u1ECB 0 th\xE0nh \xF4 tr\u1ED1ng v\xE0 l\xE0m tr\xF2n ph\u1EA7n hi\u1EC3n th\u1ECB c\u1EE7a s\u1ED1 l\u1EBB \u2014 n\xEAn kh\xF4ng gi\u1EEF). Di\u1EC7n m\u1EA1o file xu\u1EA5t \u0111\u1ED5i nh\u1EB9: \xF4 s\u1ED1 nguy\xEAn hi\u1EC3n th\u1ECB theo format \'0\'.\n- **`SdFormGenericValidation` + 3 symbol li\xEAn quan import \u0111\u01B0\u1EE3c b\u1EB1ng t\xEAn** \u2014 `form-generic-validation.model` gi\u1EDD n\u1EB1m trong models barrel c\u1EE7a `components/form-generic`; tr\u01B0\u1EDBc \u0111\xE2y `SdFormGeneric.validations` d\xF9ng c\xE1c type n\xE0y nh\u01B0ng consumer kh\xF4ng annotate \u0111\u01B0\u1EE3c bi\u1EBFn b\u1EB1ng ch\xFAng.\n- **`npm run check:i18n` xanh l\u1EA7n \u0111\u1EA7u v\xE0 \u0111\xE1ng tin** \u2014 stripper hi\u1EC3u regex literal (m\u1ED9t regex ch\u1EE9a quote/backtick t\u1EEBng \u0111\u1EA9y m\xE1y tr\u1EA1ng th\xE1i v\xE0o string v\u0129nh vi\u1EC5n, \u0111\u1EBFm nh\u1EA7m comment l\xE0 code \u2014 `excel.service.ts` b\u1ECB \u0111\u1EBFm 22 ch\u1ED7 to\xE0n comment); checker gi\u1EDD \u0111\u1ECDc marker `// @i18n-ignore` (d\xF2ng \u0111\xF3 ho\u1EB7c d\xF2ng ngay d\u01B0\u1EDBi) \u0111\xFAng nh\u01B0 i18n.md \u0111\xE3 h\u1EE9a; budget per-file t\xEDnh l\u1EA1i theo s\u1ED1 th\u1EF1c. +9 test (`npm run test:check-i18n`, g\u1ED9p v\xE0o `test:scripts`).\n\n### Security\n\n- **Bearer token could be sent to any host.** `SdKeycloakInterceptor` gated the `Authorization` header on `config.secureRoutes?.some(route => req.url.includes(route))` \u2014 an unanchored substring test with no host check. With the documented example `secureRoutes: [\'/api/v1\']`, a request to `https://analytics.thirdparty.com/api/v1/collect` received the access token. It now matches on parsed origin plus a segment-aware path prefix.\n- **`sdSafeHtml` was an unconditional sanitizer bypass.** It called `bypassSecurityTrustHtml()` on every value with no sanitize step, and `<sd-table>` pipes server-supplied cell data through it \u2014 a stored-XSS sink inside the library. It now **sanitizes by default**; the bypass is an explicit `| sdSafeHtml: true`. **BREAKING** for call sites relying on markup the sanitizer strips (inline `<svg>`, custom elements): add `: true` there.\n- **`javascript:` URLs could execute in the app origin.** Six sidebars gated `window.open` on `path.includes(\'http\')`, which `javascript:fetch(...)//http` satisfies. One passed no `noopener`, leaving the opened page with `window.opener`. All now parse the URL and always pass `noopener,noreferrer`.\n- **Lookalike hosts inherited auth hooks.** `SdApiService` and its interceptor selected a handler with `url.startsWith(host)`, so `https://api.example.com.attacker.tld/x` matched a handler configured for `https://api.example.com`.\n- **Every optional config degraded to permissive.** `SdAuthGuard`/`SdPortalGuard` returned `true` when their callback was unset; `hasPermission(\'\')` returned `true` while the permission guard fed it route data unchecked, so a misspelled `data` key was granted; `*sdPermission` rendered on an empty binding without consulting the service; `SdLayoutService` fell back to a mock `demo@example.com` signed-in user; `SdLicenseService` approved any hostname merely *containing* `localhost`. All now fail closed, with "no restriction" declared explicitly via the new `SD_PERMISSION_PUBLIC`.\n- **Root singletons cached identity with no way to clear it**, so state survived signout into the next user in the same tab. `SdPermissionService` gains `reset()`/`invalidate()`, the 401 latch became a time window instead of a permanent boolean, and `SdLayoutStorageService` gains a namespace resolver plus `clear()`. Permission codes are no longer mirrored into `sessionStorage` under a fixed UUID \u2014 readable *and writable* by any script on the origin; persistence is opt-in via `persistCache` and documented as not an authorization boundary.\n- `decodeToken` renamed **`readUnverifiedTokenClaims`**: it performs no signature or `exp` check and must never drive an authorization decision.\n- **Third-party network calls removed.** The offline interceptor pinged `jsonplaceholder.typicode.com` every 3 s through the app\'s full interceptor chain, leaking any unconditionally-attached auth header off-origin; it is now a same-origin probe behind `SD_NO_INTERNET_PROBE_URL`, issued via `HttpBackend`. `SdUtilities.getClientPublicIp()` called `api.ipify.org`; the endpoint is now a **required argument**.\n- API cache and in-flight dedupe keys came from a 32-bit rolling hash collapsed to 31 bits; a collision made two unrelated requests share one response body. Now SHA-256.\n\n### Fixed\n\n- **Accessibility.** Four a11y lint rules were switched **off**, and `aria-hidden="true"` had been used to silence tooling rather than fix markup \u2014 including on 10 real `<input>` elements and on wrappers containing an entire `mat-form-field`, an entire `<mat-calendar>`, the whole form-builder canvas, a whole sidebar menu branch, and three `mat-sort-header` divs, which made table sorting unusable with assistive tech. The rules are back on as `error`. Click-only controls are keyboard-operable, async states carry `role="status"`/`aria-live`, the `role="tablist"`/`treeitem` patterns are complete, and every `outline: none` has a `:focus-visible` replacement \u2014 notably `sd-button`, and `sd-modal`, which leaked a global focus-ring reset through `ViewEncapsulation.None`.\n- **Ch\u1EBF \u0111\u1ED9 `hideInlineError` gi\u1EDD c\xF3 ti\u1EBFng cho screen reader.** \u1EDE ch\u1EBF \u0111\u1ED9 n\xE0y th\xF4ng b\xE1o l\u1ED7i ch\u1EC9 s\u1ED1ng trong tooltip c\u1EE7a icon \u2014 kh\xF4ng c\xF3 text node n\xE0o \u0111\u1EC3 `aria-describedby` tr\u1ECF t\u1EDBi, n\xEAn tr\xECnh \u0111\u1ECDc m\xE0n h\xECnh kh\xF4ng \u0111\u1ECDc g\xEC c\u1EA3. Nay message \u0111\u01B0\u1EE3c render th\xEAm v\xE0o m\u1ED9t ph\u1EA7n t\u1EED ch\u1EC9-d\xE0nh-cho-screen-reader (`span.sd-visually-hidden`, class utility m\u1EDBi trong `assets/scss`) v\xE0 `aria-describedby` tr\u1ECF v\xE0o \u0111\xF3. \xC1p cho 12 control: input, input-number, textarea, select, autocomplete, date, date-range, datetime, chip, chip-calendar, time, editor.\n- **Hai wrapper `role="button"` b\u1ECDc ph\u1EA7n t\u1EED t\u01B0\u01A1ng t\xE1c \u0111\xE3 \u0111\u01B0\u1EE3c t\xE1i c\u1EA5u tr\xFAc** \u2014 AT g\u1ED9p c\u1EA3 c\u1EE5m th\xE0nh M\u1ED8T n\xFAt v\xE0 b\u1ECF ph\u1EA7n t\u1EED b\xEAn trong kh\u1ECFi accessibility tree. (a) H\xE0ng menu sidebar v1: `role` / `tabindex` / `aria-current` / Enter-Space chuy\u1EC3n xu\u1ED1ng ph\u1EA7n ti\xEAu \u0111\u1EC1, n\xFAt ghim th\xE0nh sibling \u0111\u1ED9c l\u1EADp, row gi\u1EEF `(click)` cho chu\u1ED9t. (b) Card tr\xEAn canvas c\u1EE7a form-builder (group / break / component \u2014 b\u1ECDc c\u1EA3 n\xFAt h\xE0nh \u0111\u1ED9ng l\u1EABn preview control c\xF3 `<input>` th\u1EADt): card b\u1ECF h\u1EB3n `role="button"`, affordance b\xE0n ph\xEDm/AT th\xE0nh m\u1ED9t `<button>` th\u1EADt \u1EDF \u0111\u1EA7u thanh action (`aria-pressed` + `aria-label`), v\xE0 thanh action hi\u1EC7n c\u1EA3 khi `:focus-within` \u0111\u1EC3 focus kh\xF4ng r\u01A1i v\xE0o n\xFAt v\xF4 h\xECnh.\n- **Form error messages that never rendered.** Writing to the validation control with `setValue(v, { emitEvent: false })` suppresses the async validator\'s completion, so the state signal never ticks and OnPush shows a red outline with no message. Fixed in `clear()` on input / input-number / autocomplete, in every write in date-range, and in `<sd-select>`\'s primary selection path.\n- **Validators a consumer attached to the public `formControl` were silently wiped** by destructive `clearValidators()`/`setValidators()` blocks in select, autocomplete, textarea, chip, chip-calendar and date-range.\n- **`<sd-date-range>` `[required]` could never fail** \u2014 `Validators.required` was applied to a control whose value is always an object.\n- **`<sd-date-range>` and `<sd-time-range>` no longer inject their internal endpoint controls into the consumer\'s FormGroup under random UUID keys**, which broke the value shape and `form.reset(obj)`.\n- **The shared form connector silently dropped the control** when the parent FormGroup already declared one at the same `name`: the user typed, `form.value` never changed, the form submitted empty, and nothing warned.\n- **`<sd-chip>` / `<sd-chip-calendar>` mutated the caller\'s array in place** and re-set the same reference, so `[(model)]` never emitted.\n- **A live OOM reproduction** in `query-builder` \u2014 `[items]="field.values || []"` allocated a fresh array every change-detection pass and `sd-select`\'s `toObservable(items)` loop froze the page. Fixed, along with the same allocation shape in query-bar, table, org-chart and tab-router.\n- **Table row identity was a content hash**, so `flattenTree`\'s `visited` Set **silently dropped the second of two content-equal rows from the rendered output**. Identity now comes from the new `option.rowKey`, falling back to per-object identity. **BREAKING** for tables using `preserveSelection` across a server re-fetch, or asserting on `data-autoid="\u2026-tree-toggle-<id>"` in E2E.\n- **A pure pipe mutated its own input** (`selection-visible`), appending duplicates without bound; a sibling pipe slept 500 ms inside an `async` pure pipe to paper over a change-detection ordering problem.\n- **Promises that never settled on the cancel path.** All six `SdConfirmService` methods, `SdDocxService.open()` and `SdUtilities.upload()` left the caller\'s `await` pending forever when the user backed out. `upload()` additionally resolved inside its loop, so validation failures after the first file were swallowed.\n- **Resources outliving their owner.** `<sd-modal>` had no destroy hook at all and orphaned its overlay; `sd-upload-file` left four drag listeners on a consumer-supplied element; `SdNotifyService` never destroyed the `<body>` node it created; `<sd-side-drawer>`\'s scroll lock could leave the page permanently unscrollable with two stacked drawers; `<sd-modal-resizable>` rewrote inline styles on every `.modal-resizable` element in the document; and 13 `setTimeout` sites across `forms/**` stored no handle, two of which called `detectChanges()` after destroy. `SdCacheService` kept every cached URL forever \u2014 now a bounded LRU via `maxMemoryEntries` (default 500).\n- **`SdLoadingService` leaked its overlay and stylesheet into the document permanently** under `provideAnimations()`: `Renderer2` from `AnimationRendererFactory` defers `removeChild` to the animation engine\'s next flush, which never happens on teardown.\n- SSR-unsafe global `document`/`window` access in `preview-image`, `SdLicenseService`, `global-error.handler` and a sidebar.\n- **The published package was not consumable.** The primary entry point exported **nothing** \u2014 `src/public-api.ts` was ten bare `import` statements, so `import { SdButton } from \'@sdcorejs/angular\'` resolved to nothing in every published version. `@angular/router` and `@angular/animations` were used but declared as neither dependency nor peer. `"sideEffects": false` was factually wrong (`Chart.register(...)` at module scope, five bare `prismjs` grammar imports), letting bundlers legally drop them with a silent production-only failure. `@omer-go/docx-parser-converter-ts` was a hard runtime dependency with zero imports.\n\n### Removed (BREAKING for consumers)\n\n- **Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/services/docx`** (`SdDocxService` \u2014 chuy\u1EC3n DOCX sang HTML qua pandoc). \u0110\xE2y l\xE0 entry point duy nh\u1EA5t ch\u1EE9a code vendored GPL-2.0-or-later (b\u1EA3n copy pandoc-wasm core) trong m\u1ED9t package khai MIT; xo\xE1 n\xF3 lo\u1EA1i b\u1ECF xung \u0111\u1ED9t gi\u1EA5y ph\xE9p thay v\xEC ph\u1EA3i relicense hay t\xE1ch package. Kh\xF4ng c\xF3 thay th\u1EBF trong th\u01B0 vi\u1EC7n \u2014 consumer c\u1EA7n ch\u1EE9c n\u0103ng n\xE0y t\u1EF1 t\xEDch h\u1EE3p [pandoc-wasm](https://github.com/pandoc/pandoc-wasm) (GPL) \u1EDF t\u1EA7ng app c\u1EE7a m\xECnh. Dependency `@bjorn3/browser_wasi_shim` (ch\u1EC9 docx d\xF9ng) v\xE0 3 key i18n `core.docx.*` \u0111i theo. File `NOTICE` (th\xEAm trong ch\xEDnh chu k\u1EF3 Unreleased n\xE0y) c\u0169ng g\u1EE1 v\xEC kh\xF4ng c\xF2n code vendored n\xE0o \u0111\u1EC3 ghi nh\u1EADn.\n- **B\u1ECF TO\xC0N B\u1ED8 re-export t\u1EEB `@sdcorejs/utils`** \u2014 nguy\xEAn t\u1EAFc: c\xE1i g\xEC `@sdcorejs/utils` s\u1EDF h\u1EEFu th\xEC import v\xE0 \u0111\u1ECDc doc \u1EDF `@sdcorejs/utils`, th\u01B0 vi\u1EC7n n\xE0y kh\xF4ng l\xE0m m\u1EB7t ti\u1EC1n trung gian n\u1EEFa. Package \u0111\xF3 \u0111\xE3 l\xE0 dependency runtime c\u1EE7a th\u01B0 vi\u1EC7n n\xEAn c\xF3 s\u1EB5n trong tree; th\xEAm n\xF3 v\xE0o `package.json` c\u1EE7a app khi import tr\u1EF1c ti\u1EBFp. C\u1EE5 th\u1EC3:\n  - `utilities/extensions` b\u1ECF re-export `ArrayUtilities` / `ColorUtilities` / `DateUtilities` / `NumberUtilities` / `StringUtilities` / `ValidationUtilities` / `Utilities` / `BrowserUtilities` \u2192 import t\u1EEB `@sdcorejs/utils/fns`. Entry point c\xF2n l\u1EA1i `ObjectUtilities`, `SdUtilities` (14 h\xE0m c\xE0i \u0111\u1EB7t c\u1EE5c b\u1ED9) v\xE0 nh\xF3m url-safety (`sdParseUrl`, `sdOpenExternal`, \u2026) \u2014 code local.\n  - `utilities/models` b\u1ECF re-export `Color`, `Size`, `Order`, `NestedKeyOf`, `Filter*`, `Operator*`, `QueryReq` / `PagingReq` / `PagingRes`, `MaybeAsync` / `resolveMaybeAsync` / `normalizeAsync`, `ValidationPatternType` / `ValidationPattern` \u2192 `@sdcorejs/utils/models`; `VALIDATION_PATTERNS`, `EMPTY_STR` \u2192 `@sdcorejs/utils/constants`. Entry point c\xF2n l\u1EA1i `icon.model` + `unwrap-signal.model` (local). B\u1EA3ng "\u1EDF \u0111\xE2u b\xE2y gi\u1EDD" \u0111\u1EA7y \u0111\u1EE7 trong `utilities/models/models.md`.\n  - **Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/models`** \u2014 n\xF3 ch\u1EC9 re-export `Language` + `SUPPORTED_LANGUAGES`; import t\u1EEB `@sdcorejs/utils/models` / `@sdcorejs/utils/constants`. `@sdcorejs/angular/i18n` c\u0169ng th\xF4i re-export hai t\xEAn n\xE0y.\n  - Spec c\u1EE7a c\xE1c file re-export thu\u1EA7n (array/color/date/number/string extension, pattern.model) xo\xE1 theo \u2014 ch\xFAng test h\xE0nh vi c\u1EE7a `@sdcorejs/utils`, thu\u1ED9c repo \u0111\xF3.\n- **Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/components/document-builder`** (`<sd-document-builder>` \u2014 tr\xECnh d\u1EF1ng t\xE0i li\u1EC7u CKEditor cho m\u1EABu h\u1EE3p \u0111\u1ED3ng, legacy t\u1EEB h\u1EC7 c\u0169). Kh\xF4ng c\xF3 thay th\u1EBF; so\u1EA1n th\u1EA3o rich-text v\u1EABn c\xF2n `<sd-editor>` / `<sd-mini-editor>`. 6 key i18n `core.component.document-builder.*` \u0111i theo. `<sd-ckeditor-styles>` gi\u1EEF nguy\xEAn (editor/mini-editor v\u1EABn nh\xFAng).\n\n### Changed (BREAKING for consumers)\n\n\u0110\u1EE3t \u0111\u1ED5i t\xEAn public API c\xF3 ch\u1EE7 \u0111\xEDch, **xo\xE1 h\u1EB3n t\xEAn c\u0169, kh\xF4ng gi\u1EEF alias `@deprecated`**. L\xFD do g\u1ED9p m\u1ED9t l\u1EA7n: tag format `^\\d+\\.\\d+$` kho\xE1 major theo Angular line, n\xEAn semver KH\xD4NG th\u1EC3 ph\xE1t t\xEDn hi\u1EC7u breaking \u2014 m\u1ED7i l\u1EA7n ho\xE3n th\xEC chi ph\xED migration v\u1EC1 sau l\u1EA1i t\u0103ng.\n\n- **Pipe `translate` \u2192 `sdTranslate`** (class `TranslatePipe` \u2192 `SdTranslatePipe`). T\xEAn c\u0169 \u0111\u1EE5ng tr\u1EF1c di\u1EC7n pipe c\xF9ng t\xEAn c\u1EE7a `@ngx-translate/core`; c\u1EA3 hai \u0111\u1EC1u standalone n\xEAn app d\xF9ng song song KH\xD4NG import chung v\xE0o m\u1ED9t component \u0111\u01B0\u1EE3c. Migration: find/replace `| translate` \u2192 `| sdTranslate` v\xE0 `TranslatePipe` \u2192 `SdTranslatePipe`.\n- **`MatPaginatorIntlCro` \u2192 `SdTablePaginatorIntl`** (`@sdcorejs/angular/components/table`). T\xEAn c\u0169 l\xE0 v\u1EBFt copy-paste t\u1EEB v\xED d\u1EE5 locale Croatia, l\u1EA1i mang ti\u1EC1n t\u1ED1 `Mat` g\xE2y hi\u1EC3u nh\u1EA7m l\xE0 API c\u1EE7a Angular Material.\n- **Sidebar b\u1ECF t\xEAn kh\xF4ng ti\u1EC1n t\u1ED1** \u2014 `SidebarV2Component` \u2192 `SdSidebarV2` (`sidebar-v2` \u2192 `sd-sidebar-v2`), t\u01B0\u01A1ng t\u1EF1 `V3`, `MobileV1/V2/V3`; `SidebarMobileOverlayComponent` \u2192 `SdSidebarMobileOverlay`. N\u1ED9i b\u1ED9: `SidebarV1Component` \u2192 `SdSidebarV1`, `SidebarComponent` (selector `sidebar` \u2014 t\xEAn element to\xE0n c\u1EE5c c\u1EF1c d\u1EC5 \u0111\u1EE5ng) \u2192 `SdSidebarV1Panel` (`sd-sidebar-v1-panel`).\n- **Helper PascalCase c\u1EE7a `form-generic` chuy\u1EC3n sang `sd` + camelCase** \u2014 `GenerateId` / `GenerateKey` / `GetAttributes` / `GetComponentAttributes` / `GetDatetimeValue` / `GetVariableAttributes` / `EvaluateExpression` / `ExpressionToJavascriptExpression` / `TemplateToCondition` / `SdFormatComponent` \u2192 `sdGenerateId` / `sdGenerateKey` / `sdGetAttributes` / \u2026 Ch\xFAng l\xE0 h\xE0m thu\u1EA7n nh\u01B0ng \u0111\u1ECDc nh\u01B0 class (`new GenerateId()` nh\xECn v\u1EABn h\u1EE3p l\u1EC7). Const \u0111i k\xE8m: `DayInfoTypes` / `DayInfoPreviouses` / `AttributeOperators` / `TableColumnTypes` / `FormBuilderComponents` / `COMPONENT_ICONS` \u2192 `SD_DAY_INFO_TYPES` / `SD_DAY_INFO_PREVIOUSES` / `SD_ATTRIBUTE_OPERATORS` / `SD_TABLE_COLUMN_TYPES` / `SD_FORM_BUILDER_COMPONENTS` / `SD_COMPONENT_ICONS`.\n- **Namespace `qb*` / `QB_*` / `Qb*` c\u1EE7a `query-builder` \u2192 `sdQb*` / `SD_QB_*` / `SdQb*`** \u2014 v\xED d\u1EE5 `qbNewRule` \u2192 `sdQbNewRule`, `isQbGroup` \u2192 `sdIsQbGroup`, `QB_OPERATORS_BY_TYPE` \u2192 `SD_QB_OPERATORS_BY_TYPE`, `QbGroup` / `QbRule` / `QbToken` \u2192 `SdQbGroup` / `SdQbRule` / `SdQbToken`. C\xE1c type \u0111\xE3 \u0111\xFAng ti\u1EC1n t\u1ED1 (`SdQbRelativeUnit`, \u2026) gi\u1EEF nguy\xEAn.\n- **`sdLoadError` \u2192 `loadError` tr\xEAn `<sd-tree-select>` v\xE0 `<sd-entity-picker>`** \u2014 hai component n\xE0y l\xE0 ch\u1ED7 duy nh\u1EA5t d\xF9ng ti\u1EC1n t\u1ED1 `sd` cho s\u1EF1 ki\u1EC7n load-error, trong khi `<sd-tree>` v\xE0 `<sd-preview-pdf>` (c\xF9ng s\u1EF1 ki\u1EC7n, c\xF9ng `SdTreeLoadErrorEvent` \u1EDF nh\xF3m tree) \u0111\xE3 publish t\xEAn `loadError` t\u1EEB tr\u01B0\u1EDBc. G\u1ED9p v\u1EC1 `loadError`. Migration: find/replace `(sdLoadError)` \u2192 `(loadError)` tr\xEAn hai element \u0111\xF3.\n- **Xo\xE1 to\xE0n b\u1ED9 alias `@deprecated` d\u01B0\u1EDBi `utilities/**`** \u2014 `SdColor`, `SdSize`, `SdNestedKeyOf`, `SdOrder`, `SdFilter*`, `SdQueryReq` / `SdPagingReq` / `SdPagingRes`, `SdMaybeAsync` / `SdResolveMaybeAsync` / `SdNormalizeAsync`, `SD_EMPTY_STR`, `hslToHex` / `rgbToHex`, `SdPatternType` / `SdPatternCommon` / `SdPatternCommons`, v\xE0 c\xE1c member l\u1ED7i th\u1EDDi tr\xEAn `StringUtilities` (`REGEX_PHONE_VN`, `REGEX_IDVN`, `REGEX_IDVN_OR_PASSPORT`, `isValidEmail`, `isValidPhone`, `isValidCode`). B\u1EA3ng \xE1nh x\u1EA1 \u0111\u1EA7y \u0111\u1EE7 \u1EDF `utilities/models/models.md`. \u26A0\uFE0F Ri\xEAng nh\xF3m pattern KH\xD4NG ph\u1EA3i \u0111\u1ED5i t\xEAn thu\u1EA7n: field `regex` nay l\xE0 `pattern`, v\xE0 3 member \u0111\u1ED5i t\xEAn k\xE8m i18n key (`PHONE_VN` \u2192 `VN_PHONE`, `IDVN` \u2192 `VN_ID`, `IDVN_OR_PASSPORT` \u2192 `VN_ID_OR_PASSPORT`). `<sd-input [pattern]>` v\u1EABn nh\u1EADn 3 chu\u1ED7i c\u0169 qua b\u1EA3ng alias n\u1ED9i b\u1ED9 n\xEAn template kh\xF4ng v\u1EE1.\n\n### Changed\n\n- **Si\u1EBFt default generic `any` \u2192 `unknown` tr\xEAn 19 file model** (82 khai b\xE1o), g\u1ED3m `components/tree/src/tree.model.ts` (to\xE0n b\u1ED9 type export), `components/table/src/services/table-filter/table-filter.model.ts`, `components/table/src/models/table-column.model.ts`, `components/query-bar/src/query-bar.model.ts`, `forms/models/**`, \u2026 Consumer vi\u1EBFt `SdTableColumn<User>` hay `SdTreeOption<User>` t\u1EEB nay \u0111\u01B0\u1EE3c suy lu\u1EADn ki\u1EC3u th\u1EADt v\xE0 b\xE1o l\u1ED7i khi d\xF9ng sai, thay v\xEC im l\u1EB7ng nh\u01B0 v\u1EDBi `any`. Th\xEAm `SdTableColumnAnyRow` cho v\xE0i v\u1ECB tr\xED tham s\u1ED1 n\u1ED9i b\u1ED9 row-agnostic (gi\u1EA3i th\xEDch trong `table-column.model.ts`).\n- **`SdUtilities` KH\xD4NG b\u1ECB xo\xE1** \u2014 marker `@deprecated` c\u0169 m\xF4 t\u1EA3 sai: \u0111\xE2y kh\xF4ng ph\u1EA3i alias m\xE0 l\xE0 14 h\xE0m c\xE0i \u0111\u1EB7t c\u1EE5c b\u1ED9. Marker \u0111\xE3 \u0111\u01B0\u1EE3c g\u1EE1, implementation gi\u1EEF nguy\xEAn.\n\n### Kh\xF4ng \u0111\u1ED5i (c\xF3 ch\u1EE7 \u0111\xEDch)\n\n- **T\xEAn output KH\xD4NG \u0111\u1ED5i h\xE0ng lo\u1EA1t.** \u0110\u1EE3t n\xE0y t\u1EEBng th\xEAm ti\u1EC1n t\u1ED1 `sd` cho ~73 output (`close` \u2192 `sdClose`, `search` \u2192 `sdSearch`, \u2026) nh\u01B0ng \u0111\xE3 \u0111\u1EA3o l\u1EA1i tr\u01B0\u1EDBc khi release: t\xEAn kh\xF4ng ti\u1EC1n t\u1ED1 l\xE0 t\xEAn \u0111\xE3 publish t\u1EDBi 1.6, quen thu\u1ED9c, v\xE0 kh\u1EDBp th\xF4ng l\u1EC7 Angular Material (output kh\xF4ng mang prefix). H\u1EC7 qu\u1EA3 ch\u1EA5p nh\u1EADn: surface output v\u1EABn **mixed** \u2014 16 t\xEAn `sd*` \u0111\xE3 publish t\u1EEB 1.4/1.6 (`sdChanges`, `sdBlur`, `sdFocus`, `sdClosed`, `sdCloseError` tr\xEAn modal/side-drawer/inform/import-excel, `sdCancel` tr\xEAn job-progress, `sdAction`, `sdKeydown`, \u2026) gi\u1EEF nguy\xEAn v\xEC \u0111\u1ED5i ch\xFAng m\u1EDBi l\xE0 breaking. Mu\u1ED1n th\u1ED1ng nh\u1EA5t to\xE0n b\u1ED9 th\xEC c\u1EA7n m\u1ED9t \u0111\u1EE3t ri\xEAng k\xE8m codemod.\n- `click` c\u1EE7a `<sd-button>` gi\u1EEF nguy\xEAn t\xEAn. \u0110\u1ED5i sang `sdClick` s\u1EBD khi\u1EBFn m\u1ECDi `(click)` s\u1EB5n c\xF3 c\u1EE7a consumer im l\u1EB7ng r\u01A1i v\u1EC1 DOM event g\u1ED1c \u2014 v\u1EABn ch\u1EA1y nh\u01B0ng \u0111\u1ED5i payload v\xE0 b\u1ECF qua gate `disabled`. \u0110\xE2y l\xE0 ki\u1EC3u breaking "\xE2m th\u1EA7m" nguy hi\u1EC3m nh\u1EA5t, c\u1EA7n m\u1ED9t \u0111\u1EE3t ri\xEAng k\xE8m codemod.\n- M\u1ED9t s\u1ED1 v\xF9ng v\u1EABn gi\u1EEF default generic `any` v\xEC `unknown` g\xE2y lan qu\xE1 r\u1ED9ng (`import-excel`, `services/excel`, ph\u1EA7n l\u1EDBn `components/table/src/models/**`, `form-generic-component.model.ts`) \u2014 chi ti\u1EBFt trong ghi ch\xFA review c\u1EE7a \u0111\u1EE3t n\xE0y.',sections:[{key:"changed",title:"Changed (BREAKING for consumers)",level:3,anchor:"release-2-0-changed-breaking-for-consumers",markdown:"- **`SdLicenseService`, `SdBaseSecureComponent` and `ISdCoreConfiguration.licenseKey` are gone.** The package is MIT and public; no component has enforced a license since the gate was made dormant, so the three surfaces were carrying a promise the library does not keep. Removed outright rather than left deprecated: an exported base class whose constructor could still `throw` on a mis-configured host is a production hazard for anyone who inherits it.\n\n  Migration \u2014 delete `licenseKey` from the object you provide for `SD_CORE_CONFIGURATION` (the field no longer exists, so TypeScript will point at it):\n\n  ```diff\n   const sdCoreConfig: ISdCoreConfiguration = {\n  -  licenseKey: environment.sdLicenseKeys,\n     language: 'vi',\n     format: { number: '1.234.567,89' },\n   };\n  ```\n\n  If you `extends SdBaseSecureComponent` anywhere, drop the `extends` clause and the `super()` call \u2014 the class did nothing except call `enforceLicense()`. The entry points `@sdcorejs/angular/services/license` and `@sdcorejs/angular/components/base` no longer resolve, and `services/license/sd-license.md` is no longer published. Consumers gating features by permission should keep using `*sdPermission`, which is unrelated and unchanged. `ISdCoreConfiguration.language` \u2014 always supported, never documented \u2014 is now in the configuration doc.",items:[{text:"**`SdLicenseService`, `SdBaseSecureComponent` and `ISdCoreConfiguration.licenseKey` are gone.** The package is MIT and public; no component has enforced a license since the gate was made dormant, so the three surfaces were carrying a promise the library does not keep. Removed outright rather than left deprecated: an exported base class whose constructor could still `throw` on a mis-configured host is a production hazard for anyone who inherits it.",ordered:!1,children:[{text:"licenseKey: environment.sdLicenseKeys,",ordered:!1,children:[]}]}],children:[]},{key:"changed",title:"Changed",level:3,anchor:"release-2-0-changed",markdown:"- **`<sd-tree-select>`'s trigger is now the same shell as `<sd-select>`.** It used to draw its own bordered `<button>`, which meant a form mixing the two showed two different field heights, corner radii, label positions and error placements, and the `label` input was accepted but never rendered at all. The trigger is a readonly `<input matInput>` inside a `<mat-form-field>`, so it inherits the floating label, the error subscript, the suffix slot and the size/appearance tokens from the same place every other control does. The only intentional difference is the suffix icon \u2014 `account_tree` instead of the select caret \u2014 signalling that this control opens a tree dialog rather than a dropdown; the clear `\xD7` replaces that icon while a value is present, exactly as in `<sd-select>`. New inputs `size`, `appearance`, `helperText` and `hideInlineError` come with the shell. Inside the modal, the search box is an `<sd-input>` and Cancel/Apply are `<sd-button>`s (`outline`/`secondary` and `fill`/`primary`); the body now sets its own `16px` padding, since `.sd-modal-body` is deliberately padding-free and the search field had been sitting flush against the modal edge. **Behaviour note:** `aria-invalid` now follows Material's convention \u2014 unset while the field is both `required` and empty, `true` for every other error \u2014 because the field is a Material control now. The message element is a `<mat-error>` (still tagged `data-tree-select-error`) instead of a bare `<div>`.\n\n- **A single-selection `<sd-tree>` renders radios instead of checkboxes.** `selector.single` already limited the selection to one node, but the control still looked like a checkbox \u2014 so the limit only announced itself after the user ticked a second node and watched the first one clear. `<sd-tree-select>` without `multiple` picks this up for free. The radios are deliberately not wrapped in a `<mat-radio-group>`: a group binds Arrow Up/Down to moving the selection, and tree rows already use those keys to navigate. Clicking the selected radio still clears it, exactly as the checkbox did. New public signal `selectionSingle()`.\n\n- **`<sd-time>`'s picker trigger is sized like every other suffix affordance.** It was the last control still using `mat-icon-button`, whose 40px box and 48px touch target pushed the clock away from the right edge and gave it a visibly larger hit area than the icons in `<sd-date>` / `<sd-datetime>` / `<sd-date-range>`. It is now the same plain `<button class=\"sd-suffix-icon sd-suffix-btn\">` those three already use, so the four controls finally line up. `<sd-time-range>` inherits the fix through its two endpoints.\n\n- **The selection quick-action bar stays closed when the selection has no actions**, in both `<sd-table>` and `<sd-tree>`. It used to open on any selection, so a table or tree that declares `selector.visible` without `selector.actions` floated a bar over the content whose entire payload was a count the checkboxes already showed and a `\xD7` to undo the click that opened it. The gate is the *resolved* action list, so a selection mixing rows with different permitted actions can resolve to zero and keep the bar closed too; deselect through the checkboxes. `<sd-tree>` exposes the decision as `selectionQuickActionOpened()`. This is what removes the bar from `<sd-tree-select>`'s picker modal, where the tree declares no actions and Apply/Cancel already live in the footer.\n\n- **`<sd-time-range>` clears one endpoint at a time.** The single group-level `\xD7` sat after the \"to\" field, aligned with neither input, and could only empty both ends at once \u2014 so narrowing a range meant clearing it and retyping. `clearable` is forwarded to both `<sd-time>` endpoints instead, giving each field the same in-suffix clear `<sd-input>` has. The `\u2192` separator is sized to the input row height (`--sd-time-range-row-height`, resolved per `size`) rather than a hard-coded 56px, so it is centred against the fields instead of against the taller error-subscript box. `clear()` still empties both endpoints regardless of `clearable`. Removed: the `showClear` signal and the `.sd-time-range-clear` element.",items:[{text:"**`<sd-tree-select>`'s trigger is now the same shell as `<sd-select>`.** It used to draw its own bordered `<button>`, which meant a form mixing the two showed two different field heights, corner radii, label positions and error placements, and the `label` input was accepted but never rendered at all. The trigger is a readonly `<input matInput>` inside a `<mat-form-field>`, so it inherits the floating label, the error subscript, the suffix slot and the size/appearance tokens from the same place every other control does. The only intentional difference is the suffix icon \u2014 `account_tree` instead of the select caret \u2014 signalling that this control opens a tree dialog rather than a dropdown; the clear `\xD7` replaces that icon while a value is present, exactly as in `<sd-select>`. New inputs `size`, `appearance`, `helperText` and `hideInlineError` come with the shell. Inside the modal, the search box is an `<sd-input>` and Cancel/Apply are `<sd-button>`s (`outline`/`secondary` and `fill`/`primary`); the body now sets its own `16px` padding, since `.sd-modal-body` is deliberately padding-free and the search field had been sitting flush against the modal edge. **Behaviour note:** `aria-invalid` now follows Material's convention \u2014 unset while the field is both `required` and empty, `true` for every other error \u2014 because the field is a Material control now. The message element is a `<mat-error>` (still tagged `data-tree-select-error`) instead of a bare `<div>`.",ordered:!1,children:[]},{text:"**A single-selection `<sd-tree>` renders radios instead of checkboxes.** `selector.single` already limited the selection to one node, but the control still looked like a checkbox \u2014 so the limit only announced itself after the user ticked a second node and watched the first one clear. `<sd-tree-select>` without `multiple` picks this up for free. The radios are deliberately not wrapped in a `<mat-radio-group>`: a group binds Arrow Up/Down to moving the selection, and tree rows already use those keys to navigate. Clicking the selected radio still clears it, exactly as the checkbox did. New public signal `selectionSingle()`.",ordered:!1,children:[]},{text:'**`<sd-time>`\'s picker trigger is sized like every other suffix affordance.** It was the last control still using `mat-icon-button`, whose 40px box and 48px touch target pushed the clock away from the right edge and gave it a visibly larger hit area than the icons in `<sd-date>` / `<sd-datetime>` / `<sd-date-range>`. It is now the same plain `<button class="sd-suffix-icon sd-suffix-btn">` those three already use, so the four controls finally line up. `<sd-time-range>` inherits the fix through its two endpoints.',ordered:!1,children:[]},{text:"**The selection quick-action bar stays closed when the selection has no actions**, in both `<sd-table>` and `<sd-tree>`. It used to open on any selection, so a table or tree that declares `selector.visible` without `selector.actions` floated a bar over the content whose entire payload was a count the checkboxes already showed and a `\xD7` to undo the click that opened it. The gate is the *resolved* action list, so a selection mixing rows with different permitted actions can resolve to zero and keep the bar closed too; deselect through the checkboxes. `<sd-tree>` exposes the decision as `selectionQuickActionOpened()`. This is what removes the bar from `<sd-tree-select>`'s picker modal, where the tree declares no actions and Apply/Cancel already live in the footer.",ordered:!1,children:[]},{text:'**`<sd-time-range>` clears one endpoint at a time.** The single group-level `\xD7` sat after the "to" field, aligned with neither input, and could only empty both ends at once \u2014 so narrowing a range meant clearing it and retyping. `clearable` is forwarded to both `<sd-time>` endpoints instead, giving each field the same in-suffix clear `<sd-input>` has. The `\u2192` separator is sized to the input row height (`--sd-time-range-row-height`, resolved per `size`) rather than a hard-coded 56px, so it is centred against the fields instead of against the taller error-subscript box. `clear()` still empties both endpoints regardless of `clearable`. Removed: the `showClear` signal and the `.sd-time-range-clear` element.',ordered:!1,children:[]}],children:[]},{key:"fixed",title:"Fixed",level:3,anchor:"release-2-0-fixed",markdown:"- **`<sd-time>`'s clear button never appeared.** `showClear` is a `computed` reading `formControl.value` \u2014 a plain property that emits no signal \u2014 so it settled on `false` at first paint, while the field was empty, and never recomputed. The bug had been dormant because `clearable` defaults to `false`; `<sd-time-range>` turning it on is what surfaced it. It now tracks the control's event stream *and* `valueModel`, the latter because the shared form connector writes programmatic values with `{ emitEvent: false }` and so never ticks the event stream at all.",items:[{text:"**`<sd-time>`'s clear button never appeared.** `showClear` is a `computed` reading `formControl.value` \u2014 a plain property that emits no signal \u2014 so it settled on `false` at first paint, while the field was empty, and never recomputed. The bug had been dormant because `clearable` defaults to `false`; `<sd-time-range>` turning it on is what surfaced it. It now tracks the control's event stream *and* `valueModel`, the latter because the shared form connector writes programmatic values with `{ emitEvent: false }` and so never ticks the event stream at all.",ordered:!1,children:[]}],children:[]},{key:"added",title:"Added",level:3,anchor:"release-2-0-added",markdown:"- **Live demos for the six directives and seven pipes** that shipped as reference text only: `*sdDesktop`, `*sdMobile`, `[sdHoverCopy]`, `a[sdHref]`, `[sdScroll]`, `[sdTooltip]`, and `sdEmpty`, `sdFormatDate`, `sdFormatDatetime`, `sdFormatNumber`, `sdSafeHtml`, `sdTimeDifferent`, `sdView`. 28 new showcase sections; each page moves from a published-doc entry to a full demo page, so the Examples tab and the deep-linkable section anchors work there like they do for components.",items:[{text:"**Live demos for the six directives and seven pipes** that shipped as reference text only: `*sdDesktop`, `*sdMobile`, `[sdHoverCopy]`, `a[sdHref]`, `[sdScroll]`, `[sdTooltip]`, and `sdEmpty`, `sdFormatDate`, `sdFormatDatetime`, `sdFormatNumber`, `sdSafeHtml`, `sdTimeDifferent`, `sdView`. 28 new showcase sections; each page moves from a published-doc entry to a full demo page, so the Examples tab and the deep-linkable section anchors work there like they do for components.",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed",level:3,anchor:"release-2-0-changed-2",markdown:'- **The `<sd-table>` column-setup dialog was rebuilt around its own density.** Every row is a list of on/off values, so the three `sd-switch` controls became `sd-checkbox` \u2014 a switch reads as a heavyweight, standalone setting and seven stacked pairs of them dominated the dialog. The title and width fields drop to `size="sm"` and every cell gains vertical padding, so text no longer sits flush against the row edge. The grid lines move from Material\'s default (`on-surface` at ~12%) to `outline-variant`, keep only horizontal rules, and sit inside a single rounded border, so the eye follows rows instead of a heavy lattice. The width and toggle columns are width-capped to give the header-title field the remaining space, the drag handle became a real `<button>` (keyboard/AT reachable, `grab` cursor, no button chrome) and rows highlight on hover. **New:** a localized hint above the table (`core.component.table.config.drag-hint`) advertises drag-to-reorder, which had no affordance beyond an unlabelled handle; turning **Hi\u1EC3n th\u1ECB** off now dims the row and disables its title/width/fixed/truncate editors \u2014 those settings do nothing for a hidden column \u2014 while leaving the display checkbox itself enabled so the column can be restored. Footer buttons lose their `mr-8`; the modal footer already lays them out with `gap: 8px`, so the margin only widened the gaps unevenly. Two new i18n keys across all five locales; first specs for the dialog (6).\n\n- **A `checkbox` field built in form-builder rendered nothing at runtime.** `item.component.html` is form-render\'s only type dispatcher and its two `@if / @else if` chains had no `checkbox` branch and no fallback, so the field silently disappeared \u2014 the palette, the attribute panel and the schema type had all shipped without the renderer half. New `lib-checkbox` control, built on the same shape as the other simple controls (same inputs, `defaultValue` applied only while the value is still `undefined` so unticking is not undone on the next render, and `setVariables` honoured). Note that `sd-checkbox` has no `required` input, so a checkbox field\'s `validate.required` is not enforced; the binding is deliberately absent rather than written and silently ignored.\n- **`sd-tree` rows were misaligned wherever checkboxes are shown** \u2014 which is every `<sd-tree-select>`, since it turns the selector on by default. `--sd-tree-control-size` is 28px and the chevron fills that slot exactly, but the checkbox only had its `height` clamped while Material\'s `.mdc-checkbox` is a content-box sized by `--mdc-checkbox-state-layer-size` (40px), so the real box overflowed the slot and the column drifted; the `transform: scale(0.85)` that tried to compensate could not, because it scales around the centre of the already-clamped box. Forcing the state-layer token to the slot size makes the checkbox fill it like the chevron does, and the scale hack is gone (the tick is now a full 18px, matching the 20px chevron and item icon). Rows also aligned their controls to the middle of the whole text block, so a label wrapping to two lines dragged the chevron and checkbox down with it; controls now align to the first line. Single-line rows are pixel-identical \u2014 the row padding moves 6px \u2192 8px precisely so that `flex-start` and `center` coincide against the 44px `min-height`.\n- **`form-generic`\'s table field used slots that do not exist.** Its side-drawer projected `<div sdTitle>` and `<div sdBody>`, but `sd-side-drawer` only accepts `sdHeaderLeft` / `sdHeaderRight` / `sdFooterLeft` / `sdFooterRight` plus a default `ng-content` \u2014 so both blocks fell into the drawer body, putting "T\u1EA1o m\u1EDBi/c\u1EADp nh\u1EADt" inside the content while the header stayed stuck on "Chi ti\u1EBFt". The drawer title is now driven by the actual task (add row / update row / detail, two new i18n keys), and the content sits in the default slot.\n- **The same drawer was indented and scrolled sideways.** Its content carried `margin: -16px`, which existed to cancel padding the drawer no longer applies (`.sd-side-drawer-content` is deliberately `padding: 0` so each consumer decides). The negative margin therefore dragged the content past the edge. Dropped, along with a `height: calc(100vh - 97px)` whose magic number no longer matched the header/footer. Fields stack with a declared `gap` instead of a hard `mt-16` on every one of them, including the first.\n- **Fields in a `group` sat flush against the section frame** \u2014 `.sd-section-body` is also `padding: 0` by design, and the fields only carried their own `px-8`, so the group now declares its own inset.\n- **The expression pill lost all of its styling inside the validation dialog.** `.fb-expr` was styled from `form-builder.component.scss` under `:host ::ng-deep`, which only reaches `expression-builder` while it sits inside the builder\'s DOM; the validation dialog renders through an overlay on `document.body`, so the rules never applied and the control collapsed into bare text ("FEEL / Thi\u1EBFt l\u1EADp bi\u1EC3u th\u1EE9c / functions"). The styles moved to the component that owns the markup, so they hold in any host.\n- **Deleting a nested condition removed the wrong row.** The level-2 delete button passed `idxLv1` \u2014 the index of the *parent group* \u2014 into the child condition list, so removing the second child deleted the first, and removing anything from a group positioned beyond the child count did nothing at all.',items:[{text:'**The `<sd-table>` column-setup dialog was rebuilt around its own density.** Every row is a list of on/off values, so the three `sd-switch` controls became `sd-checkbox` \u2014 a switch reads as a heavyweight, standalone setting and seven stacked pairs of them dominated the dialog. The title and width fields drop to `size="sm"` and every cell gains vertical padding, so text no longer sits flush against the row edge. The grid lines move from Material\'s default (`on-surface` at ~12%) to `outline-variant`, keep only horizontal rules, and sit inside a single rounded border, so the eye follows rows instead of a heavy lattice. The width and toggle columns are width-capped to give the header-title field the remaining space, the drag handle became a real `<button>` (keyboard/AT reachable, `grab` cursor, no button chrome) and rows highlight on hover. **New:** a localized hint above the table (`core.component.table.config.drag-hint`) advertises drag-to-reorder, which had no affordance beyond an unlabelled handle; turning **Hi\u1EC3n th\u1ECB** off now dims the row and disables its title/width/fixed/truncate editors \u2014 those settings do nothing for a hidden column \u2014 while leaving the display checkbox itself enabled so the column can be restored. Footer buttons lose their `mr-8`; the modal footer already lays them out with `gap: 8px`, so the margin only widened the gaps unevenly. Two new i18n keys across all five locales; first specs for the dialog (6).',ordered:!1,children:[]},{text:"**A `checkbox` field built in form-builder rendered nothing at runtime.** `item.component.html` is form-render's only type dispatcher and its two `@if / @else if` chains had no `checkbox` branch and no fallback, so the field silently disappeared \u2014 the palette, the attribute panel and the schema type had all shipped without the renderer half. New `lib-checkbox` control, built on the same shape as the other simple controls (same inputs, `defaultValue` applied only while the value is still `undefined` so unticking is not undone on the next render, and `setVariables` honoured). Note that `sd-checkbox` has no `required` input, so a checkbox field's `validate.required` is not enforced; the binding is deliberately absent rather than written and silently ignored.",ordered:!1,children:[]},{text:"**`sd-tree` rows were misaligned wherever checkboxes are shown** \u2014 which is every `<sd-tree-select>`, since it turns the selector on by default. `--sd-tree-control-size` is 28px and the chevron fills that slot exactly, but the checkbox only had its `height` clamped while Material's `.mdc-checkbox` is a content-box sized by `--mdc-checkbox-state-layer-size` (40px), so the real box overflowed the slot and the column drifted; the `transform: scale(0.85)` that tried to compensate could not, because it scales around the centre of the already-clamped box. Forcing the state-layer token to the slot size makes the checkbox fill it like the chevron does, and the scale hack is gone (the tick is now a full 18px, matching the 20px chevron and item icon). Rows also aligned their controls to the middle of the whole text block, so a label wrapping to two lines dragged the chevron and checkbox down with it; controls now align to the first line. Single-line rows are pixel-identical \u2014 the row padding moves 6px \u2192 8px precisely so that `flex-start` and `center` coincide against the 44px `min-height`.",ordered:!1,children:[]},{text:'**`form-generic`\'s table field used slots that do not exist.** Its side-drawer projected `<div sdTitle>` and `<div sdBody>`, but `sd-side-drawer` only accepts `sdHeaderLeft` / `sdHeaderRight` / `sdFooterLeft` / `sdFooterRight` plus a default `ng-content` \u2014 so both blocks fell into the drawer body, putting "T\u1EA1o m\u1EDBi/c\u1EADp nh\u1EADt" inside the content while the header stayed stuck on "Chi ti\u1EBFt". The drawer title is now driven by the actual task (add row / update row / detail, two new i18n keys), and the content sits in the default slot.',ordered:!1,children:[]},{text:"**The same drawer was indented and scrolled sideways.** Its content carried `margin: -16px`, which existed to cancel padding the drawer no longer applies (`.sd-side-drawer-content` is deliberately `padding: 0` so each consumer decides). The negative margin therefore dragged the content past the edge. Dropped, along with a `height: calc(100vh - 97px)` whose magic number no longer matched the header/footer. Fields stack with a declared `gap` instead of a hard `mt-16` on every one of them, including the first.",ordered:!1,children:[]},{text:"**Fields in a `group` sat flush against the section frame** \u2014 `.sd-section-body` is also `padding: 0` by design, and the fields only carried their own `px-8`, so the group now declares its own inset.",ordered:!1,children:[]},{text:'**The expression pill lost all of its styling inside the validation dialog.** `.fb-expr` was styled from `form-builder.component.scss` under `:host ::ng-deep`, which only reaches `expression-builder` while it sits inside the builder\'s DOM; the validation dialog renders through an overlay on `document.body`, so the rules never applied and the control collapsed into bare text ("FEEL / Thi\u1EBFt l\u1EADp bi\u1EC3u th\u1EE9c / functions"). The styles moved to the component that owns the markup, so they hold in any host.',ordered:!1,children:[]},{text:"**Deleting a nested condition removed the wrong row.** The level-2 delete button passed `idxLv1` \u2014 the index of the *parent group* \u2014 into the child condition list, so removing the second child deleted the first, and removing anything from a group positioned beyond the child count did nothing at all.",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed",level:3,anchor:"release-2-0-changed-3",markdown:'- **The Save button in the table field\'s drawer sat at double the standard inset.** `.sd-side-drawer-footer` already pads 16px \u2014 every other `sdFooterRight` consumer in the library relies on that alone \u2014 but the projected wrapper also carried `pr-16`, for 32px total. The extra padding was a leftover from when this markup targeted a non-existent `sdFooter` slot and fell into the unpadded drawer body; the later rename to the real slot made it redundant. The sibling `text-right` was inert too (the slot is already a right-aligned flex container).\n- **The form-builder\'s JSON, variable and validation dialogs were reworked.** JSON moves from a bare `sd-textarea` to `sd-code-editor` with `language="json"` and is pretty-printed on open \u2014 it used to be a single unreadable line that could not realistically be hand-edited. The variable rows become bordered cards on a fixed `key / label / delete` grid (they were a flex row where both inputs stretched and the delete button rendered as large as a primary action), the add control changes from `<a href="javascript:;">` to a real button, and both dialogs gain an empty state. In the validation dialog the delete control was an `<sd-icon (click)>` \u2014 not a button, so it could not be reached or activated from the keyboard \u2014 and each rule was a `mat-elevation-z2` block with inline styles; rules are bordered cards now and the `function` branch finally labels its select. The toolbar buttons for variables and validations carry a count badge, so the form\'s configuration is visible without opening either dialog. Three new i18n keys across all five locales.',items:[{text:"**The Save button in the table field's drawer sat at double the standard inset.** `.sd-side-drawer-footer` already pads 16px \u2014 every other `sdFooterRight` consumer in the library relies on that alone \u2014 but the projected wrapper also carried `pr-16`, for 32px total. The extra padding was a leftover from when this markup targeted a non-existent `sdFooter` slot and fell into the unpadded drawer body; the later rename to the real slot made it redundant. The sibling `text-right` was inert too (the slot is already a right-aligned flex container).",ordered:!1,children:[]},{text:'**The form-builder\'s JSON, variable and validation dialogs were reworked.** JSON moves from a bare `sd-textarea` to `sd-code-editor` with `language="json"` and is pretty-printed on open \u2014 it used to be a single unreadable line that could not realistically be hand-edited. The variable rows become bordered cards on a fixed `key / label / delete` grid (they were a flex row where both inputs stretched and the delete button rendered as large as a primary action), the add control changes from `<a href="javascript:;">` to a real button, and both dialogs gain an empty state. In the validation dialog the delete control was an `<sd-icon (click)>` \u2014 not a button, so it could not be reached or activated from the keyboard \u2014 and each rule was a `mat-elevation-z2` block with inline styles; rules are bordered cards now and the `function` branch finally labels its select. The toolbar buttons for variables and validations carry a count badge, so the form\'s configuration is visible without opening either dialog. Three new i18n keys across all five locales.',ordered:!1,children:[]}],children:[]},{key:"fixed",title:"Fixed",level:3,anchor:"release-2-0-fixed-2",markdown:"- **The `<sd-table>` footer showed two different backgrounds.** Material paints the paginator itself with `--mat-paginator-container-background-color`, whose default is `--mat-sys-surface` \u2014 not white \u2014 so the pager half of the footer sat on a different colour than the action half. The paginator is part of the footer rather than a surface of its own, so it is transparent now and takes the footer background.",items:[{text:"**The `<sd-table>` footer showed two different backgrounds.** Material paints the paginator itself with `--mat-paginator-container-background-color`, whose default is `--mat-sys-surface` \u2014 not white \u2014 so the pager half of the footer sat on a different colour than the action half. The paginator is part of the footer rather than a surface of its own, so it is transparent now and takes the footer background.",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed",level:3,anchor:"release-2-0-changed-4",markdown:"- **The `<sd-table>` paginator is more compact.** Its row kept Material's default `--mat-paginator-container-size` (56px min-height) and the previous/next buttons their 40px hover state layer, which is a lot of footer for a table whose rows are 36px. Both come down through Material's own tokens (40px row, 32px state layer, 20px icon) instead of overriding sizes on the elements.",items:[{text:"**The `<sd-table>` paginator is more compact.** Its row kept Material's default `--mat-paginator-container-size` (56px min-height) and the previous/next buttons their 40px hover state layer, which is a lot of footer for a table whose rows are 36px. Both come down through Material's own tokens (40px row, 32px state layer, 20px icon) instead of overriding sizes on the elements.",ordered:!1,children:[]}],children:[]},{key:"added",title:"Added",level:3,anchor:"release-2-0-added-2",markdown:'- **`<sd-table>` can render content in the command column\'s header** through a new `sdTableCommandHeaderDef` template (exported as `SdTableCommandHeaderDefDirective`). That header cell has always been empty \u2014 it only reserves width for the per-row command buttons \u2014 so it is the natural home for a table-level action. It takes no field argument, unlike `sdTableCellDef`/`sdTableTitleDef`, because there is only one command column, and it renders no wrapper at all when no template is projected. `form-generic`\'s table field now puts its "add row" button there instead of below the table, which removes a full strip of vertical space and places "add" directly above the edit/delete buttons it belongs with.\n\n- **`<sd-select multiple>` can offer a "T\u1EA5t c\u1EA3" row** \u2014 new opt-in input `showSelectAll` (default `false`) renders a checkbox row at the top of the panel, but only when `items` is a static array or `Signal` (it stays hidden for `SdSearch` lazy items, where the full dataset is unknown so "all" has no meaning). Ticking selects every enabled item matching the current search text and **adds** to the existing selection, so entries selected outside the active filter survive; unticking removes only that scope. The scope is computed from the source array rather than the rendered list, so `limit` paging cannot silently leave items out, and items disabled through `disabledField` are never touched in either direction. The checkbox carries the usual checked/indeterminate/unchecked states. `sdChange`/`sdSelection` still fire only on panel close. New public method `toggleSelectAll()`, new i18n key `core.form.select.selectAll` across all five locales, and the row exposes `data-autoid="<autoId>-select-all"`. The row reuses the option\'s own `<mat-pseudo-checkbox>` markup so it inherits option sizing, spacing and theme colour instead of restyling a real `mat-checkbox`; because that element is `aria-hidden`, the row itself carries `role="checkbox"`, `aria-checked`, `tabindex` and Enter/Space.\n- **`SdToastData` + `SdToastType` are now exported** from `@sdcorejs/angular/services/notify`. Both already sat on the public surface of `SdNotifyService` (`toasts: WritableSignal<ToastData[]>`, `clearByType(type: ToastType)`) but were not exported, so a consumer could not annotate either. Aliased with the `Sd` prefix like the existing `NotifyOption as SdNotifyOption`. The toast container/toast components stay internal \u2014 they are not exported and `\u2026/services/notify/components` is not an entry point, contrary to what `sd-notify.md` used to claim.\n- **Docs for three previously undocumented entry points** \u2014 `components/form-generic` (form builder, form renderer, `sd-feel-expression`, the schema model and the configuration token), `components/ckeditor-styles`, and `forms/directives` (`sdSuffixDef` / `sdLabelDef` / `sdViewDef` / `sdItemDef`, with a table of which control reads which).\n- **CI actually runs the tests.** A `test` job now gates every npm publish, and a new `ci.yml` runs `check:sync`, lint, build, the unit suite and the repo script tests on every push and pull request. Previously nothing ran the ~4,600 specs at all, and `npm run lint:release` \u2014 a documented pre-release step \u2014 was red on a clean `main`.\n- **64 new i18n keys across all five locales** (vi, en, ja, ko, zh; 525 \u2192 589, parity verified) covering `query-bar`, `query-builder`, `tree`, `sd-hover-copy` and `modules/layout`, which previously shipped hardcoded Vietnamese and English.\n- **Coverage \u0111o tr\xEAn m\u1EABu s\u1ED1 \u0111\u1EA7y \u0111\u1EE7, v\xE0 threshold si\u1EBFt l\u1EA1i \u0111\xFAng s\u1ED1 \u0111o** \u2014 `coverage-includes.spec.ts` d\xF9ng `import.meta.webpackContext` k\xE9o m\u1ECDi file source v\xE0o test bundle, n\xEAn file kh\xF4ng spec n\xE0o import v\u1EABn \u0111\u01B0\u1EE3c instrument (tr\u01B0\u1EDBc \u0111\xF3 n\xF3 kh\xF4ng n\u1EB1m \u1EDF c\u1EA3 t\u1EED s\u1ED1 l\u1EABn m\u1EABu s\u1ED1, khi\u1EBFn vi\u1EC7c th\xEAm test \u0111\u1EA7u ti\xEAn v\xE0o m\u1ED9t v\xF9ng ch\u01B0a c\xF3 test c\xF3 th\u1EC3 l\xE0m % T\u1EE4T). `tsconfig.spec.json` m\u1EDF `include` sang `**/*.ts` \u2014 h\u1EC7 qu\u1EA3 ph\u1EE5 \u0111\xE1ng gi\xE1: `ng test` gi\u1EDD type-check c\u1EA3 source d\u01B0\u1EDBi path mapping source (kh\xF4ng ph\u1EA3i `dist`). Threshold 72/62/71/72 \u2192 **78/67/75/78**, s\xE1t s\u1ED1 \u0111o th\u1EADt 79.01/68.00/76.48/79.83 (tr\u01B0\u1EDBc \u0111\xE2y gate th\u1EA5p h\u01A1n th\u1EF1c t\u1EBF ~7pp n\xEAn g\u1EA7n nh\u01B0 kh\xF4ng ch\u1EB7n g\xEC).\n- **Karma kh\xF4ng c\xF2n b\xE1o xanh cho m\u1ED9t run R\u1ED6NG** \u2014 bundle n\u1EB7ng h\u01A1n l\xE0m Chrome b\u1ECB coi l\xE0 ch\u1EBFt tr\u01B0\u1EDBc khi ch\u1EA1y spec \u0111\u1EA7u ti\xEAn (`Disconnected \u2026 ping timeout`), 0 spec ch\u1EA1y m\xE0 exit code v\u1EABn 0. `browserNoActivityTimeout` / `browserDisconnectTimeout` / `captureTimeout` / `pingTimeout` \u0111\u01B0\u1EE3c n\xE2ng \u0111\u1EC3 ch\u1EDD \u0111\xFAng page load n\u1EB7ng (kh\xF4ng ph\u1EA3i \u0111\u1EC3 che spec ch\u1EADm).\n- **32 i18n key m\u1EDBi (m\u1ED7i key \xD7 5 locale, parity 599)** \u2014 nh\xF3m `query-bar` (popover: swap-field, placeholder ch\u1ECDn/nh\u1EADp gi\xE1 tr\u1ECB, loading lazy-values; field-picker empty; to\xE0n b\u1ED9 menu saved-filters), nh\xF3m `modules/layout/sidebar` (v2 rail + mobile v2: brand, nh\xF3m menu, \u0111\xF3ng menu, placeholder t\xECm ki\u1EBFm, primary-nav, "Th\xEAm"), v\xE0 `core.component.form-builder.select-item`. \u0110\xE2y l\xE0 c\xE1c chu\u1ED7i ti\u1EBFng Vi\u1EC7t hardcode n\u1EB1m D\u01AF\u1EDAI ng\u01B0\u1EE1ng budget n\xEAn checker c\u0169 kh\xF4ng b\xE1o.\n- **\u0110\u1EB7t t\xEAn b\u1ED9 l\u1ECDc \u0111\xE3 l\u01B0u d\xF9ng dialog th\u1EADt** \u2014 `SdQuerySavedFiltersMenu.promptSave()` chuy\u1EC3n t\u1EEB `window.prompt` sang `SdConfirmService.withInput` (nay l\xE0 `async`). `window.prompt` ch\u1EB7n UI thread, kh\xF4ng style/d\u1ECBch \u0111\u01B0\u1EE3c v\xE0 b\u1ECB v\xF4 hi\u1EC7u trong m\u1ED9t s\u1ED1 ng\u1EEF c\u1EA3nh nh\xFAng.\n- **\xD4 s\u1ED1 trong file Excel xu\u1EA5t ra gi\u1EDD TH\u1EACT S\u1EF0 c\xF3 number format** \u2014 `SdExcelService.export` t\u1EEBng set `numFmt = \'#\'` r\u1ED3i g\xE1n \u0111\xE8 c\u1EA3 object `style` ngay sau (exceljs thay th\u1EBF to\xE0n b\u1ED9 style khi g\xE1n), n\xEAn format ch\u01B0a t\u1EEBng \u0111\u01B0\u1EE3c \xE1p. Nay numFmt n\u1EB1m trong c\xF9ng object style: s\u1ED1 nguy\xEAn mang `\'0\'`, s\u1ED1 th\u1EADp ph\xE2n gi\u1EEF General (format \'#\' c\u0169 n\u1EBFu \u0111\u01B0\u1EE3c \xE1p s\u1EBD hi\u1EC3n th\u1ECB 0 th\xE0nh \xF4 tr\u1ED1ng v\xE0 l\xE0m tr\xF2n ph\u1EA7n hi\u1EC3n th\u1ECB c\u1EE7a s\u1ED1 l\u1EBB \u2014 n\xEAn kh\xF4ng gi\u1EEF). Di\u1EC7n m\u1EA1o file xu\u1EA5t \u0111\u1ED5i nh\u1EB9: \xF4 s\u1ED1 nguy\xEAn hi\u1EC3n th\u1ECB theo format \'0\'.\n- **`SdFormGenericValidation` + 3 symbol li\xEAn quan import \u0111\u01B0\u1EE3c b\u1EB1ng t\xEAn** \u2014 `form-generic-validation.model` gi\u1EDD n\u1EB1m trong models barrel c\u1EE7a `components/form-generic`; tr\u01B0\u1EDBc \u0111\xE2y `SdFormGeneric.validations` d\xF9ng c\xE1c type n\xE0y nh\u01B0ng consumer kh\xF4ng annotate \u0111\u01B0\u1EE3c bi\u1EBFn b\u1EB1ng ch\xFAng.\n- **`npm run check:i18n` xanh l\u1EA7n \u0111\u1EA7u v\xE0 \u0111\xE1ng tin** \u2014 stripper hi\u1EC3u regex literal (m\u1ED9t regex ch\u1EE9a quote/backtick t\u1EEBng \u0111\u1EA9y m\xE1y tr\u1EA1ng th\xE1i v\xE0o string v\u0129nh vi\u1EC5n, \u0111\u1EBFm nh\u1EA7m comment l\xE0 code \u2014 `excel.service.ts` b\u1ECB \u0111\u1EBFm 22 ch\u1ED7 to\xE0n comment); checker gi\u1EDD \u0111\u1ECDc marker `// @i18n-ignore` (d\xF2ng \u0111\xF3 ho\u1EB7c d\xF2ng ngay d\u01B0\u1EDBi) \u0111\xFAng nh\u01B0 i18n.md \u0111\xE3 h\u1EE9a; budget per-file t\xEDnh l\u1EA1i theo s\u1ED1 th\u1EF1c. +9 test (`npm run test:check-i18n`, g\u1ED9p v\xE0o `test:scripts`).',items:[{text:'**`<sd-table>` can render content in the command column\'s header** through a new `sdTableCommandHeaderDef` template (exported as `SdTableCommandHeaderDefDirective`). That header cell has always been empty \u2014 it only reserves width for the per-row command buttons \u2014 so it is the natural home for a table-level action. It takes no field argument, unlike `sdTableCellDef`/`sdTableTitleDef`, because there is only one command column, and it renders no wrapper at all when no template is projected. `form-generic`\'s table field now puts its "add row" button there instead of below the table, which removes a full strip of vertical space and places "add" directly above the edit/delete buttons it belongs with.',ordered:!1,children:[]},{text:'**`<sd-select multiple>` can offer a "T\u1EA5t c\u1EA3" row** \u2014 new opt-in input `showSelectAll` (default `false`) renders a checkbox row at the top of the panel, but only when `items` is a static array or `Signal` (it stays hidden for `SdSearch` lazy items, where the full dataset is unknown so "all" has no meaning). Ticking selects every enabled item matching the current search text and **adds** to the existing selection, so entries selected outside the active filter survive; unticking removes only that scope. The scope is computed from the source array rather than the rendered list, so `limit` paging cannot silently leave items out, and items disabled through `disabledField` are never touched in either direction. The checkbox carries the usual checked/indeterminate/unchecked states. `sdChange`/`sdSelection` still fire only on panel close. New public method `toggleSelectAll()`, new i18n key `core.form.select.selectAll` across all five locales, and the row exposes `data-autoid="<autoId>-select-all"`. The row reuses the option\'s own `<mat-pseudo-checkbox>` markup so it inherits option sizing, spacing and theme colour instead of restyling a real `mat-checkbox`; because that element is `aria-hidden`, the row itself carries `role="checkbox"`, `aria-checked`, `tabindex` and Enter/Space.',ordered:!1,children:[]},{text:"**`SdToastData` + `SdToastType` are now exported** from `@sdcorejs/angular/services/notify`. Both already sat on the public surface of `SdNotifyService` (`toasts: WritableSignal<ToastData[]>`, `clearByType(type: ToastType)`) but were not exported, so a consumer could not annotate either. Aliased with the `Sd` prefix like the existing `NotifyOption as SdNotifyOption`. The toast container/toast components stay internal \u2014 they are not exported and `\u2026/services/notify/components` is not an entry point, contrary to what `sd-notify.md` used to claim.",ordered:!1,children:[]},{text:"**Docs for three previously undocumented entry points** \u2014 `components/form-generic` (form builder, form renderer, `sd-feel-expression`, the schema model and the configuration token), `components/ckeditor-styles`, and `forms/directives` (`sdSuffixDef` / `sdLabelDef` / `sdViewDef` / `sdItemDef`, with a table of which control reads which).",ordered:!1,children:[]},{text:"**CI actually runs the tests.** A `test` job now gates every npm publish, and a new `ci.yml` runs `check:sync`, lint, build, the unit suite and the repo script tests on every push and pull request. Previously nothing ran the ~4,600 specs at all, and `npm run lint:release` \u2014 a documented pre-release step \u2014 was red on a clean `main`.",ordered:!1,children:[]},{text:"**64 new i18n keys across all five locales** (vi, en, ja, ko, zh; 525 \u2192 589, parity verified) covering `query-bar`, `query-builder`, `tree`, `sd-hover-copy` and `modules/layout`, which previously shipped hardcoded Vietnamese and English.",ordered:!1,children:[]},{text:"**Coverage \u0111o tr\xEAn m\u1EABu s\u1ED1 \u0111\u1EA7y \u0111\u1EE7, v\xE0 threshold si\u1EBFt l\u1EA1i \u0111\xFAng s\u1ED1 \u0111o** \u2014 `coverage-includes.spec.ts` d\xF9ng `import.meta.webpackContext` k\xE9o m\u1ECDi file source v\xE0o test bundle, n\xEAn file kh\xF4ng spec n\xE0o import v\u1EABn \u0111\u01B0\u1EE3c instrument (tr\u01B0\u1EDBc \u0111\xF3 n\xF3 kh\xF4ng n\u1EB1m \u1EDF c\u1EA3 t\u1EED s\u1ED1 l\u1EABn m\u1EABu s\u1ED1, khi\u1EBFn vi\u1EC7c th\xEAm test \u0111\u1EA7u ti\xEAn v\xE0o m\u1ED9t v\xF9ng ch\u01B0a c\xF3 test c\xF3 th\u1EC3 l\xE0m % T\u1EE4T). `tsconfig.spec.json` m\u1EDF `include` sang `**/*.ts` \u2014 h\u1EC7 qu\u1EA3 ph\u1EE5 \u0111\xE1ng gi\xE1: `ng test` gi\u1EDD type-check c\u1EA3 source d\u01B0\u1EDBi path mapping source (kh\xF4ng ph\u1EA3i `dist`). Threshold 72/62/71/72 \u2192 **78/67/75/78**, s\xE1t s\u1ED1 \u0111o th\u1EADt 79.01/68.00/76.48/79.83 (tr\u01B0\u1EDBc \u0111\xE2y gate th\u1EA5p h\u01A1n th\u1EF1c t\u1EBF ~7pp n\xEAn g\u1EA7n nh\u01B0 kh\xF4ng ch\u1EB7n g\xEC).",ordered:!1,children:[]},{text:"**Karma kh\xF4ng c\xF2n b\xE1o xanh cho m\u1ED9t run R\u1ED6NG** \u2014 bundle n\u1EB7ng h\u01A1n l\xE0m Chrome b\u1ECB coi l\xE0 ch\u1EBFt tr\u01B0\u1EDBc khi ch\u1EA1y spec \u0111\u1EA7u ti\xEAn (`Disconnected \u2026 ping timeout`), 0 spec ch\u1EA1y m\xE0 exit code v\u1EABn 0. `browserNoActivityTimeout` / `browserDisconnectTimeout` / `captureTimeout` / `pingTimeout` \u0111\u01B0\u1EE3c n\xE2ng \u0111\u1EC3 ch\u1EDD \u0111\xFAng page load n\u1EB7ng (kh\xF4ng ph\u1EA3i \u0111\u1EC3 che spec ch\u1EADm).",ordered:!1,children:[]},{text:'**32 i18n key m\u1EDBi (m\u1ED7i key \xD7 5 locale, parity 599)** \u2014 nh\xF3m `query-bar` (popover: swap-field, placeholder ch\u1ECDn/nh\u1EADp gi\xE1 tr\u1ECB, loading lazy-values; field-picker empty; to\xE0n b\u1ED9 menu saved-filters), nh\xF3m `modules/layout/sidebar` (v2 rail + mobile v2: brand, nh\xF3m menu, \u0111\xF3ng menu, placeholder t\xECm ki\u1EBFm, primary-nav, "Th\xEAm"), v\xE0 `core.component.form-builder.select-item`. \u0110\xE2y l\xE0 c\xE1c chu\u1ED7i ti\u1EBFng Vi\u1EC7t hardcode n\u1EB1m D\u01AF\u1EDAI ng\u01B0\u1EE1ng budget n\xEAn checker c\u0169 kh\xF4ng b\xE1o.',ordered:!1,children:[]},{text:"**\u0110\u1EB7t t\xEAn b\u1ED9 l\u1ECDc \u0111\xE3 l\u01B0u d\xF9ng dialog th\u1EADt** \u2014 `SdQuerySavedFiltersMenu.promptSave()` chuy\u1EC3n t\u1EEB `window.prompt` sang `SdConfirmService.withInput` (nay l\xE0 `async`). `window.prompt` ch\u1EB7n UI thread, kh\xF4ng style/d\u1ECBch \u0111\u01B0\u1EE3c v\xE0 b\u1ECB v\xF4 hi\u1EC7u trong m\u1ED9t s\u1ED1 ng\u1EEF c\u1EA3nh nh\xFAng.",ordered:!1,children:[]},{text:"**\xD4 s\u1ED1 trong file Excel xu\u1EA5t ra gi\u1EDD TH\u1EACT S\u1EF0 c\xF3 number format** \u2014 `SdExcelService.export` t\u1EEBng set `numFmt = '#'` r\u1ED3i g\xE1n \u0111\xE8 c\u1EA3 object `style` ngay sau (exceljs thay th\u1EBF to\xE0n b\u1ED9 style khi g\xE1n), n\xEAn format ch\u01B0a t\u1EEBng \u0111\u01B0\u1EE3c \xE1p. Nay numFmt n\u1EB1m trong c\xF9ng object style: s\u1ED1 nguy\xEAn mang `'0'`, s\u1ED1 th\u1EADp ph\xE2n gi\u1EEF General (format '#' c\u0169 n\u1EBFu \u0111\u01B0\u1EE3c \xE1p s\u1EBD hi\u1EC3n th\u1ECB 0 th\xE0nh \xF4 tr\u1ED1ng v\xE0 l\xE0m tr\xF2n ph\u1EA7n hi\u1EC3n th\u1ECB c\u1EE7a s\u1ED1 l\u1EBB \u2014 n\xEAn kh\xF4ng gi\u1EEF). Di\u1EC7n m\u1EA1o file xu\u1EA5t \u0111\u1ED5i nh\u1EB9: \xF4 s\u1ED1 nguy\xEAn hi\u1EC3n th\u1ECB theo format '0'.",ordered:!1,children:[]},{text:"**`SdFormGenericValidation` + 3 symbol li\xEAn quan import \u0111\u01B0\u1EE3c b\u1EB1ng t\xEAn** \u2014 `form-generic-validation.model` gi\u1EDD n\u1EB1m trong models barrel c\u1EE7a `components/form-generic`; tr\u01B0\u1EDBc \u0111\xE2y `SdFormGeneric.validations` d\xF9ng c\xE1c type n\xE0y nh\u01B0ng consumer kh\xF4ng annotate \u0111\u01B0\u1EE3c bi\u1EBFn b\u1EB1ng ch\xFAng.",ordered:!1,children:[]},{text:"**`npm run check:i18n` xanh l\u1EA7n \u0111\u1EA7u v\xE0 \u0111\xE1ng tin** \u2014 stripper hi\u1EC3u regex literal (m\u1ED9t regex ch\u1EE9a quote/backtick t\u1EEBng \u0111\u1EA9y m\xE1y tr\u1EA1ng th\xE1i v\xE0o string v\u0129nh vi\u1EC5n, \u0111\u1EBFm nh\u1EA7m comment l\xE0 code \u2014 `excel.service.ts` b\u1ECB \u0111\u1EBFm 22 ch\u1ED7 to\xE0n comment); checker gi\u1EDD \u0111\u1ECDc marker `// @i18n-ignore` (d\xF2ng \u0111\xF3 ho\u1EB7c d\xF2ng ngay d\u01B0\u1EDBi) \u0111\xFAng nh\u01B0 i18n.md \u0111\xE3 h\u1EE9a; budget per-file t\xEDnh l\u1EA1i theo s\u1ED1 th\u1EF1c. +9 test (`npm run test:check-i18n`, g\u1ED9p v\xE0o `test:scripts`).",ordered:!1,children:[]}],children:[]},{key:"security",title:"Security",level:3,anchor:"release-2-0-security",markdown:"- **Bearer token could be sent to any host.** `SdKeycloakInterceptor` gated the `Authorization` header on `config.secureRoutes?.some(route => req.url.includes(route))` \u2014 an unanchored substring test with no host check. With the documented example `secureRoutes: ['/api/v1']`, a request to `https://analytics.thirdparty.com/api/v1/collect` received the access token. It now matches on parsed origin plus a segment-aware path prefix.\n- **`sdSafeHtml` was an unconditional sanitizer bypass.** It called `bypassSecurityTrustHtml()` on every value with no sanitize step, and `<sd-table>` pipes server-supplied cell data through it \u2014 a stored-XSS sink inside the library. It now **sanitizes by default**; the bypass is an explicit `| sdSafeHtml: true`. **BREAKING** for call sites relying on markup the sanitizer strips (inline `<svg>`, custom elements): add `: true` there.\n- **`javascript:` URLs could execute in the app origin.** Six sidebars gated `window.open` on `path.includes('http')`, which `javascript:fetch(...)//http` satisfies. One passed no `noopener`, leaving the opened page with `window.opener`. All now parse the URL and always pass `noopener,noreferrer`.\n- **Lookalike hosts inherited auth hooks.** `SdApiService` and its interceptor selected a handler with `url.startsWith(host)`, so `https://api.example.com.attacker.tld/x` matched a handler configured for `https://api.example.com`.\n- **Every optional config degraded to permissive.** `SdAuthGuard`/`SdPortalGuard` returned `true` when their callback was unset; `hasPermission('')` returned `true` while the permission guard fed it route data unchecked, so a misspelled `data` key was granted; `*sdPermission` rendered on an empty binding without consulting the service; `SdLayoutService` fell back to a mock `demo@example.com` signed-in user; `SdLicenseService` approved any hostname merely *containing* `localhost`. All now fail closed, with \"no restriction\" declared explicitly via the new `SD_PERMISSION_PUBLIC`.\n- **Root singletons cached identity with no way to clear it**, so state survived signout into the next user in the same tab. `SdPermissionService` gains `reset()`/`invalidate()`, the 401 latch became a time window instead of a permanent boolean, and `SdLayoutStorageService` gains a namespace resolver plus `clear()`. Permission codes are no longer mirrored into `sessionStorage` under a fixed UUID \u2014 readable *and writable* by any script on the origin; persistence is opt-in via `persistCache` and documented as not an authorization boundary.\n- `decodeToken` renamed **`readUnverifiedTokenClaims`**: it performs no signature or `exp` check and must never drive an authorization decision.\n- **Third-party network calls removed.** The offline interceptor pinged `jsonplaceholder.typicode.com` every 3 s through the app's full interceptor chain, leaking any unconditionally-attached auth header off-origin; it is now a same-origin probe behind `SD_NO_INTERNET_PROBE_URL`, issued via `HttpBackend`. `SdUtilities.getClientPublicIp()` called `api.ipify.org`; the endpoint is now a **required argument**.\n- API cache and in-flight dedupe keys came from a 32-bit rolling hash collapsed to 31 bits; a collision made two unrelated requests share one response body. Now SHA-256.",items:[{text:"**Bearer token could be sent to any host.** `SdKeycloakInterceptor` gated the `Authorization` header on `config.secureRoutes?.some(route => req.url.includes(route))` \u2014 an unanchored substring test with no host check. With the documented example `secureRoutes: ['/api/v1']`, a request to `https://analytics.thirdparty.com/api/v1/collect` received the access token. It now matches on parsed origin plus a segment-aware path prefix.",ordered:!1,children:[]},{text:"**`sdSafeHtml` was an unconditional sanitizer bypass.** It called `bypassSecurityTrustHtml()` on every value with no sanitize step, and `<sd-table>` pipes server-supplied cell data through it \u2014 a stored-XSS sink inside the library. It now **sanitizes by default**; the bypass is an explicit `| sdSafeHtml: true`. **BREAKING** for call sites relying on markup the sanitizer strips (inline `<svg>`, custom elements): add `: true` there.",ordered:!1,children:[]},{text:"**`javascript:` URLs could execute in the app origin.** Six sidebars gated `window.open` on `path.includes('http')`, which `javascript:fetch(...)//http` satisfies. One passed no `noopener`, leaving the opened page with `window.opener`. All now parse the URL and always pass `noopener,noreferrer`.",ordered:!1,children:[]},{text:"**Lookalike hosts inherited auth hooks.** `SdApiService` and its interceptor selected a handler with `url.startsWith(host)`, so `https://api.example.com.attacker.tld/x` matched a handler configured for `https://api.example.com`.",ordered:!1,children:[]},{text:"**Every optional config degraded to permissive.** `SdAuthGuard`/`SdPortalGuard` returned `true` when their callback was unset; `hasPermission('')` returned `true` while the permission guard fed it route data unchecked, so a misspelled `data` key was granted; `*sdPermission` rendered on an empty binding without consulting the service; `SdLayoutService` fell back to a mock `demo@example.com` signed-in user; `SdLicenseService` approved any hostname merely *containing* `localhost`. All now fail closed, with \"no restriction\" declared explicitly via the new `SD_PERMISSION_PUBLIC`.",ordered:!1,children:[]},{text:"**Root singletons cached identity with no way to clear it**, so state survived signout into the next user in the same tab. `SdPermissionService` gains `reset()`/`invalidate()`, the 401 latch became a time window instead of a permanent boolean, and `SdLayoutStorageService` gains a namespace resolver plus `clear()`. Permission codes are no longer mirrored into `sessionStorage` under a fixed UUID \u2014 readable *and writable* by any script on the origin; persistence is opt-in via `persistCache` and documented as not an authorization boundary.",ordered:!1,children:[]},{text:"`decodeToken` renamed **`readUnverifiedTokenClaims`**: it performs no signature or `exp` check and must never drive an authorization decision.",ordered:!1,children:[]},{text:"**Third-party network calls removed.** The offline interceptor pinged `jsonplaceholder.typicode.com` every 3 s through the app's full interceptor chain, leaking any unconditionally-attached auth header off-origin; it is now a same-origin probe behind `SD_NO_INTERNET_PROBE_URL`, issued via `HttpBackend`. `SdUtilities.getClientPublicIp()` called `api.ipify.org`; the endpoint is now a **required argument**.",ordered:!1,children:[]},{text:"API cache and in-flight dedupe keys came from a 32-bit rolling hash collapsed to 31 bits; a collision made two unrelated requests share one response body. Now SHA-256.",ordered:!1,children:[]}],children:[]},{key:"fixed",title:"Fixed",level:3,anchor:"release-2-0-fixed-3",markdown:'- **Accessibility.** Four a11y lint rules were switched **off**, and `aria-hidden="true"` had been used to silence tooling rather than fix markup \u2014 including on 10 real `<input>` elements and on wrappers containing an entire `mat-form-field`, an entire `<mat-calendar>`, the whole form-builder canvas, a whole sidebar menu branch, and three `mat-sort-header` divs, which made table sorting unusable with assistive tech. The rules are back on as `error`. Click-only controls are keyboard-operable, async states carry `role="status"`/`aria-live`, the `role="tablist"`/`treeitem` patterns are complete, and every `outline: none` has a `:focus-visible` replacement \u2014 notably `sd-button`, and `sd-modal`, which leaked a global focus-ring reset through `ViewEncapsulation.None`.\n- **Ch\u1EBF \u0111\u1ED9 `hideInlineError` gi\u1EDD c\xF3 ti\u1EBFng cho screen reader.** \u1EDE ch\u1EBF \u0111\u1ED9 n\xE0y th\xF4ng b\xE1o l\u1ED7i ch\u1EC9 s\u1ED1ng trong tooltip c\u1EE7a icon \u2014 kh\xF4ng c\xF3 text node n\xE0o \u0111\u1EC3 `aria-describedby` tr\u1ECF t\u1EDBi, n\xEAn tr\xECnh \u0111\u1ECDc m\xE0n h\xECnh kh\xF4ng \u0111\u1ECDc g\xEC c\u1EA3. Nay message \u0111\u01B0\u1EE3c render th\xEAm v\xE0o m\u1ED9t ph\u1EA7n t\u1EED ch\u1EC9-d\xE0nh-cho-screen-reader (`span.sd-visually-hidden`, class utility m\u1EDBi trong `assets/scss`) v\xE0 `aria-describedby` tr\u1ECF v\xE0o \u0111\xF3. \xC1p cho 12 control: input, input-number, textarea, select, autocomplete, date, date-range, datetime, chip, chip-calendar, time, editor.\n- **Hai wrapper `role="button"` b\u1ECDc ph\u1EA7n t\u1EED t\u01B0\u01A1ng t\xE1c \u0111\xE3 \u0111\u01B0\u1EE3c t\xE1i c\u1EA5u tr\xFAc** \u2014 AT g\u1ED9p c\u1EA3 c\u1EE5m th\xE0nh M\u1ED8T n\xFAt v\xE0 b\u1ECF ph\u1EA7n t\u1EED b\xEAn trong kh\u1ECFi accessibility tree. (a) H\xE0ng menu sidebar v1: `role` / `tabindex` / `aria-current` / Enter-Space chuy\u1EC3n xu\u1ED1ng ph\u1EA7n ti\xEAu \u0111\u1EC1, n\xFAt ghim th\xE0nh sibling \u0111\u1ED9c l\u1EADp, row gi\u1EEF `(click)` cho chu\u1ED9t. (b) Card tr\xEAn canvas c\u1EE7a form-builder (group / break / component \u2014 b\u1ECDc c\u1EA3 n\xFAt h\xE0nh \u0111\u1ED9ng l\u1EABn preview control c\xF3 `<input>` th\u1EADt): card b\u1ECF h\u1EB3n `role="button"`, affordance b\xE0n ph\xEDm/AT th\xE0nh m\u1ED9t `<button>` th\u1EADt \u1EDF \u0111\u1EA7u thanh action (`aria-pressed` + `aria-label`), v\xE0 thanh action hi\u1EC7n c\u1EA3 khi `:focus-within` \u0111\u1EC3 focus kh\xF4ng r\u01A1i v\xE0o n\xFAt v\xF4 h\xECnh.\n- **Form error messages that never rendered.** Writing to the validation control with `setValue(v, { emitEvent: false })` suppresses the async validator\'s completion, so the state signal never ticks and OnPush shows a red outline with no message. Fixed in `clear()` on input / input-number / autocomplete, in every write in date-range, and in `<sd-select>`\'s primary selection path.\n- **Validators a consumer attached to the public `formControl` were silently wiped** by destructive `clearValidators()`/`setValidators()` blocks in select, autocomplete, textarea, chip, chip-calendar and date-range.\n- **`<sd-date-range>` `[required]` could never fail** \u2014 `Validators.required` was applied to a control whose value is always an object.\n- **`<sd-date-range>` and `<sd-time-range>` no longer inject their internal endpoint controls into the consumer\'s FormGroup under random UUID keys**, which broke the value shape and `form.reset(obj)`.\n- **The shared form connector silently dropped the control** when the parent FormGroup already declared one at the same `name`: the user typed, `form.value` never changed, the form submitted empty, and nothing warned.\n- **`<sd-chip>` / `<sd-chip-calendar>` mutated the caller\'s array in place** and re-set the same reference, so `[(model)]` never emitted.\n- **A live OOM reproduction** in `query-builder` \u2014 `[items]="field.values || []"` allocated a fresh array every change-detection pass and `sd-select`\'s `toObservable(items)` loop froze the page. Fixed, along with the same allocation shape in query-bar, table, org-chart and tab-router.\n- **Table row identity was a content hash**, so `flattenTree`\'s `visited` Set **silently dropped the second of two content-equal rows from the rendered output**. Identity now comes from the new `option.rowKey`, falling back to per-object identity. **BREAKING** for tables using `preserveSelection` across a server re-fetch, or asserting on `data-autoid="\u2026-tree-toggle-<id>"` in E2E.\n- **A pure pipe mutated its own input** (`selection-visible`), appending duplicates without bound; a sibling pipe slept 500 ms inside an `async` pure pipe to paper over a change-detection ordering problem.\n- **Promises that never settled on the cancel path.** All six `SdConfirmService` methods, `SdDocxService.open()` and `SdUtilities.upload()` left the caller\'s `await` pending forever when the user backed out. `upload()` additionally resolved inside its loop, so validation failures after the first file were swallowed.\n- **Resources outliving their owner.** `<sd-modal>` had no destroy hook at all and orphaned its overlay; `sd-upload-file` left four drag listeners on a consumer-supplied element; `SdNotifyService` never destroyed the `<body>` node it created; `<sd-side-drawer>`\'s scroll lock could leave the page permanently unscrollable with two stacked drawers; `<sd-modal-resizable>` rewrote inline styles on every `.modal-resizable` element in the document; and 13 `setTimeout` sites across `forms/**` stored no handle, two of which called `detectChanges()` after destroy. `SdCacheService` kept every cached URL forever \u2014 now a bounded LRU via `maxMemoryEntries` (default 500).\n- **`SdLoadingService` leaked its overlay and stylesheet into the document permanently** under `provideAnimations()`: `Renderer2` from `AnimationRendererFactory` defers `removeChild` to the animation engine\'s next flush, which never happens on teardown.\n- SSR-unsafe global `document`/`window` access in `preview-image`, `SdLicenseService`, `global-error.handler` and a sidebar.\n- **The published package was not consumable.** The primary entry point exported **nothing** \u2014 `src/public-api.ts` was ten bare `import` statements, so `import { SdButton } from \'@sdcorejs/angular\'` resolved to nothing in every published version. `@angular/router` and `@angular/animations` were used but declared as neither dependency nor peer. `"sideEffects": false` was factually wrong (`Chart.register(...)` at module scope, five bare `prismjs` grammar imports), letting bundlers legally drop them with a silent production-only failure. `@omer-go/docx-parser-converter-ts` was a hard runtime dependency with zero imports.',items:[{text:'**Accessibility.** Four a11y lint rules were switched **off**, and `aria-hidden="true"` had been used to silence tooling rather than fix markup \u2014 including on 10 real `<input>` elements and on wrappers containing an entire `mat-form-field`, an entire `<mat-calendar>`, the whole form-builder canvas, a whole sidebar menu branch, and three `mat-sort-header` divs, which made table sorting unusable with assistive tech. The rules are back on as `error`. Click-only controls are keyboard-operable, async states carry `role="status"`/`aria-live`, the `role="tablist"`/`treeitem` patterns are complete, and every `outline: none` has a `:focus-visible` replacement \u2014 notably `sd-button`, and `sd-modal`, which leaked a global focus-ring reset through `ViewEncapsulation.None`.',ordered:!1,children:[]},{text:"**Ch\u1EBF \u0111\u1ED9 `hideInlineError` gi\u1EDD c\xF3 ti\u1EBFng cho screen reader.** \u1EDE ch\u1EBF \u0111\u1ED9 n\xE0y th\xF4ng b\xE1o l\u1ED7i ch\u1EC9 s\u1ED1ng trong tooltip c\u1EE7a icon \u2014 kh\xF4ng c\xF3 text node n\xE0o \u0111\u1EC3 `aria-describedby` tr\u1ECF t\u1EDBi, n\xEAn tr\xECnh \u0111\u1ECDc m\xE0n h\xECnh kh\xF4ng \u0111\u1ECDc g\xEC c\u1EA3. Nay message \u0111\u01B0\u1EE3c render th\xEAm v\xE0o m\u1ED9t ph\u1EA7n t\u1EED ch\u1EC9-d\xE0nh-cho-screen-reader (`span.sd-visually-hidden`, class utility m\u1EDBi trong `assets/scss`) v\xE0 `aria-describedby` tr\u1ECF v\xE0o \u0111\xF3. \xC1p cho 12 control: input, input-number, textarea, select, autocomplete, date, date-range, datetime, chip, chip-calendar, time, editor.",ordered:!1,children:[]},{text:'**Hai wrapper `role="button"` b\u1ECDc ph\u1EA7n t\u1EED t\u01B0\u01A1ng t\xE1c \u0111\xE3 \u0111\u01B0\u1EE3c t\xE1i c\u1EA5u tr\xFAc** \u2014 AT g\u1ED9p c\u1EA3 c\u1EE5m th\xE0nh M\u1ED8T n\xFAt v\xE0 b\u1ECF ph\u1EA7n t\u1EED b\xEAn trong kh\u1ECFi accessibility tree. (a) H\xE0ng menu sidebar v1: `role` / `tabindex` / `aria-current` / Enter-Space chuy\u1EC3n xu\u1ED1ng ph\u1EA7n ti\xEAu \u0111\u1EC1, n\xFAt ghim th\xE0nh sibling \u0111\u1ED9c l\u1EADp, row gi\u1EEF `(click)` cho chu\u1ED9t. (b) Card tr\xEAn canvas c\u1EE7a form-builder (group / break / component \u2014 b\u1ECDc c\u1EA3 n\xFAt h\xE0nh \u0111\u1ED9ng l\u1EABn preview control c\xF3 `<input>` th\u1EADt): card b\u1ECF h\u1EB3n `role="button"`, affordance b\xE0n ph\xEDm/AT th\xE0nh m\u1ED9t `<button>` th\u1EADt \u1EDF \u0111\u1EA7u thanh action (`aria-pressed` + `aria-label`), v\xE0 thanh action hi\u1EC7n c\u1EA3 khi `:focus-within` \u0111\u1EC3 focus kh\xF4ng r\u01A1i v\xE0o n\xFAt v\xF4 h\xECnh.',ordered:!1,children:[]},{text:"**Form error messages that never rendered.** Writing to the validation control with `setValue(v, { emitEvent: false })` suppresses the async validator's completion, so the state signal never ticks and OnPush shows a red outline with no message. Fixed in `clear()` on input / input-number / autocomplete, in every write in date-range, and in `<sd-select>`'s primary selection path.",ordered:!1,children:[]},{text:"**Validators a consumer attached to the public `formControl` were silently wiped** by destructive `clearValidators()`/`setValidators()` blocks in select, autocomplete, textarea, chip, chip-calendar and date-range.",ordered:!1,children:[]},{text:"**`<sd-date-range>` `[required]` could never fail** \u2014 `Validators.required` was applied to a control whose value is always an object.",ordered:!1,children:[]},{text:"**`<sd-date-range>` and `<sd-time-range>` no longer inject their internal endpoint controls into the consumer's FormGroup under random UUID keys**, which broke the value shape and `form.reset(obj)`.",ordered:!1,children:[]},{text:"**The shared form connector silently dropped the control** when the parent FormGroup already declared one at the same `name`: the user typed, `form.value` never changed, the form submitted empty, and nothing warned.",ordered:!1,children:[]},{text:"**`<sd-chip>` / `<sd-chip-calendar>` mutated the caller's array in place** and re-set the same reference, so `[(model)]` never emitted.",ordered:!1,children:[]},{text:'**A live OOM reproduction** in `query-builder` \u2014 `[items]="field.values || []"` allocated a fresh array every change-detection pass and `sd-select`\'s `toObservable(items)` loop froze the page. Fixed, along with the same allocation shape in query-bar, table, org-chart and tab-router.',ordered:!1,children:[]},{text:'**Table row identity was a content hash**, so `flattenTree`\'s `visited` Set **silently dropped the second of two content-equal rows from the rendered output**. Identity now comes from the new `option.rowKey`, falling back to per-object identity. **BREAKING** for tables using `preserveSelection` across a server re-fetch, or asserting on `data-autoid="\u2026-tree-toggle-<id>"` in E2E.',ordered:!1,children:[]},{text:"**A pure pipe mutated its own input** (`selection-visible`), appending duplicates without bound; a sibling pipe slept 500 ms inside an `async` pure pipe to paper over a change-detection ordering problem.",ordered:!1,children:[]},{text:"**Promises that never settled on the cancel path.** All six `SdConfirmService` methods, `SdDocxService.open()` and `SdUtilities.upload()` left the caller's `await` pending forever when the user backed out. `upload()` additionally resolved inside its loop, so validation failures after the first file were swallowed.",ordered:!1,children:[]},{text:"**Resources outliving their owner.** `<sd-modal>` had no destroy hook at all and orphaned its overlay; `sd-upload-file` left four drag listeners on a consumer-supplied element; `SdNotifyService` never destroyed the `<body>` node it created; `<sd-side-drawer>`'s scroll lock could leave the page permanently unscrollable with two stacked drawers; `<sd-modal-resizable>` rewrote inline styles on every `.modal-resizable` element in the document; and 13 `setTimeout` sites across `forms/**` stored no handle, two of which called `detectChanges()` after destroy. `SdCacheService` kept every cached URL forever \u2014 now a bounded LRU via `maxMemoryEntries` (default 500).",ordered:!1,children:[]},{text:"**`SdLoadingService` leaked its overlay and stylesheet into the document permanently** under `provideAnimations()`: `Renderer2` from `AnimationRendererFactory` defers `removeChild` to the animation engine's next flush, which never happens on teardown.",ordered:!1,children:[]},{text:"SSR-unsafe global `document`/`window` access in `preview-image`, `SdLicenseService`, `global-error.handler` and a sidebar.",ordered:!1,children:[]},{text:"**The published package was not consumable.** The primary entry point exported **nothing** \u2014 `src/public-api.ts` was ten bare `import` statements, so `import { SdButton } from '@sdcorejs/angular'` resolved to nothing in every published version. `@angular/router` and `@angular/animations` were used but declared as neither dependency nor peer. `\"sideEffects\": false` was factually wrong (`Chart.register(...)` at module scope, five bare `prismjs` grammar imports), letting bundlers legally drop them with a silent production-only failure. `@omer-go/docx-parser-converter-ts` was a hard runtime dependency with zero imports.",ordered:!1,children:[]}],children:[]},{key:"removed",title:"Removed (BREAKING for consumers)",level:3,anchor:"release-2-0-removed-breaking-for-consumers",markdown:'- **Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/services/docx`** (`SdDocxService` \u2014 chuy\u1EC3n DOCX sang HTML qua pandoc). \u0110\xE2y l\xE0 entry point duy nh\u1EA5t ch\u1EE9a code vendored GPL-2.0-or-later (b\u1EA3n copy pandoc-wasm core) trong m\u1ED9t package khai MIT; xo\xE1 n\xF3 lo\u1EA1i b\u1ECF xung \u0111\u1ED9t gi\u1EA5y ph\xE9p thay v\xEC ph\u1EA3i relicense hay t\xE1ch package. Kh\xF4ng c\xF3 thay th\u1EBF trong th\u01B0 vi\u1EC7n \u2014 consumer c\u1EA7n ch\u1EE9c n\u0103ng n\xE0y t\u1EF1 t\xEDch h\u1EE3p [pandoc-wasm](https://github.com/pandoc/pandoc-wasm) (GPL) \u1EDF t\u1EA7ng app c\u1EE7a m\xECnh. Dependency `@bjorn3/browser_wasi_shim` (ch\u1EC9 docx d\xF9ng) v\xE0 3 key i18n `core.docx.*` \u0111i theo. File `NOTICE` (th\xEAm trong ch\xEDnh chu k\u1EF3 Unreleased n\xE0y) c\u0169ng g\u1EE1 v\xEC kh\xF4ng c\xF2n code vendored n\xE0o \u0111\u1EC3 ghi nh\u1EADn.\n- **B\u1ECF TO\xC0N B\u1ED8 re-export t\u1EEB `@sdcorejs/utils`** \u2014 nguy\xEAn t\u1EAFc: c\xE1i g\xEC `@sdcorejs/utils` s\u1EDF h\u1EEFu th\xEC import v\xE0 \u0111\u1ECDc doc \u1EDF `@sdcorejs/utils`, th\u01B0 vi\u1EC7n n\xE0y kh\xF4ng l\xE0m m\u1EB7t ti\u1EC1n trung gian n\u1EEFa. Package \u0111\xF3 \u0111\xE3 l\xE0 dependency runtime c\u1EE7a th\u01B0 vi\u1EC7n n\xEAn c\xF3 s\u1EB5n trong tree; th\xEAm n\xF3 v\xE0o `package.json` c\u1EE7a app khi import tr\u1EF1c ti\u1EBFp. C\u1EE5 th\u1EC3:\n  - `utilities/extensions` b\u1ECF re-export `ArrayUtilities` / `ColorUtilities` / `DateUtilities` / `NumberUtilities` / `StringUtilities` / `ValidationUtilities` / `Utilities` / `BrowserUtilities` \u2192 import t\u1EEB `@sdcorejs/utils/fns`. Entry point c\xF2n l\u1EA1i `ObjectUtilities`, `SdUtilities` (14 h\xE0m c\xE0i \u0111\u1EB7t c\u1EE5c b\u1ED9) v\xE0 nh\xF3m url-safety (`sdParseUrl`, `sdOpenExternal`, \u2026) \u2014 code local.\n  - `utilities/models` b\u1ECF re-export `Color`, `Size`, `Order`, `NestedKeyOf`, `Filter*`, `Operator*`, `QueryReq` / `PagingReq` / `PagingRes`, `MaybeAsync` / `resolveMaybeAsync` / `normalizeAsync`, `ValidationPatternType` / `ValidationPattern` \u2192 `@sdcorejs/utils/models`; `VALIDATION_PATTERNS`, `EMPTY_STR` \u2192 `@sdcorejs/utils/constants`. Entry point c\xF2n l\u1EA1i `icon.model` + `unwrap-signal.model` (local). B\u1EA3ng "\u1EDF \u0111\xE2u b\xE2y gi\u1EDD" \u0111\u1EA7y \u0111\u1EE7 trong `utilities/models/models.md`.\n  - **Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/models`** \u2014 n\xF3 ch\u1EC9 re-export `Language` + `SUPPORTED_LANGUAGES`; import t\u1EEB `@sdcorejs/utils/models` / `@sdcorejs/utils/constants`. `@sdcorejs/angular/i18n` c\u0169ng th\xF4i re-export hai t\xEAn n\xE0y.\n  - Spec c\u1EE7a c\xE1c file re-export thu\u1EA7n (array/color/date/number/string extension, pattern.model) xo\xE1 theo \u2014 ch\xFAng test h\xE0nh vi c\u1EE7a `@sdcorejs/utils`, thu\u1ED9c repo \u0111\xF3.\n- **Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/components/document-builder`** (`<sd-document-builder>` \u2014 tr\xECnh d\u1EF1ng t\xE0i li\u1EC7u CKEditor cho m\u1EABu h\u1EE3p \u0111\u1ED3ng, legacy t\u1EEB h\u1EC7 c\u0169). Kh\xF4ng c\xF3 thay th\u1EBF; so\u1EA1n th\u1EA3o rich-text v\u1EABn c\xF2n `<sd-editor>` / `<sd-mini-editor>`. 6 key i18n `core.component.document-builder.*` \u0111i theo. `<sd-ckeditor-styles>` gi\u1EEF nguy\xEAn (editor/mini-editor v\u1EABn nh\xFAng).',items:[{text:"**Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/services/docx`** (`SdDocxService` \u2014 chuy\u1EC3n DOCX sang HTML qua pandoc). \u0110\xE2y l\xE0 entry point duy nh\u1EA5t ch\u1EE9a code vendored GPL-2.0-or-later (b\u1EA3n copy pandoc-wasm core) trong m\u1ED9t package khai MIT; xo\xE1 n\xF3 lo\u1EA1i b\u1ECF xung \u0111\u1ED9t gi\u1EA5y ph\xE9p thay v\xEC ph\u1EA3i relicense hay t\xE1ch package. Kh\xF4ng c\xF3 thay th\u1EBF trong th\u01B0 vi\u1EC7n \u2014 consumer c\u1EA7n ch\u1EE9c n\u0103ng n\xE0y t\u1EF1 t\xEDch h\u1EE3p [pandoc-wasm](https://github.com/pandoc/pandoc-wasm) (GPL) \u1EDF t\u1EA7ng app c\u1EE7a m\xECnh. Dependency `@bjorn3/browser_wasi_shim` (ch\u1EC9 docx d\xF9ng) v\xE0 3 key i18n `core.docx.*` \u0111i theo. File `NOTICE` (th\xEAm trong ch\xEDnh chu k\u1EF3 Unreleased n\xE0y) c\u0169ng g\u1EE1 v\xEC kh\xF4ng c\xF2n code vendored n\xE0o \u0111\u1EC3 ghi nh\u1EADn.",ordered:!1,children:[]},{text:"**B\u1ECF TO\xC0N B\u1ED8 re-export t\u1EEB `@sdcorejs/utils`** \u2014 nguy\xEAn t\u1EAFc: c\xE1i g\xEC `@sdcorejs/utils` s\u1EDF h\u1EEFu th\xEC import v\xE0 \u0111\u1ECDc doc \u1EDF `@sdcorejs/utils`, th\u01B0 vi\u1EC7n n\xE0y kh\xF4ng l\xE0m m\u1EB7t ti\u1EC1n trung gian n\u1EEFa. Package \u0111\xF3 \u0111\xE3 l\xE0 dependency runtime c\u1EE7a th\u01B0 vi\u1EC7n n\xEAn c\xF3 s\u1EB5n trong tree; th\xEAm n\xF3 v\xE0o `package.json` c\u1EE7a app khi import tr\u1EF1c ti\u1EBFp. C\u1EE5 th\u1EC3:",ordered:!1,children:[{text:"`utilities/extensions` b\u1ECF re-export `ArrayUtilities` / `ColorUtilities` / `DateUtilities` / `NumberUtilities` / `StringUtilities` / `ValidationUtilities` / `Utilities` / `BrowserUtilities` \u2192 import t\u1EEB `@sdcorejs/utils/fns`. Entry point c\xF2n l\u1EA1i `ObjectUtilities`, `SdUtilities` (14 h\xE0m c\xE0i \u0111\u1EB7t c\u1EE5c b\u1ED9) v\xE0 nh\xF3m url-safety (`sdParseUrl`, `sdOpenExternal`, \u2026) \u2014 code local.",ordered:!1,children:[]},{text:'`utilities/models` b\u1ECF re-export `Color`, `Size`, `Order`, `NestedKeyOf`, `Filter*`, `Operator*`, `QueryReq` / `PagingReq` / `PagingRes`, `MaybeAsync` / `resolveMaybeAsync` / `normalizeAsync`, `ValidationPatternType` / `ValidationPattern` \u2192 `@sdcorejs/utils/models`; `VALIDATION_PATTERNS`, `EMPTY_STR` \u2192 `@sdcorejs/utils/constants`. Entry point c\xF2n l\u1EA1i `icon.model` + `unwrap-signal.model` (local). B\u1EA3ng "\u1EDF \u0111\xE2u b\xE2y gi\u1EDD" \u0111\u1EA7y \u0111\u1EE7 trong `utilities/models/models.md`.',ordered:!1,children:[]},{text:"**Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/models`** \u2014 n\xF3 ch\u1EC9 re-export `Language` + `SUPPORTED_LANGUAGES`; import t\u1EEB `@sdcorejs/utils/models` / `@sdcorejs/utils/constants`. `@sdcorejs/angular/i18n` c\u0169ng th\xF4i re-export hai t\xEAn n\xE0y.",ordered:!1,children:[]},{text:"Spec c\u1EE7a c\xE1c file re-export thu\u1EA7n (array/color/date/number/string extension, pattern.model) xo\xE1 theo \u2014 ch\xFAng test h\xE0nh vi c\u1EE7a `@sdcorejs/utils`, thu\u1ED9c repo \u0111\xF3.",ordered:!1,children:[]}]},{text:"**Xo\xE1 h\u1EB3n entry point `@sdcorejs/angular/components/document-builder`** (`<sd-document-builder>` \u2014 tr\xECnh d\u1EF1ng t\xE0i li\u1EC7u CKEditor cho m\u1EABu h\u1EE3p \u0111\u1ED3ng, legacy t\u1EEB h\u1EC7 c\u0169). Kh\xF4ng c\xF3 thay th\u1EBF; so\u1EA1n th\u1EA3o rich-text v\u1EABn c\xF2n `<sd-editor>` / `<sd-mini-editor>`. 6 key i18n `core.component.document-builder.*` \u0111i theo. `<sd-ckeditor-styles>` gi\u1EEF nguy\xEAn (editor/mini-editor v\u1EABn nh\xFAng).",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed (BREAKING for consumers)",level:3,anchor:"release-2-0-changed-breaking-for-consumers-2",markdown:"\u0110\u1EE3t \u0111\u1ED5i t\xEAn public API c\xF3 ch\u1EE7 \u0111\xEDch, **xo\xE1 h\u1EB3n t\xEAn c\u0169, kh\xF4ng gi\u1EEF alias `@deprecated`**. L\xFD do g\u1ED9p m\u1ED9t l\u1EA7n: tag format `^\\d+\\.\\d+$` kho\xE1 major theo Angular line, n\xEAn semver KH\xD4NG th\u1EC3 ph\xE1t t\xEDn hi\u1EC7u breaking \u2014 m\u1ED7i l\u1EA7n ho\xE3n th\xEC chi ph\xED migration v\u1EC1 sau l\u1EA1i t\u0103ng.\n\n- **Pipe `translate` \u2192 `sdTranslate`** (class `TranslatePipe` \u2192 `SdTranslatePipe`). T\xEAn c\u0169 \u0111\u1EE5ng tr\u1EF1c di\u1EC7n pipe c\xF9ng t\xEAn c\u1EE7a `@ngx-translate/core`; c\u1EA3 hai \u0111\u1EC1u standalone n\xEAn app d\xF9ng song song KH\xD4NG import chung v\xE0o m\u1ED9t component \u0111\u01B0\u1EE3c. Migration: find/replace `| translate` \u2192 `| sdTranslate` v\xE0 `TranslatePipe` \u2192 `SdTranslatePipe`.\n- **`MatPaginatorIntlCro` \u2192 `SdTablePaginatorIntl`** (`@sdcorejs/angular/components/table`). T\xEAn c\u0169 l\xE0 v\u1EBFt copy-paste t\u1EEB v\xED d\u1EE5 locale Croatia, l\u1EA1i mang ti\u1EC1n t\u1ED1 `Mat` g\xE2y hi\u1EC3u nh\u1EA7m l\xE0 API c\u1EE7a Angular Material.\n- **Sidebar b\u1ECF t\xEAn kh\xF4ng ti\u1EC1n t\u1ED1** \u2014 `SidebarV2Component` \u2192 `SdSidebarV2` (`sidebar-v2` \u2192 `sd-sidebar-v2`), t\u01B0\u01A1ng t\u1EF1 `V3`, `MobileV1/V2/V3`; `SidebarMobileOverlayComponent` \u2192 `SdSidebarMobileOverlay`. N\u1ED9i b\u1ED9: `SidebarV1Component` \u2192 `SdSidebarV1`, `SidebarComponent` (selector `sidebar` \u2014 t\xEAn element to\xE0n c\u1EE5c c\u1EF1c d\u1EC5 \u0111\u1EE5ng) \u2192 `SdSidebarV1Panel` (`sd-sidebar-v1-panel`).\n- **Helper PascalCase c\u1EE7a `form-generic` chuy\u1EC3n sang `sd` + camelCase** \u2014 `GenerateId` / `GenerateKey` / `GetAttributes` / `GetComponentAttributes` / `GetDatetimeValue` / `GetVariableAttributes` / `EvaluateExpression` / `ExpressionToJavascriptExpression` / `TemplateToCondition` / `SdFormatComponent` \u2192 `sdGenerateId` / `sdGenerateKey` / `sdGetAttributes` / \u2026 Ch\xFAng l\xE0 h\xE0m thu\u1EA7n nh\u01B0ng \u0111\u1ECDc nh\u01B0 class (`new GenerateId()` nh\xECn v\u1EABn h\u1EE3p l\u1EC7). Const \u0111i k\xE8m: `DayInfoTypes` / `DayInfoPreviouses` / `AttributeOperators` / `TableColumnTypes` / `FormBuilderComponents` / `COMPONENT_ICONS` \u2192 `SD_DAY_INFO_TYPES` / `SD_DAY_INFO_PREVIOUSES` / `SD_ATTRIBUTE_OPERATORS` / `SD_TABLE_COLUMN_TYPES` / `SD_FORM_BUILDER_COMPONENTS` / `SD_COMPONENT_ICONS`.\n- **Namespace `qb*` / `QB_*` / `Qb*` c\u1EE7a `query-builder` \u2192 `sdQb*` / `SD_QB_*` / `SdQb*`** \u2014 v\xED d\u1EE5 `qbNewRule` \u2192 `sdQbNewRule`, `isQbGroup` \u2192 `sdIsQbGroup`, `QB_OPERATORS_BY_TYPE` \u2192 `SD_QB_OPERATORS_BY_TYPE`, `QbGroup` / `QbRule` / `QbToken` \u2192 `SdQbGroup` / `SdQbRule` / `SdQbToken`. C\xE1c type \u0111\xE3 \u0111\xFAng ti\u1EC1n t\u1ED1 (`SdQbRelativeUnit`, \u2026) gi\u1EEF nguy\xEAn.\n- **`sdLoadError` \u2192 `loadError` tr\xEAn `<sd-tree-select>` v\xE0 `<sd-entity-picker>`** \u2014 hai component n\xE0y l\xE0 ch\u1ED7 duy nh\u1EA5t d\xF9ng ti\u1EC1n t\u1ED1 `sd` cho s\u1EF1 ki\u1EC7n load-error, trong khi `<sd-tree>` v\xE0 `<sd-preview-pdf>` (c\xF9ng s\u1EF1 ki\u1EC7n, c\xF9ng `SdTreeLoadErrorEvent` \u1EDF nh\xF3m tree) \u0111\xE3 publish t\xEAn `loadError` t\u1EEB tr\u01B0\u1EDBc. G\u1ED9p v\u1EC1 `loadError`. Migration: find/replace `(sdLoadError)` \u2192 `(loadError)` tr\xEAn hai element \u0111\xF3.\n- **Xo\xE1 to\xE0n b\u1ED9 alias `@deprecated` d\u01B0\u1EDBi `utilities/**`** \u2014 `SdColor`, `SdSize`, `SdNestedKeyOf`, `SdOrder`, `SdFilter*`, `SdQueryReq` / `SdPagingReq` / `SdPagingRes`, `SdMaybeAsync` / `SdResolveMaybeAsync` / `SdNormalizeAsync`, `SD_EMPTY_STR`, `hslToHex` / `rgbToHex`, `SdPatternType` / `SdPatternCommon` / `SdPatternCommons`, v\xE0 c\xE1c member l\u1ED7i th\u1EDDi tr\xEAn `StringUtilities` (`REGEX_PHONE_VN`, `REGEX_IDVN`, `REGEX_IDVN_OR_PASSPORT`, `isValidEmail`, `isValidPhone`, `isValidCode`). B\u1EA3ng \xE1nh x\u1EA1 \u0111\u1EA7y \u0111\u1EE7 \u1EDF `utilities/models/models.md`. \u26A0\uFE0F Ri\xEAng nh\xF3m pattern KH\xD4NG ph\u1EA3i \u0111\u1ED5i t\xEAn thu\u1EA7n: field `regex` nay l\xE0 `pattern`, v\xE0 3 member \u0111\u1ED5i t\xEAn k\xE8m i18n key (`PHONE_VN` \u2192 `VN_PHONE`, `IDVN` \u2192 `VN_ID`, `IDVN_OR_PASSPORT` \u2192 `VN_ID_OR_PASSPORT`). `<sd-input [pattern]>` v\u1EABn nh\u1EADn 3 chu\u1ED7i c\u0169 qua b\u1EA3ng alias n\u1ED9i b\u1ED9 n\xEAn template kh\xF4ng v\u1EE1.",items:[{text:"**Pipe `translate` \u2192 `sdTranslate`** (class `TranslatePipe` \u2192 `SdTranslatePipe`). T\xEAn c\u0169 \u0111\u1EE5ng tr\u1EF1c di\u1EC7n pipe c\xF9ng t\xEAn c\u1EE7a `@ngx-translate/core`; c\u1EA3 hai \u0111\u1EC1u standalone n\xEAn app d\xF9ng song song KH\xD4NG import chung v\xE0o m\u1ED9t component \u0111\u01B0\u1EE3c. Migration: find/replace `| translate` \u2192 `| sdTranslate` v\xE0 `TranslatePipe` \u2192 `SdTranslatePipe`.",ordered:!1,children:[]},{text:"**`MatPaginatorIntlCro` \u2192 `SdTablePaginatorIntl`** (`@sdcorejs/angular/components/table`). T\xEAn c\u0169 l\xE0 v\u1EBFt copy-paste t\u1EEB v\xED d\u1EE5 locale Croatia, l\u1EA1i mang ti\u1EC1n t\u1ED1 `Mat` g\xE2y hi\u1EC3u nh\u1EA7m l\xE0 API c\u1EE7a Angular Material.",ordered:!1,children:[]},{text:"**Sidebar b\u1ECF t\xEAn kh\xF4ng ti\u1EC1n t\u1ED1** \u2014 `SidebarV2Component` \u2192 `SdSidebarV2` (`sidebar-v2` \u2192 `sd-sidebar-v2`), t\u01B0\u01A1ng t\u1EF1 `V3`, `MobileV1/V2/V3`; `SidebarMobileOverlayComponent` \u2192 `SdSidebarMobileOverlay`. N\u1ED9i b\u1ED9: `SidebarV1Component` \u2192 `SdSidebarV1`, `SidebarComponent` (selector `sidebar` \u2014 t\xEAn element to\xE0n c\u1EE5c c\u1EF1c d\u1EC5 \u0111\u1EE5ng) \u2192 `SdSidebarV1Panel` (`sd-sidebar-v1-panel`).",ordered:!1,children:[]},{text:"**Helper PascalCase c\u1EE7a `form-generic` chuy\u1EC3n sang `sd` + camelCase** \u2014 `GenerateId` / `GenerateKey` / `GetAttributes` / `GetComponentAttributes` / `GetDatetimeValue` / `GetVariableAttributes` / `EvaluateExpression` / `ExpressionToJavascriptExpression` / `TemplateToCondition` / `SdFormatComponent` \u2192 `sdGenerateId` / `sdGenerateKey` / `sdGetAttributes` / \u2026 Ch\xFAng l\xE0 h\xE0m thu\u1EA7n nh\u01B0ng \u0111\u1ECDc nh\u01B0 class (`new GenerateId()` nh\xECn v\u1EABn h\u1EE3p l\u1EC7). Const \u0111i k\xE8m: `DayInfoTypes` / `DayInfoPreviouses` / `AttributeOperators` / `TableColumnTypes` / `FormBuilderComponents` / `COMPONENT_ICONS` \u2192 `SD_DAY_INFO_TYPES` / `SD_DAY_INFO_PREVIOUSES` / `SD_ATTRIBUTE_OPERATORS` / `SD_TABLE_COLUMN_TYPES` / `SD_FORM_BUILDER_COMPONENTS` / `SD_COMPONENT_ICONS`.",ordered:!1,children:[]},{text:"**Namespace `qb*` / `QB_*` / `Qb*` c\u1EE7a `query-builder` \u2192 `sdQb*` / `SD_QB_*` / `SdQb*`** \u2014 v\xED d\u1EE5 `qbNewRule` \u2192 `sdQbNewRule`, `isQbGroup` \u2192 `sdIsQbGroup`, `QB_OPERATORS_BY_TYPE` \u2192 `SD_QB_OPERATORS_BY_TYPE`, `QbGroup` / `QbRule` / `QbToken` \u2192 `SdQbGroup` / `SdQbRule` / `SdQbToken`. C\xE1c type \u0111\xE3 \u0111\xFAng ti\u1EC1n t\u1ED1 (`SdQbRelativeUnit`, \u2026) gi\u1EEF nguy\xEAn.",ordered:!1,children:[]},{text:"**`sdLoadError` \u2192 `loadError` tr\xEAn `<sd-tree-select>` v\xE0 `<sd-entity-picker>`** \u2014 hai component n\xE0y l\xE0 ch\u1ED7 duy nh\u1EA5t d\xF9ng ti\u1EC1n t\u1ED1 `sd` cho s\u1EF1 ki\u1EC7n load-error, trong khi `<sd-tree>` v\xE0 `<sd-preview-pdf>` (c\xF9ng s\u1EF1 ki\u1EC7n, c\xF9ng `SdTreeLoadErrorEvent` \u1EDF nh\xF3m tree) \u0111\xE3 publish t\xEAn `loadError` t\u1EEB tr\u01B0\u1EDBc. G\u1ED9p v\u1EC1 `loadError`. Migration: find/replace `(sdLoadError)` \u2192 `(loadError)` tr\xEAn hai element \u0111\xF3.",ordered:!1,children:[]},{text:"**Xo\xE1 to\xE0n b\u1ED9 alias `@deprecated` d\u01B0\u1EDBi `utilities/**`** \u2014 `SdColor`, `SdSize`, `SdNestedKeyOf`, `SdOrder`, `SdFilter*`, `SdQueryReq` / `SdPagingReq` / `SdPagingRes`, `SdMaybeAsync` / `SdResolveMaybeAsync` / `SdNormalizeAsync`, `SD_EMPTY_STR`, `hslToHex` / `rgbToHex`, `SdPatternType` / `SdPatternCommon` / `SdPatternCommons`, v\xE0 c\xE1c member l\u1ED7i th\u1EDDi tr\xEAn `StringUtilities` (`REGEX_PHONE_VN`, `REGEX_IDVN`, `REGEX_IDVN_OR_PASSPORT`, `isValidEmail`, `isValidPhone`, `isValidCode`). B\u1EA3ng \xE1nh x\u1EA1 \u0111\u1EA7y \u0111\u1EE7 \u1EDF `utilities/models/models.md`. \u26A0\uFE0F Ri\xEAng nh\xF3m pattern KH\xD4NG ph\u1EA3i \u0111\u1ED5i t\xEAn thu\u1EA7n: field `regex` nay l\xE0 `pattern`, v\xE0 3 member \u0111\u1ED5i t\xEAn k\xE8m i18n key (`PHONE_VN` \u2192 `VN_PHONE`, `IDVN` \u2192 `VN_ID`, `IDVN_OR_PASSPORT` \u2192 `VN_ID_OR_PASSPORT`). `<sd-input [pattern]>` v\u1EABn nh\u1EADn 3 chu\u1ED7i c\u0169 qua b\u1EA3ng alias n\u1ED9i b\u1ED9 n\xEAn template kh\xF4ng v\u1EE1.",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed",level:3,anchor:"release-2-0-changed-5",markdown:"- **Si\u1EBFt default generic `any` \u2192 `unknown` tr\xEAn 19 file model** (82 khai b\xE1o), g\u1ED3m `components/tree/src/tree.model.ts` (to\xE0n b\u1ED9 type export), `components/table/src/services/table-filter/table-filter.model.ts`, `components/table/src/models/table-column.model.ts`, `components/query-bar/src/query-bar.model.ts`, `forms/models/**`, \u2026 Consumer vi\u1EBFt `SdTableColumn<User>` hay `SdTreeOption<User>` t\u1EEB nay \u0111\u01B0\u1EE3c suy lu\u1EADn ki\u1EC3u th\u1EADt v\xE0 b\xE1o l\u1ED7i khi d\xF9ng sai, thay v\xEC im l\u1EB7ng nh\u01B0 v\u1EDBi `any`. Th\xEAm `SdTableColumnAnyRow` cho v\xE0i v\u1ECB tr\xED tham s\u1ED1 n\u1ED9i b\u1ED9 row-agnostic (gi\u1EA3i th\xEDch trong `table-column.model.ts`).\n- **`SdUtilities` KH\xD4NG b\u1ECB xo\xE1** \u2014 marker `@deprecated` c\u0169 m\xF4 t\u1EA3 sai: \u0111\xE2y kh\xF4ng ph\u1EA3i alias m\xE0 l\xE0 14 h\xE0m c\xE0i \u0111\u1EB7t c\u1EE5c b\u1ED9. Marker \u0111\xE3 \u0111\u01B0\u1EE3c g\u1EE1, implementation gi\u1EEF nguy\xEAn.",items:[{text:"**Si\u1EBFt default generic `any` \u2192 `unknown` tr\xEAn 19 file model** (82 khai b\xE1o), g\u1ED3m `components/tree/src/tree.model.ts` (to\xE0n b\u1ED9 type export), `components/table/src/services/table-filter/table-filter.model.ts`, `components/table/src/models/table-column.model.ts`, `components/query-bar/src/query-bar.model.ts`, `forms/models/**`, \u2026 Consumer vi\u1EBFt `SdTableColumn<User>` hay `SdTreeOption<User>` t\u1EEB nay \u0111\u01B0\u1EE3c suy lu\u1EADn ki\u1EC3u th\u1EADt v\xE0 b\xE1o l\u1ED7i khi d\xF9ng sai, thay v\xEC im l\u1EB7ng nh\u01B0 v\u1EDBi `any`. Th\xEAm `SdTableColumnAnyRow` cho v\xE0i v\u1ECB tr\xED tham s\u1ED1 n\u1ED9i b\u1ED9 row-agnostic (gi\u1EA3i th\xEDch trong `table-column.model.ts`).",ordered:!1,children:[]},{text:"**`SdUtilities` KH\xD4NG b\u1ECB xo\xE1** \u2014 marker `@deprecated` c\u0169 m\xF4 t\u1EA3 sai: \u0111\xE2y kh\xF4ng ph\u1EA3i alias m\xE0 l\xE0 14 h\xE0m c\xE0i \u0111\u1EB7t c\u1EE5c b\u1ED9. Marker \u0111\xE3 \u0111\u01B0\u1EE3c g\u1EE1, implementation gi\u1EEF nguy\xEAn.",ordered:!1,children:[]}],children:[]},{key:"khong-oi-co-chu-ich",title:"Kh\xF4ng \u0111\u1ED5i (c\xF3 ch\u1EE7 \u0111\xEDch)",level:3,anchor:"release-2-0-khong-oi-co-chu-ich",markdown:'- **T\xEAn output KH\xD4NG \u0111\u1ED5i h\xE0ng lo\u1EA1t.** \u0110\u1EE3t n\xE0y t\u1EEBng th\xEAm ti\u1EC1n t\u1ED1 `sd` cho ~73 output (`close` \u2192 `sdClose`, `search` \u2192 `sdSearch`, \u2026) nh\u01B0ng \u0111\xE3 \u0111\u1EA3o l\u1EA1i tr\u01B0\u1EDBc khi release: t\xEAn kh\xF4ng ti\u1EC1n t\u1ED1 l\xE0 t\xEAn \u0111\xE3 publish t\u1EDBi 1.6, quen thu\u1ED9c, v\xE0 kh\u1EDBp th\xF4ng l\u1EC7 Angular Material (output kh\xF4ng mang prefix). H\u1EC7 qu\u1EA3 ch\u1EA5p nh\u1EADn: surface output v\u1EABn **mixed** \u2014 16 t\xEAn `sd*` \u0111\xE3 publish t\u1EEB 1.4/1.6 (`sdChanges`, `sdBlur`, `sdFocus`, `sdClosed`, `sdCloseError` tr\xEAn modal/side-drawer/inform/import-excel, `sdCancel` tr\xEAn job-progress, `sdAction`, `sdKeydown`, \u2026) gi\u1EEF nguy\xEAn v\xEC \u0111\u1ED5i ch\xFAng m\u1EDBi l\xE0 breaking. Mu\u1ED1n th\u1ED1ng nh\u1EA5t to\xE0n b\u1ED9 th\xEC c\u1EA7n m\u1ED9t \u0111\u1EE3t ri\xEAng k\xE8m codemod.\n- `click` c\u1EE7a `<sd-button>` gi\u1EEF nguy\xEAn t\xEAn. \u0110\u1ED5i sang `sdClick` s\u1EBD khi\u1EBFn m\u1ECDi `(click)` s\u1EB5n c\xF3 c\u1EE7a consumer im l\u1EB7ng r\u01A1i v\u1EC1 DOM event g\u1ED1c \u2014 v\u1EABn ch\u1EA1y nh\u01B0ng \u0111\u1ED5i payload v\xE0 b\u1ECF qua gate `disabled`. \u0110\xE2y l\xE0 ki\u1EC3u breaking "\xE2m th\u1EA7m" nguy hi\u1EC3m nh\u1EA5t, c\u1EA7n m\u1ED9t \u0111\u1EE3t ri\xEAng k\xE8m codemod.\n- M\u1ED9t s\u1ED1 v\xF9ng v\u1EABn gi\u1EEF default generic `any` v\xEC `unknown` g\xE2y lan qu\xE1 r\u1ED9ng (`import-excel`, `services/excel`, ph\u1EA7n l\u1EDBn `components/table/src/models/**`, `form-generic-component.model.ts`) \u2014 chi ti\u1EBFt trong ghi ch\xFA review c\u1EE7a \u0111\u1EE3t n\xE0y.',items:[{text:"**T\xEAn output KH\xD4NG \u0111\u1ED5i h\xE0ng lo\u1EA1t.** \u0110\u1EE3t n\xE0y t\u1EEBng th\xEAm ti\u1EC1n t\u1ED1 `sd` cho ~73 output (`close` \u2192 `sdClose`, `search` \u2192 `sdSearch`, \u2026) nh\u01B0ng \u0111\xE3 \u0111\u1EA3o l\u1EA1i tr\u01B0\u1EDBc khi release: t\xEAn kh\xF4ng ti\u1EC1n t\u1ED1 l\xE0 t\xEAn \u0111\xE3 publish t\u1EDBi 1.6, quen thu\u1ED9c, v\xE0 kh\u1EDBp th\xF4ng l\u1EC7 Angular Material (output kh\xF4ng mang prefix). H\u1EC7 qu\u1EA3 ch\u1EA5p nh\u1EADn: surface output v\u1EABn **mixed** \u2014 16 t\xEAn `sd*` \u0111\xE3 publish t\u1EEB 1.4/1.6 (`sdChanges`, `sdBlur`, `sdFocus`, `sdClosed`, `sdCloseError` tr\xEAn modal/side-drawer/inform/import-excel, `sdCancel` tr\xEAn job-progress, `sdAction`, `sdKeydown`, \u2026) gi\u1EEF nguy\xEAn v\xEC \u0111\u1ED5i ch\xFAng m\u1EDBi l\xE0 breaking. Mu\u1ED1n th\u1ED1ng nh\u1EA5t to\xE0n b\u1ED9 th\xEC c\u1EA7n m\u1ED9t \u0111\u1EE3t ri\xEAng k\xE8m codemod.",ordered:!1,children:[]},{text:'`click` c\u1EE7a `<sd-button>` gi\u1EEF nguy\xEAn t\xEAn. \u0110\u1ED5i sang `sdClick` s\u1EBD khi\u1EBFn m\u1ECDi `(click)` s\u1EB5n c\xF3 c\u1EE7a consumer im l\u1EB7ng r\u01A1i v\u1EC1 DOM event g\u1ED1c \u2014 v\u1EABn ch\u1EA1y nh\u01B0ng \u0111\u1ED5i payload v\xE0 b\u1ECF qua gate `disabled`. \u0110\xE2y l\xE0 ki\u1EC3u breaking "\xE2m th\u1EA7m" nguy hi\u1EC3m nh\u1EA5t, c\u1EA7n m\u1ED9t \u0111\u1EE3t ri\xEAng k\xE8m codemod.',ordered:!1,children:[]},{text:"M\u1ED9t s\u1ED1 v\xF9ng v\u1EABn gi\u1EEF default generic `any` v\xEC `unknown` g\xE2y lan qu\xE1 r\u1ED9ng (`import-excel`, `services/excel`, ph\u1EA7n l\u1EDBn `components/table/src/models/**`, `form-generic-component.model.ts`) \u2014 chi ti\u1EBFt trong ghi ch\xFA review c\u1EE7a \u0111\u1EE3t n\xE0y.",ordered:!1,children:[]}],children:[]}]},{id:"release-1-6",title:"Release 1.6",suffix:"1.6",date:"2026-08-07",anchor:"release-1-6",unreleased:!1,packageVersions:[{angularMajor:19,version:"19.1.6"},{angularMajor:20,version:"20.1.6"},{angularMajor:21,version:"21.1.6"}],summaryMarkdown:"Release suffix `1.6` publishes `19.1.6`, `20.1.6`, and `21.1.6` as a stable release across the maintained Angular lines.",markdown:'Release suffix `1.6` publishes `19.1.6`, `20.1.6`, and `21.1.6` as a stable release across the maintained Angular lines.\n\n### Added\n\n- **`sd-date` auto-formats a typed date** - separators appear while you type (`22081991` becomes `22/08/1991`); non-digits are dropped and the input is capped at eight digits. Deleting never re-adds the separator you just removed.\n- **Sidebar menu search matches routes and initials** - the menu filter now also matches a menu\'s route path (`product`) and the ordered initials of its label (`sp` finds `San pham`), on top of the existing accent-insensitive title match. `HighlightSearchPipe` marks each matched initial when there is no contiguous substring to highlight.\n- **Tabs for the built-in `forbidden` and `not-found` pages** - both register a `@SdTabComponent` (icons `block` and `search_off`), so opening them in a tabbed shell shows a real icon and a translated label instead of an empty tab. New i18n keys `core.module.layout.{forbidden,not-found}.tab-name` in all five locales, plus a shared `resolveTabName()` helper that `home` now uses as well.\n\n### Fixed\n\n- **`sd-date` and `sd-date-range` accepted a half-typed date as a value** - typing `11/12/2` produced year 0002, `11/12/20` produced year 0020, and deleting back to `11` produced year 1100, with the error flag cleared so the field looked valid. Both inputs are bound to Angular Material date inputs, which re-parse the text after every keystroke; the stock date-fns adapter accepts a short year and falls back to `parseISO`, which reads `11` as a century. The new `SdStrictDateFnsAdapter` skips that fallback and requires the text to round-trip through the configured format, so a control only ever receives a date the user finished typing. `sd-datetime` is unaffected - its input is not `[matDatepicker]`-bound.\n- **PDFs failed to open in production builds** - `<sd-preview-pdf>` pointed pdf.js at a worker file that esbuild never emitted, so every document ended in the "Setting up fake worker failed" network-error state after an AOT deploy while `ng serve` was fine. The worker bundle is now inlined and handed to pdf.js as a `blob:` URL, so consumer apps need no `angular.json` assets entry and no manual file copy. Requires a deployment CSP that allows `worker-src blob:`; `sd-preview.md` documents the manual `workerSrc` escape hatch.\n- **`sd-table` export button label was hard-coded English** - the main Export button now goes through `I18nService` like the export-excel and export-csv menu items already did, so a Vietnamese user sees "Xuat du lieu". New keys `core.component.table.export` and `core.component.table.exporting` in all five locales.\n\n### Changed (BREAKING for consumers)\n\n- **`TableExportService.exportTitle` is no longer a `WritableSignal`** - it became a `computed()` over the current language plus export progress. Drive progress through the new `setExportProgress(percent | null)` instead of assigning to `exportTitle`. Nothing in the library assigned to it; only consumers that did need to migrate.',sections:[{key:"added",title:"Added",level:3,anchor:"release-1-6-added",markdown:"- **`sd-date` auto-formats a typed date** - separators appear while you type (`22081991` becomes `22/08/1991`); non-digits are dropped and the input is capped at eight digits. Deleting never re-adds the separator you just removed.\n- **Sidebar menu search matches routes and initials** - the menu filter now also matches a menu's route path (`product`) and the ordered initials of its label (`sp` finds `San pham`), on top of the existing accent-insensitive title match. `HighlightSearchPipe` marks each matched initial when there is no contiguous substring to highlight.\n- **Tabs for the built-in `forbidden` and `not-found` pages** - both register a `@SdTabComponent` (icons `block` and `search_off`), so opening them in a tabbed shell shows a real icon and a translated label instead of an empty tab. New i18n keys `core.module.layout.{forbidden,not-found}.tab-name` in all five locales, plus a shared `resolveTabName()` helper that `home` now uses as well.",items:[{text:"**`sd-date` auto-formats a typed date** - separators appear while you type (`22081991` becomes `22/08/1991`); non-digits are dropped and the input is capped at eight digits. Deleting never re-adds the separator you just removed.",ordered:!1,children:[]},{text:"**Sidebar menu search matches routes and initials** - the menu filter now also matches a menu's route path (`product`) and the ordered initials of its label (`sp` finds `San pham`), on top of the existing accent-insensitive title match. `HighlightSearchPipe` marks each matched initial when there is no contiguous substring to highlight.",ordered:!1,children:[]},{text:"**Tabs for the built-in `forbidden` and `not-found` pages** - both register a `@SdTabComponent` (icons `block` and `search_off`), so opening them in a tabbed shell shows a real icon and a translated label instead of an empty tab. New i18n keys `core.module.layout.{forbidden,not-found}.tab-name` in all five locales, plus a shared `resolveTabName()` helper that `home` now uses as well.",ordered:!1,children:[]}],children:[]},{key:"fixed",title:"Fixed",level:3,anchor:"release-1-6-fixed",markdown:'- **`sd-date` and `sd-date-range` accepted a half-typed date as a value** - typing `11/12/2` produced year 0002, `11/12/20` produced year 0020, and deleting back to `11` produced year 1100, with the error flag cleared so the field looked valid. Both inputs are bound to Angular Material date inputs, which re-parse the text after every keystroke; the stock date-fns adapter accepts a short year and falls back to `parseISO`, which reads `11` as a century. The new `SdStrictDateFnsAdapter` skips that fallback and requires the text to round-trip through the configured format, so a control only ever receives a date the user finished typing. `sd-datetime` is unaffected - its input is not `[matDatepicker]`-bound.\n- **PDFs failed to open in production builds** - `<sd-preview-pdf>` pointed pdf.js at a worker file that esbuild never emitted, so every document ended in the "Setting up fake worker failed" network-error state after an AOT deploy while `ng serve` was fine. The worker bundle is now inlined and handed to pdf.js as a `blob:` URL, so consumer apps need no `angular.json` assets entry and no manual file copy. Requires a deployment CSP that allows `worker-src blob:`; `sd-preview.md` documents the manual `workerSrc` escape hatch.\n- **`sd-table` export button label was hard-coded English** - the main Export button now goes through `I18nService` like the export-excel and export-csv menu items already did, so a Vietnamese user sees "Xuat du lieu". New keys `core.component.table.export` and `core.component.table.exporting` in all five locales.',items:[{text:"**`sd-date` and `sd-date-range` accepted a half-typed date as a value** - typing `11/12/2` produced year 0002, `11/12/20` produced year 0020, and deleting back to `11` produced year 1100, with the error flag cleared so the field looked valid. Both inputs are bound to Angular Material date inputs, which re-parse the text after every keystroke; the stock date-fns adapter accepts a short year and falls back to `parseISO`, which reads `11` as a century. The new `SdStrictDateFnsAdapter` skips that fallback and requires the text to round-trip through the configured format, so a control only ever receives a date the user finished typing. `sd-datetime` is unaffected - its input is not `[matDatepicker]`-bound.",ordered:!1,children:[]},{text:'**PDFs failed to open in production builds** - `<sd-preview-pdf>` pointed pdf.js at a worker file that esbuild never emitted, so every document ended in the "Setting up fake worker failed" network-error state after an AOT deploy while `ng serve` was fine. The worker bundle is now inlined and handed to pdf.js as a `blob:` URL, so consumer apps need no `angular.json` assets entry and no manual file copy. Requires a deployment CSP that allows `worker-src blob:`; `sd-preview.md` documents the manual `workerSrc` escape hatch.',ordered:!1,children:[]},{text:'**`sd-table` export button label was hard-coded English** - the main Export button now goes through `I18nService` like the export-excel and export-csv menu items already did, so a Vietnamese user sees "Xuat du lieu". New keys `core.component.table.export` and `core.component.table.exporting` in all five locales.',ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed (BREAKING for consumers)",level:3,anchor:"release-1-6-changed-breaking-for-consumers",markdown:"- **`TableExportService.exportTitle` is no longer a `WritableSignal`** - it became a `computed()` over the current language plus export progress. Drive progress through the new `setExportProgress(percent | null)` instead of assigning to `exportTitle`. Nothing in the library assigned to it; only consumers that did need to migrate.",items:[{text:"**`TableExportService.exportTitle` is no longer a `WritableSignal`** - it became a `computed()` over the current language plus export progress. Drive progress through the new `setExportProgress(percent | null)` instead of assigning to `exportTitle`. Nothing in the library assigned to it; only consumers that did need to migrate.",ordered:!1,children:[]}],children:[]}]},{id:"release-1-5",title:"Release 1.5",suffix:"1.5",date:"2026-07-28",anchor:"release-1-5",unreleased:!1,packageVersions:[{angularMajor:19,version:"19.1.5"},{angularMajor:20,version:"20.1.5"},{angularMajor:21,version:"21.1.5"}],summaryMarkdown:"Release suffix `1.5` publishes `19.1.5`, `20.1.5`, and `21.1.5` as a stable release across the maintained Angular lines.",markdown:"Release suffix `1.5` publishes `19.1.5`, `20.1.5`, and `21.1.5` as a stable release across the maintained Angular lines.\n\n### Changed\n\n- **Layout V2/V3 navigation polish** - centered compact account and drawer controls, removed the collapsed V3 fallback brand icon, and unified desktop/mobile menu search behind an internal Soft-pill presentation without changing public APIs or filtering behavior.\n- **Layout account menu** - added optional profile, settings and notification actions plus role metadata to the shared V1/V2/V3 account presentation; consumers continue to own navigation, authorization and notification data.\n\n### Fixed\n\n- **Showcase published-doc versions** - derive every supported `1.2+` documentation release from `published-docs/versions.json`, so the version selector exposes the existing 1.3/1.4 archives and automatically accepts the 1.5 archives after publishing instead of canonicalizing every route to 1.2.\n- **Layout mobile containment** - render the Layout, Mobile V2 and Mobile V3 custom-element hosts as blocks so full-height shells and the V3 topbar start inside consumer and Showcase containers instead of aligning below an inline baseline.",sections:[{key:"changed",title:"Changed",level:3,anchor:"release-1-5-changed",markdown:`- **Layout V2/V3 navigation polish** - centered compact account and drawer controls, removed the collapsed V3 fallback brand icon, and unified desktop/mobile menu search behind an internal Soft-pill presentation without changing public APIs or filtering behavior.
- **Layout account menu** - added optional profile, settings and notification actions plus role metadata to the shared V1/V2/V3 account presentation; consumers continue to own navigation, authorization and notification data.`,items:[{text:"**Layout V2/V3 navigation polish** - centered compact account and drawer controls, removed the collapsed V3 fallback brand icon, and unified desktop/mobile menu search behind an internal Soft-pill presentation without changing public APIs or filtering behavior.",ordered:!1,children:[]},{text:"**Layout account menu** - added optional profile, settings and notification actions plus role metadata to the shared V1/V2/V3 account presentation; consumers continue to own navigation, authorization and notification data.",ordered:!1,children:[]}],children:[]},{key:"fixed",title:"Fixed",level:3,anchor:"release-1-5-fixed",markdown:"- **Showcase published-doc versions** - derive every supported `1.2+` documentation release from `published-docs/versions.json`, so the version selector exposes the existing 1.3/1.4 archives and automatically accepts the 1.5 archives after publishing instead of canonicalizing every route to 1.2.\n- **Layout mobile containment** - render the Layout, Mobile V2 and Mobile V3 custom-element hosts as blocks so full-height shells and the V3 topbar start inside consumer and Showcase containers instead of aligning below an inline baseline.",items:[{text:"**Showcase published-doc versions** - derive every supported `1.2+` documentation release from `published-docs/versions.json`, so the version selector exposes the existing 1.3/1.4 archives and automatically accepts the 1.5 archives after publishing instead of canonicalizing every route to 1.2.",ordered:!1,children:[]},{text:"**Layout mobile containment** - render the Layout, Mobile V2 and Mobile V3 custom-element hosts as blocks so full-height shells and the V3 topbar start inside consumer and Showcase containers instead of aligning below an inline baseline.",ordered:!1,children:[]}],children:[]}]},{id:"release-1-4",title:"Release 1.4",suffix:"1.4",date:"2026-07-23",anchor:"release-1-4",unreleased:!1,packageVersions:[{angularMajor:19,version:"19.1.4"},{angularMajor:20,version:"20.1.4"},{angularMajor:21,version:"21.1.4"}],summaryMarkdown:"Release suffix `1.4` publishes `19.1.4`, `20.1.4`, and `21.1.4` as a stable release across the maintained Angular lines.",markdown:"Release suffix `1.4` publishes `19.1.4`, `20.1.4`, and `21.1.4` as a stable release across the maintained Angular lines.\n\n### Added\n\n- **Time controls and input masks** - added timezone-free `SdTime` / `SdTimeRange`, reusable raw/display input-mask adapters and common business presets, all integrated with the shared form lifecycle contract.\n- **Responsive navigation and state UI** - added the signal-based `SdViewportService`, router/manual `SdBreadcrumb`, and accessible loading/empty/error/forbidden/success `SdDataState` presentation.\n- **Typed business selection** - added generic server-backed `SdEntityPicker` and static/lazy `SdTreeSelect` controls with stable-key selection, hydration, templates, responsive layout, keyboard behavior and form integration.\n- **Safe workflow and operations primitives** - added scoped `SdUnsavedChangesService` guards/adapters, shared poll/SSE `SdTaskService`, `SdJobProgress`, and normalized/redacted `SdAuditDiff`.\n- **Graph persistence foundation** - added the public `SdGraphSerializer` plus identity/envelope/storage-adapter entrypoint used by cache and storage to preserve `Date`, `Map`, `Set`, shared references and cycles with bounded validation.\n- **Complete PDF workflows** - added continuous scroll, printing adapters, outline navigation, bounded thumbnails/search, browser capability tokens and consumer-facing PDF state/event types.\n\n### Changed (BREAKING for consumers)\n\n- **Angular signal input/output contracts** - migrated compatible component inputs and outputs to `input()` and `output()` across Angular 19/20/21 while preserving template binding names and behavior. Direct class consumers and tests must read migrated inputs as signals (for example `component.value()`), set them through Angular's `ComponentRef.setInput(...)`, and use the output `emit()`/`subscribe()` contract instead of RxJS-only `EventEmitter` APIs.\n\n### Changed\n\n- **Angular dependency injection internals** - migrated compatible constructor-injected dependencies to field-level `inject()` with compatibility constructors where public inheritance may depend on the existing class shape. Runtime constructor arguments and classes instantiated directly outside Angular injection context remain constructor-based.\n- **Signal migration safety** - retained setter/aliased inputs and outputs whose listener/type semantics cannot be migrated safely yet, including outputs that rely on `EventEmitter.observed`; these exceptions preserve current runtime behavior.\n- **Form lifecycle foundation** - consolidated registration, dynamic parent/name changes, model/control synchronization, validation state and destroy cleanup behind `\u0275sdFormControlConnector` while retaining existing template bindings.\n- **API request safety** - GET requests deduplicate by default; mutations deduplicate/retry only through explicit opt-in. Added typed PATCH, per-caller `AbortSignal`, bounded backoff and deterministic in-flight cleanup; response generics now default to `unknown`.\n- **Loading ownership** - `start()` now returns an idempotent ref, overlapping owners are reference-counted, `run()` scopes async work, and browser DOM/style state is shared and cleaned up safely.\n- **Cache and storage persistence** - activated `SD_CACHE_CONFIG`, added namespace/version/custom serializers, exact cached-absence snapshots, load coalescing, typed handle teardown, legacy JSON migration and SSR/quota/corruption containment.\n- **Responsive layout behavior** - layout responds live to viewport signals while legacy responsive services/tokens remain supported as adapters.\n- **Release documentation rollout** - the multi-version sync now treats the canonical npm README and i18n quality scripts as managed release inputs, while the sync guard fails closed on README, package-version, script or generated-workspace drift.\n\n### Fixed\n\n- **`sd-table` empty-result reload** - kept a configured reload action enabled when the current items or total are zero, allowing users to retry without changing export or paginator behavior.\n- **Showcase loading documentation manifest** - aligned the generated loading-service example count with the current showcase source so documentation generation no longer reports a stale manifest expectation. (#15)\n- **Published no-op and lifecycle paths** - implemented the previously inert PDF print/outline/continuous APIs and hardened modal/drawer/tab close hooks, task cancellation, picker races and tree cascade/error behavior.\n- **Accessibility and resource cleanup** - added semantic breadcrumb/data-state/progress/audit markup, focus/keyboard handling, bounded PDF work, SSR guards and deterministic listeners/timers/subscriptions/EventSource teardown across the new surfaces.\n- **Form expression evaluation** - corrected `NextDay` handling so relative expressions remove the `NextDay` token instead of the unrelated `LastDay` token before evaluation.\n- **Showcase release and layout demos** - preserved repeated populated changelog groups such as breaking and non-breaking `Changed` sections, and made the Layout Desktop/Mobile controls switch the actual rendered sidebar through the scoped viewport adapter.\n\n### Deprecated\n\n- **API compatibility aliases** - deprecated `SdApiOption.autoCache` in favor of `dedupe` and `SD_API_CONFIGURATION` in favor of `SD_API_CONFIG`.\n- **Generic cache callback providers** - deprecated directly typed legacy callbacks; migrate to globally safe `unknown` callbacks or use `adaptLegacySdCacheCallbacks()` temporarily with a runtime guard.",sections:[{key:"added",title:"Added",level:3,anchor:"release-1-4-added",markdown:"- **Time controls and input masks** - added timezone-free `SdTime` / `SdTimeRange`, reusable raw/display input-mask adapters and common business presets, all integrated with the shared form lifecycle contract.\n- **Responsive navigation and state UI** - added the signal-based `SdViewportService`, router/manual `SdBreadcrumb`, and accessible loading/empty/error/forbidden/success `SdDataState` presentation.\n- **Typed business selection** - added generic server-backed `SdEntityPicker` and static/lazy `SdTreeSelect` controls with stable-key selection, hydration, templates, responsive layout, keyboard behavior and form integration.\n- **Safe workflow and operations primitives** - added scoped `SdUnsavedChangesService` guards/adapters, shared poll/SSE `SdTaskService`, `SdJobProgress`, and normalized/redacted `SdAuditDiff`.\n- **Graph persistence foundation** - added the public `SdGraphSerializer` plus identity/envelope/storage-adapter entrypoint used by cache and storage to preserve `Date`, `Map`, `Set`, shared references and cycles with bounded validation.\n- **Complete PDF workflows** - added continuous scroll, printing adapters, outline navigation, bounded thumbnails/search, browser capability tokens and consumer-facing PDF state/event types.",items:[{text:"**Time controls and input masks** - added timezone-free `SdTime` / `SdTimeRange`, reusable raw/display input-mask adapters and common business presets, all integrated with the shared form lifecycle contract.",ordered:!1,children:[]},{text:"**Responsive navigation and state UI** - added the signal-based `SdViewportService`, router/manual `SdBreadcrumb`, and accessible loading/empty/error/forbidden/success `SdDataState` presentation.",ordered:!1,children:[]},{text:"**Typed business selection** - added generic server-backed `SdEntityPicker` and static/lazy `SdTreeSelect` controls with stable-key selection, hydration, templates, responsive layout, keyboard behavior and form integration.",ordered:!1,children:[]},{text:"**Safe workflow and operations primitives** - added scoped `SdUnsavedChangesService` guards/adapters, shared poll/SSE `SdTaskService`, `SdJobProgress`, and normalized/redacted `SdAuditDiff`.",ordered:!1,children:[]},{text:"**Graph persistence foundation** - added the public `SdGraphSerializer` plus identity/envelope/storage-adapter entrypoint used by cache and storage to preserve `Date`, `Map`, `Set`, shared references and cycles with bounded validation.",ordered:!1,children:[]},{text:"**Complete PDF workflows** - added continuous scroll, printing adapters, outline navigation, bounded thumbnails/search, browser capability tokens and consumer-facing PDF state/event types.",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed (BREAKING for consumers)",level:3,anchor:"release-1-4-changed-breaking-for-consumers",markdown:"- **Angular signal input/output contracts** - migrated compatible component inputs and outputs to `input()` and `output()` across Angular 19/20/21 while preserving template binding names and behavior. Direct class consumers and tests must read migrated inputs as signals (for example `component.value()`), set them through Angular's `ComponentRef.setInput(...)`, and use the output `emit()`/`subscribe()` contract instead of RxJS-only `EventEmitter` APIs.",items:[{text:"**Angular signal input/output contracts** - migrated compatible component inputs and outputs to `input()` and `output()` across Angular 19/20/21 while preserving template binding names and behavior. Direct class consumers and tests must read migrated inputs as signals (for example `component.value()`), set them through Angular's `ComponentRef.setInput(...)`, and use the output `emit()`/`subscribe()` contract instead of RxJS-only `EventEmitter` APIs.",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed",level:3,anchor:"release-1-4-changed",markdown:"- **Angular dependency injection internals** - migrated compatible constructor-injected dependencies to field-level `inject()` with compatibility constructors where public inheritance may depend on the existing class shape. Runtime constructor arguments and classes instantiated directly outside Angular injection context remain constructor-based.\n- **Signal migration safety** - retained setter/aliased inputs and outputs whose listener/type semantics cannot be migrated safely yet, including outputs that rely on `EventEmitter.observed`; these exceptions preserve current runtime behavior.\n- **Form lifecycle foundation** - consolidated registration, dynamic parent/name changes, model/control synchronization, validation state and destroy cleanup behind `\u0275sdFormControlConnector` while retaining existing template bindings.\n- **API request safety** - GET requests deduplicate by default; mutations deduplicate/retry only through explicit opt-in. Added typed PATCH, per-caller `AbortSignal`, bounded backoff and deterministic in-flight cleanup; response generics now default to `unknown`.\n- **Loading ownership** - `start()` now returns an idempotent ref, overlapping owners are reference-counted, `run()` scopes async work, and browser DOM/style state is shared and cleaned up safely.\n- **Cache and storage persistence** - activated `SD_CACHE_CONFIG`, added namespace/version/custom serializers, exact cached-absence snapshots, load coalescing, typed handle teardown, legacy JSON migration and SSR/quota/corruption containment.\n- **Responsive layout behavior** - layout responds live to viewport signals while legacy responsive services/tokens remain supported as adapters.\n- **Release documentation rollout** - the multi-version sync now treats the canonical npm README and i18n quality scripts as managed release inputs, while the sync guard fails closed on README, package-version, script or generated-workspace drift.",items:[{text:"**Angular dependency injection internals** - migrated compatible constructor-injected dependencies to field-level `inject()` with compatibility constructors where public inheritance may depend on the existing class shape. Runtime constructor arguments and classes instantiated directly outside Angular injection context remain constructor-based.",ordered:!1,children:[]},{text:"**Signal migration safety** - retained setter/aliased inputs and outputs whose listener/type semantics cannot be migrated safely yet, including outputs that rely on `EventEmitter.observed`; these exceptions preserve current runtime behavior.",ordered:!1,children:[]},{text:"**Form lifecycle foundation** - consolidated registration, dynamic parent/name changes, model/control synchronization, validation state and destroy cleanup behind `\u0275sdFormControlConnector` while retaining existing template bindings.",ordered:!1,children:[]},{text:"**API request safety** - GET requests deduplicate by default; mutations deduplicate/retry only through explicit opt-in. Added typed PATCH, per-caller `AbortSignal`, bounded backoff and deterministic in-flight cleanup; response generics now default to `unknown`.",ordered:!1,children:[]},{text:"**Loading ownership** - `start()` now returns an idempotent ref, overlapping owners are reference-counted, `run()` scopes async work, and browser DOM/style state is shared and cleaned up safely.",ordered:!1,children:[]},{text:"**Cache and storage persistence** - activated `SD_CACHE_CONFIG`, added namespace/version/custom serializers, exact cached-absence snapshots, load coalescing, typed handle teardown, legacy JSON migration and SSR/quota/corruption containment.",ordered:!1,children:[]},{text:"**Responsive layout behavior** - layout responds live to viewport signals while legacy responsive services/tokens remain supported as adapters.",ordered:!1,children:[]},{text:"**Release documentation rollout** - the multi-version sync now treats the canonical npm README and i18n quality scripts as managed release inputs, while the sync guard fails closed on README, package-version, script or generated-workspace drift.",ordered:!1,children:[]}],children:[]},{key:"fixed",title:"Fixed",level:3,anchor:"release-1-4-fixed",markdown:"- **`sd-table` empty-result reload** - kept a configured reload action enabled when the current items or total are zero, allowing users to retry without changing export or paginator behavior.\n- **Showcase loading documentation manifest** - aligned the generated loading-service example count with the current showcase source so documentation generation no longer reports a stale manifest expectation. (#15)\n- **Published no-op and lifecycle paths** - implemented the previously inert PDF print/outline/continuous APIs and hardened modal/drawer/tab close hooks, task cancellation, picker races and tree cascade/error behavior.\n- **Accessibility and resource cleanup** - added semantic breadcrumb/data-state/progress/audit markup, focus/keyboard handling, bounded PDF work, SSR guards and deterministic listeners/timers/subscriptions/EventSource teardown across the new surfaces.\n- **Form expression evaluation** - corrected `NextDay` handling so relative expressions remove the `NextDay` token instead of the unrelated `LastDay` token before evaluation.\n- **Showcase release and layout demos** - preserved repeated populated changelog groups such as breaking and non-breaking `Changed` sections, and made the Layout Desktop/Mobile controls switch the actual rendered sidebar through the scoped viewport adapter.",items:[{text:"**`sd-table` empty-result reload** - kept a configured reload action enabled when the current items or total are zero, allowing users to retry without changing export or paginator behavior.",ordered:!1,children:[]},{text:"**Showcase loading documentation manifest** - aligned the generated loading-service example count with the current showcase source so documentation generation no longer reports a stale manifest expectation. (#15)",ordered:!1,children:[]},{text:"**Published no-op and lifecycle paths** - implemented the previously inert PDF print/outline/continuous APIs and hardened modal/drawer/tab close hooks, task cancellation, picker races and tree cascade/error behavior.",ordered:!1,children:[]},{text:"**Accessibility and resource cleanup** - added semantic breadcrumb/data-state/progress/audit markup, focus/keyboard handling, bounded PDF work, SSR guards and deterministic listeners/timers/subscriptions/EventSource teardown across the new surfaces.",ordered:!1,children:[]},{text:"**Form expression evaluation** - corrected `NextDay` handling so relative expressions remove the `NextDay` token instead of the unrelated `LastDay` token before evaluation.",ordered:!1,children:[]},{text:"**Showcase release and layout demos** - preserved repeated populated changelog groups such as breaking and non-breaking `Changed` sections, and made the Layout Desktop/Mobile controls switch the actual rendered sidebar through the scoped viewport adapter.",ordered:!1,children:[]}],children:[]},{key:"deprecated",title:"Deprecated",level:3,anchor:"release-1-4-deprecated",markdown:"- **API compatibility aliases** - deprecated `SdApiOption.autoCache` in favor of `dedupe` and `SD_API_CONFIGURATION` in favor of `SD_API_CONFIG`.\n- **Generic cache callback providers** - deprecated directly typed legacy callbacks; migrate to globally safe `unknown` callbacks or use `adaptLegacySdCacheCallbacks()` temporarily with a runtime guard.",items:[{text:"**API compatibility aliases** - deprecated `SdApiOption.autoCache` in favor of `dedupe` and `SD_API_CONFIGURATION` in favor of `SD_API_CONFIG`.",ordered:!1,children:[]},{text:"**Generic cache callback providers** - deprecated directly typed legacy callbacks; migrate to globally safe `unknown` callbacks or use `adaptLegacySdCacheCallbacks()` temporarily with a runtime guard.",ordered:!1,children:[]}],children:[]}]},{id:"release-1-3",title:"Release 1.3",suffix:"1.3",date:"2026-07-18",anchor:"release-1-3",unreleased:!1,packageVersions:[{angularMajor:19,version:"19.1.3"},{angularMajor:20,version:"20.1.3"},{angularMajor:21,version:"21.1.3"}],summaryMarkdown:"Release suffix `1.3` publishes `19.1.3`, `20.1.3`, and `21.1.3` as a stable release across the maintained Angular lines.",markdown:"Release suffix `1.3` publishes `19.1.3`, `20.1.3`, and `21.1.3` as a stable release across the maintained Angular lines.\n\n### Added\n\n- **Opt-in clearable controls** - added `clearable` support to text, number, color, date, and datetime controls, and enabled clear actions for inline-column and external table filters. Existing behavior remains unchanged unless the option is enabled. (1176664)\n- **Shared datetime picker integration** - replaced the remaining vendored datetime picker implementation with `@sdcorejs/angular-material-datetime@1.0.3`, synchronized the integration across Angular 19/20/21, and preserved model refresh behavior with focused regression coverage. (f66de9c)\n- **Showcase release discovery** - added SDCoreJS branding, social metadata, indexable GitHub Pages route shells, and a version-aware documentation experience across the maintained workspaces. (#8, 8f334f6)\n\n### Changed (BREAKING for consumers)\n\n- **Removed the AuthOM integration** - deleted `@sdcorejs/angular/modules/authom` and all related Showcase and published-document references, with no compatibility stub. **Migration:** remove AuthOM imports and migrate each use case to the supported `auth`, `keycloak`, `permission`, `layout`, or `icon` entry point as appropriate. (dfced07)\n\n### Changed\n\n- **Type-only barrel exports** - marked 46 type-only wildcard re-exports explicitly with `export type *` while preserving all public type names and runtime value exports across Angular 19/20/21. (#12)\n- **Showcase documentation navigation** - refreshed maintainer attribution and navigation while preserving legacy routes and stable fragment IDs. (#9)\n\n### Fixed\n\n- **Loading overlays on duplicate hosts** - loading start, stop, and state checks now update every matching host, including duplicated router-tab hosts. (#11)\n- **Table selection and tab reload behavior** - select-all skips disabled rows and keeps header state synchronized; forced tab reload recreates existing tabs even for same-URL navigation. (43dfed1)\n- **Form-builder drag/drop feedback** - stabilized palette previews and live placeholder movement so only one preview is rendered and drops follow the active CDK position. (2b7d644, 7b774f5, 2468118)",sections:[{key:"added",title:"Added",level:3,anchor:"release-1-3-added",markdown:"- **Opt-in clearable controls** - added `clearable` support to text, number, color, date, and datetime controls, and enabled clear actions for inline-column and external table filters. Existing behavior remains unchanged unless the option is enabled. (1176664)\n- **Shared datetime picker integration** - replaced the remaining vendored datetime picker implementation with `@sdcorejs/angular-material-datetime@1.0.3`, synchronized the integration across Angular 19/20/21, and preserved model refresh behavior with focused regression coverage. (f66de9c)\n- **Showcase release discovery** - added SDCoreJS branding, social metadata, indexable GitHub Pages route shells, and a version-aware documentation experience across the maintained workspaces. (#8, 8f334f6)",items:[{text:"**Opt-in clearable controls** - added `clearable` support to text, number, color, date, and datetime controls, and enabled clear actions for inline-column and external table filters. Existing behavior remains unchanged unless the option is enabled. (1176664)",ordered:!1,children:[]},{text:"**Shared datetime picker integration** - replaced the remaining vendored datetime picker implementation with `@sdcorejs/angular-material-datetime@1.0.3`, synchronized the integration across Angular 19/20/21, and preserved model refresh behavior with focused regression coverage. (f66de9c)",ordered:!1,children:[]},{text:"**Showcase release discovery** - added SDCoreJS branding, social metadata, indexable GitHub Pages route shells, and a version-aware documentation experience across the maintained workspaces. (#8, 8f334f6)",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed (BREAKING for consumers)",level:3,anchor:"release-1-3-changed-breaking-for-consumers",markdown:"- **Removed the AuthOM integration** - deleted `@sdcorejs/angular/modules/authom` and all related Showcase and published-document references, with no compatibility stub. **Migration:** remove AuthOM imports and migrate each use case to the supported `auth`, `keycloak`, `permission`, `layout`, or `icon` entry point as appropriate. (dfced07)",items:[{text:"**Removed the AuthOM integration** - deleted `@sdcorejs/angular/modules/authom` and all related Showcase and published-document references, with no compatibility stub. **Migration:** remove AuthOM imports and migrate each use case to the supported `auth`, `keycloak`, `permission`, `layout`, or `icon` entry point as appropriate. (dfced07)",ordered:!1,children:[]}],children:[]},{key:"changed",title:"Changed",level:3,anchor:"release-1-3-changed",markdown:"- **Type-only barrel exports** - marked 46 type-only wildcard re-exports explicitly with `export type *` while preserving all public type names and runtime value exports across Angular 19/20/21. (#12)\n- **Showcase documentation navigation** - refreshed maintainer attribution and navigation while preserving legacy routes and stable fragment IDs. (#9)",items:[{text:"**Type-only barrel exports** - marked 46 type-only wildcard re-exports explicitly with `export type *` while preserving all public type names and runtime value exports across Angular 19/20/21. (#12)",ordered:!1,children:[]},{text:"**Showcase documentation navigation** - refreshed maintainer attribution and navigation while preserving legacy routes and stable fragment IDs. (#9)",ordered:!1,children:[]}],children:[]},{key:"fixed",title:"Fixed",level:3,anchor:"release-1-3-fixed",markdown:`- **Loading overlays on duplicate hosts** - loading start, stop, and state checks now update every matching host, including duplicated router-tab hosts. (#11)
- **Table selection and tab reload behavior** - select-all skips disabled rows and keeps header state synchronized; forced tab reload recreates existing tabs even for same-URL navigation. (43dfed1)
- **Form-builder drag/drop feedback** - stabilized palette previews and live placeholder movement so only one preview is rendered and drops follow the active CDK position. (2b7d644, 7b774f5, 2468118)`,items:[{text:"**Loading overlays on duplicate hosts** - loading start, stop, and state checks now update every matching host, including duplicated router-tab hosts. (#11)",ordered:!1,children:[]},{text:"**Table selection and tab reload behavior** - select-all skips disabled rows and keeps header state synchronized; forced tab reload recreates existing tabs even for same-URL navigation. (43dfed1)",ordered:!1,children:[]},{text:"**Form-builder drag/drop feedback** - stabilized palette previews and live placeholder movement so only one preview is rendered and drops follow the active CDK position. (2b7d644, 7b774f5, 2468118)",ordered:!1,children:[]}],children:[]}]},{id:"release-1-2",title:"Release 1.2",suffix:"1.2",date:"2026-07-11",anchor:"release-1-2",unreleased:!1,packageVersions:[{angularMajor:19,version:"19.1.2"},{angularMajor:20,version:"20.1.2"},{angularMajor:21,version:"21.1.2"}],summaryMarkdown:"Release suffix `1.2` publishes `19.1.2`, `20.1.2`, and `21.1.2` as a stable patch across the maintained Angular lines.",markdown:"Release suffix `1.2` publishes `19.1.2`, `20.1.2`, and `21.1.2` as a stable patch across the maintained Angular lines.\n\n### Fixed\n\n- **`sd-table` hidden-paginator height** - contained Material's 48px footer-action touch targets without forcing extra height when no action is rendered, preventing the table host from gaining a redundant outer vertical scrollbar for short data sets. The fix is synchronized across Angular 19/20/21 and avoids relational selectors for compatibility with the supported browser baseline. (#6)",sections:[{key:"fixed",title:"Fixed",level:3,anchor:"release-1-2-fixed",markdown:"- **`sd-table` hidden-paginator height** - contained Material's 48px footer-action touch targets without forcing extra height when no action is rendered, preventing the table host from gaining a redundant outer vertical scrollbar for short data sets. The fix is synchronized across Angular 19/20/21 and avoids relational selectors for compatibility with the supported browser baseline. (#6)",items:[{text:"**`sd-table` hidden-paginator height** - contained Material's 48px footer-action touch targets without forcing extra height when no action is rendered, preventing the table host from gaining a redundant outer vertical scrollbar for short data sets. The fix is synchronized across Angular 19/20/21 and avoids relational selectors for compatibility with the supported browser baseline. (#6)",ordered:!1,children:[]}],children:[]}]}];export{e as a};
