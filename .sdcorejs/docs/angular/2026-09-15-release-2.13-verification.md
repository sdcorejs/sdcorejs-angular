---
artifact_id: release-2.13-verification
artifact_kind: execution-doc
change_ref: inform-2.13
source_spec: none
source_plan: .sdcorejs/plans/angular/2026-09-15-inform-tip.md
commit_policy: with-change
owner: angular-integration
---

# Release 2.13 verification

Scope: merge the current Inform fixes, tip variant, API documentation and seven new showcase sections into main; prepare suffix 2.13 for Angular 19/20/21/22. Base: `47a45e4ff815559dd3c56d1a1cd9fa913d25e94e`.

## Package review

- Built and packed all four `*.2.13` packages with exact Node 22.22.3 using `deploy.ps1 -SkipInstall -DryRun`; no npm publication. Package versions are stamped during release staging, matching the existing repository convention.
- Compared against the actual same-major npm `*.2.12` tarballs. Dependency maps, export maps and packed file paths are identical.
- Declaration changes are limited to Inform: optional `type` input, exported `SdInformType`, and the `note` role return value. Authored sourcemap changes are limited to Inform component/template and the icon alias map.
- Repository-owned snapshot `scripts/release-contracts/2.13.json` records exact fingerprints. Integrity, metadata, source binding, manifest and public-surface validation passed; unchanged strict consumers compiled against all four candidates.
- Workflow tag/version pins, baseline, page retention, contract tests and generated changelog now target 2.13. Existing published archives remain unchanged.

## Verification

- Repository script suite and sync guard: passed.
- All four library builds: passed.
- Showcase: 206 tests passed; production build passed with existing CommonJS notices.
- Full library lint: passed on v19/v20/v21/v22.
- Full coverage suites: 5,463 tests passed on each of v19/v20/v21/v22 (21,852 successful executions). Statements 82.33%, branches 72.20–72.21%, functions 80.74–80.76%, lines 83.51%.
- Final diff/text hygiene: passed. All task-owned durable artifacts included; raw diagnostics excluded.

## Repairs during verification

- Normalized line endings in the Inform icon regression test and synchronized mirrors; lint was rerun.
- Updated the showcase registry tests from 362 to 369 sections to include the seven added examples. A transient text-encoding issue in this test edit was corrected before the passing rerun.
- The standalone Node runtime needed an explicit local npm CLI path for package verification on Windows; the rerun completed successfully.

Raw logs, retained tarballs and browser diagnostics remain outside the repository in the local Temp directory. Earlier Inform verification notes describe their completed pre-release checkpoints; this report supersedes their statement that derived lines were not built/tested.
