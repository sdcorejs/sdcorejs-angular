---
artifact_id: execution-doc:sdcorejs-angular:release-2.6
artifact_kind: execution-doc
change_ref: release-2.6-20260908
source_spec: none
source_plan: none
commit_policy: with-change
owner: sdcorejs-ship
track: angular
status: prepared
---

# Chuẩn bị release 2.6

Phạm vi được yêu cầu: chuẩn bị bump sau khi PR #43 đã merge. Nhánh
`chore/prepare-v2.6` bắt đầu từ `main` tại
`4ce4d7fa9ca819ed82a1af26fffe690d37cd9481`. Version mục tiêu là `19.2.6`,
`20.2.6`, `21.2.6`, `22.2.6`; tag dự kiến `v2.6`.

Đã tăng manifest canonical v19 và sinh v20/v21/v22 bằng `npm run sync`.
Changelog có mục `2.6` ngày 2026-09-08 và một mục Unreleased rỗng mới.
Workflow pin tag, artifact, baseline và retention cho release này. Angular 22
đối chiếu với `22.2.5`; fallback sang Angular 21 chỉ còn áp dụng cho release
khởi đầu `22.2.5`. Hai regression test tái hiện lỗi baseline/latest trước khi sửa
và pass sau sửa, đồng thời vẫn kiểm tra hợp đồng release 2.5.

Snapshot `scripts/release-contracts/2.6.json` được đối chiếu với tarball npm 2.5
và package build từ source đã merge. Chỉ thêm export `utilities/read-state`;
không xóa export, declaration file hoặc packed file nào. Đã đọc các thay đổi
declaration của read-state, Confirm, UploadFile, Table và Import Excel. Cả 24
fingerprint baseline/candidate đều khớp sau khi clean-install và build lại.

## Xác minh

Các build/pack/consumer chạy trên Node `22.22.3`; pack và consumer dùng npm
`11.5.1`. Dependency và lockfile của repository không thay đổi. Đã xác nhận
Angular, compiler, ng-packagr, TypeScript và axe-core cài đặt khớp lockfile của
cả bốn workspace. Source component/form/service không đổi trong lượt chuẩn bị;
manifest package chỉ đổi version.

| Kiểm tra | Kết quả |
| --- | --- |
| `npm run check:sync` | Pass, v20/v21/v22 khớp canonical v19 |
| `npm run test:scripts` | 152/152 pass, gồm release contract và workflow |
| `npm run lint:release` | Pass cả v19/v20/v21/v22 |
| `npm run build` tại bốn workspace | Pass; v19/v20/v21 đã build lại sau clean-install |
| Full coverage v19 và v22 | 5.291/5.291 mỗi dòng, CI trên chính commit main đã merge |
| `npm run test:ci` tại v20 và v21 | 5.291/5.291 mỗi dòng, local sau clean-install |
| Package contract với `--suffix 2.6 --baseline-suffix 2.5 --datetime-version 1.0.4 --compile-consumers` | Pass cả bốn tarball sau build lại: integrity, manifest, public surface và strict consumer compilation |
| Showcase `npm run build:prod` | Pass; có cảnh báo CommonJS của dependency hiện có |
| Showcase `npm test` | 206/206 pass |
| Registry npm | Chưa có cả bốn version `*.2.6` tại lúc kiểm tra |

CI main: https://github.com/sdcorejs/sdcorejs-angular/actions/runs/34195752375
(6/6 jobs thành công).

Ba test Showcase ban đầu còn đếm 98 trang/355 ví dụ. Source đã có 99 trang/356
ví dụ và sáu demo data-state; đã cập nhật kỳ vọng và kiểm tra rõ import/doc path
read-state. Không sửa hoặc bỏ assertion uniqueness, navigation hay example
loading. Các lần test v20/v21 ban đầu thiếu axe-core do node_modules cũ; cài đúng
lockfile giải quyết lỗi môi trường, không sửa test thư viện để né lỗi.

## Phạm vi bàn giao

Biên bản được lập ở thời điểm chuẩn bị local, trước commit, push, PR và tag.
Sau khi nhận kết quả kiểm tra và lưu ý audit, người dùng yêu cầu commit, push
và đánh tag. Phạm vi tiếp theo gồm đưa release commit vào main rồi đẩy `v2.6`
để kích hoạt workflow publish bốn package.
Không sửa archive `published-docs`/`published-pages`; workflow sẽ sinh chúng sau
khi publication thành công. Không bump repo legacy `@sd-angular/core` trong
phạm vi tiếp nối PR #43 này.

Audit workspace v22 báo 13 advisory (9 moderate, 4 high). Bốn high nằm trong
chuỗi dev-only `prettier-eslint`/parser/typescript-estree/minimatch của lockfile
cũ, không thuộc dependency runtime được publish. Việc xử lý dependency này
chưa nằm trong lượt bump và cần được xem xét khi quyết định phát hành.
Trạng thái publication: `NOT_PUBLISHED`; biên bản này không khẳng định đã có
release commit hoặc tag trên remote.
