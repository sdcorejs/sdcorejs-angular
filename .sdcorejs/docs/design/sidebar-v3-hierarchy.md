# Sidebar V3 implementation ledger

Continuation 2026-09-11: integrate main `d16b5c81`, resolve generated sync metadata and release suffix 2.8. See [the release integration record](../releases/2.8.md) for current combined-source verification. Unpublished status and artifact hashes below describe the original 2026-09-10 preflight.

Owner: sdcorejs-angular / core-ui-layout. Track: design. Source revision: f65ba0fab2d5c14721d11c4f8a2dc68763c8b278.
Requirement: user request 2026-09-10, Sidebar V3 improvement using recent V1 and root-menu icons only. Status: implementation direction under direct user authorization; no separate approved spec/plan or approval hash is claimed.

Artifact context: design spec, decisions and editable wireframe are task-owned source artifacts under .sdcorejs/design. This ledger is task-owned traceability under .sdcorejs/docs/design. Local test logs and build tarballs remain ignored.

Loading prerequisite: PR #53 merged to main at f65ba0fa before this branch was created. npm 2.8 remains unpublished.

## Implementation and verification

- Canonical v19: opt-in shared tree presentation (`default`, `hierarchy`, `shortcuts`), compact shared search surface, V3 header/footer/scroll containment and root-only menu icons. Defaults retain V2/mobile appearance.
- All mirrors generated by `npm run sync`. No new dependencies, routes, menu DTOs, services or storage keys. Existing navigation/pin/collapse events remain unchanged.
- Full canonical Chrome suite: 5,342 pass. Coverage: statements 81.62%, branches 71.60%, functions 79.92%, lines 82.74%.
- Layout Chrome suites: 197 pass on each Angular 20, 21 and 22. Canonical layout suite passed before the final full run.
- Showcase: 206/206 Chrome tests and production build pass after the final fixture containment change (existing CommonJS warnings only).
- Canonical lint and targeted lint on the changed TS files in v20/v21/v22: pass. Four-line sync guard and 158 repository script tests: pass.
- Four build/pack artifacts and strict package/consumer compile contracts: pass; exports and file inventories stay unchanged. Snapshot includes default-preserving shared layout inputs and the already documented main upload-file API removal.
- Console retained patch: apply-check passes against the final v20 build. A folder-scoped `.gitattributes` protects LF patch context on Windows without changing the five retained diff blocks.
- Red evidence: three initial V3 regressions failed before implementation; the later long-label test failed with the real Core `text-ellipsis !important` utility, then passed after V3 stopped applying that utility.

## Browser verification

Live Showcase on local port 4319, built from canonical dist. Checked first-level icons, text-only nested/flattened rows, three-level branch guides, compact search, keyboard focus on pin controls, pin/unpin, collapse/expand retaining the query, and account footer. The dev-server's prebundle mode initially duplicated library tokens; preview succeeded with `--prebundle=false`. Published documentation fetch is unavailable in this local server; live components run independently.

The Showcase fixture constrains V3 to its transformed preview frame, matching the existing V1 containment rule, so focusing a deep menu does not move the whole clipped shell. Demo translations include the section and pin labels, and Settings includes a third-level branch.

The PNG is an actual browser capture of the live V3 fixture with navigation scrolled to the deeper branches. It is not a generated mockup. The editable HTML is a separate design reference with generic account data and existing repository fonts.

## Delivery

Branch: `feat/sidebar-v3-hierarchy`, based on merged main `f65ba0fa`. Loading is merged in PR #53. Sidebar is a separate reviewable change; npm `20.2.8` has not been published, and no Console deployment was performed.

Local logs remain in ignored `tmp/sidebar-*`. Artifacts: `C:/Users/Admin/AppData/Local/Temp/sdcorejs-angular-sidebar-2.8/v{19,20,21,22}/`. CI must rebuild from the eventual release commit before tagging/publishing.

v20.2.8 tarball SHA-256: `9a7f703509e27006da205ccf01c56364ee62913d7b34ac1e6304fa4f728f164b`.

Artifact `.sdcorejs/design/wireframes/sidebar-v3-hierarchy/expanded.html`, SHA-256: `9af77bb3e0b7177f19cfb8add2efa2fa3e66485d2689ed51247398384db31fbf`.

Artifact `.sdcorejs/design/exports/png/sidebar-v3-hierarchy/expanded.png`, SHA-256: `9226b9900bad478b78bb1c4d33d6dac9da9eae12e336122874fd36519e4f56b7`.
