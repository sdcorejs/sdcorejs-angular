# Release 2.8 integration record

Date: 2026-09-11. Owner: sdcorejs-angular. Scope: direct user request to resolve Sidebar V3 PR #54 conflicts, merge main, then release suffix 2.8. No separate approved spec or plan is claimed.

## Source and conflict resolution

- Sidebar V3 source: `1d7236d1c38d56d6d2c0b866d5af77cde950bf57`, PR #54.
- Integrated main: `d16b5c81`, including PR #52 table-settings checkbox and small-control label alignment. Loading ownership fix from PR #53 is retained.
- Conflicts were limited to generated `Updated At` rows in the four `SYNC-STATUS.md` files. Regenerated them with `npm run sync`; production code merged without conflicts. Preserve both sets of changes and all Angular-major-specific compatibility shims.
- Move the newly merged Unreleased fixes into the 2.8 changelog and date the release 2026-09-11. Manifests already target `19.2.8`, `20.2.8`, `21.2.8`, `22.2.8`; no 2.9 bump is intended.
- Exports and packed file inventories remain unchanged from the exact same-major 2.7 baselines. Keep the documented upload-file `appearance` removal and its migration; the loading fix itself changes no API.

## Verification

- Exact Node 22.22.3 throughout local release verification.
- Full real Chrome suite with coverage on Angular 19, 20, 21 and 22: 5,346 pass per line, 21,384 total. Canonical coverage: statements 81.63%, branches 71.62%, functions 79.98%, lines 82.75%.
- `npm run lint:release`: all four workspaces pass. `npm run test:scripts`: 158 pass. `npm run check:sync`: pass.
- Four build/pack artifacts pass integrity and npm collision checks using `scripts/deploy.ps1 -PatchVersion 2.8 -SkipInstall -DryRun`.
- Strict package contracts and isolated consumer compilation pass for all four candidates (`consumersCompiled: true`). Rebuilding the combined source confirms the snapshot fingerprints remain unchanged; update its source provenance to record both integrated parents.
- Showcase: 206/206 Chrome tests and production build pass, with existing CommonJS dependency warnings.
- Console patch apply-check passes against the combined v20 build, preserving exactly five blocks. The loading block is absent.
- No conflict markers, whitespace errors, invalid UTF-8 or likely credentials in included paths. The initial tree was clean; all changed paths are integration-owned.

Local logs: ignored `tmp/merge-2.8-*.log`. Preflight tarballs: `C:/Users/Admin/AppData/Local/Temp/sdcorejs-angular-merged-2.8/v{19,20,21,22}/`. Local builds reused installed workspace dependencies; the release workflow performs clean installs before publication. The isolated consumer checks also install their own dependencies.

## Publication and Console handoff

The source-freeze record precedes publication. Merge the verified PR into main, tag that main commit `v2.8`, and push the tag to run the existing trusted-publishing workflow. Publication is successful only after the entire transaction, including generated docs/page, is green. Do not publish locally, replace an existing tag, or hand-edit release archives.

The authoritative release status is the GitHub Actions run for tag `v2.8`, together with npm versions and dist-tags. Local preflight hashes are not npm artifact hashes; CI builds immutable artifacts from the tagged commit.

Enterprise Console uses `@sdcorejs/angular@20.2.8`. Follow [the migration guide](../angular/loading-overlay-2.8/README.md): remove only the loading diff block, retain the five configuration/layout/tab-router blocks, update the exact-version guard, and run clean-install and Console regressions. This task does not deploy Console.

Artifact closure: this record and the existing loading/sidebar ledgers are required task-owned documentation. Generated workspace metadata and the release contract snapshot belong to this integration. Temporary logs, helper scripts and tarballs remain excluded.
